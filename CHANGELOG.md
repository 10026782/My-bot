# Changelog

All notable repository-level changes should be recorded here.

## Unreleased

- **PR #570 — F14-B1: legacy Contact caller migration** (`tools/contact_resolver.py`, `tma_api.py`, `daily_digest.py`, test support files; merge `ff9b574` on 09/08/2026) — F14 workstream Phase B1: migrated legacy `find_or_create_contact()` callers through new `require_contact_resolution_gate()` which is integrated into `ActionGateway.propose_action()`. New callers are filtered at the gate; existing callers flagged for manual review. Gate remains always-on (not flag-gated) to enforce entity resolution upfront before approval. Zero behavioral change to user-facing approval flow yet (Phase B2 wiring deferred). Verified in integration against #570 test suite. **Not flag-gated** — structural hardening, live but enforcement gated by Phase B2 completion.

- **PR #569 — TC6 integrator: `app.py` reply ownership cutover** (`app.py`, `test_tc6_app_reply_ownership.py`, `docs/architecture/turn-coordinator-full/TC6_APP_INTEGRATOR_PATCH_SPEC.md`; merge `d0a8620` on 09/08/2026, doc Status update `72e8923` on 09/08/2026) — Turn Coordinator workstream TC6: exact-contract correlation in `app.py` integrator seam (producer → tool-loop early return). Producer's `_queue_approval_detailed_impl()` now derives reply_owner from real `ActionLifecycleResult` via `reply_ownership_for_contract()` (WS2); Branch A (success) suppresses Agent, Branch B (verification failure) fails closed with shadow-evidence observation to RP4. All gated behind `FEATURE_SINGLE_SPEAKER_APPROVAL_UX` flag; byte-identical legacy behavior when flag OFF. Includes 50-test verification suite (18 tool-loop + 32 producer-level rollback) and Cross-Layer Impact Matrix gate (Layers 1 & 3 untouched; Layers 2 & 4 indirectly touched per documented scope). **Not flag-gated yet** — code merged, flag default-off; replaces legacy `_signal_reply_owner` derivation when activated.

- **PR #564 — TC4: lead builders and integration seam** (`core/turn_coordinator_runtime.py`, `lead_capture.py`, test support; merge `26e8434` on 08/08/2026) — Turn Coordinator workstream TC4: deterministic lead-creation builders and ActionGateway integration seam. `build_create_lead_proposal()` now enforces strict owner/tenant scope validation, captures policy preconditions, and rejects duck-typed identity checks. Foundation for future TC-integrated lead routing. Hardening: mirror TC4/lead fix applied to `prepare_task_proposal()` validation (PR #565 `6566fee`). **Not flag-gated** — structural code, unwired.

- **PR #562 — TC5: entity resolver framework** (`core/turn_coordinator_runtime.py`, `tools/contact_resolver.py`, `core/action_gateway.py`, test harness; merge `60ee811` on 08/08/2026, plus CodeRabbit follow-up `0dd2e67`) — Turn Coordinator workstream TC5: bounded entity resolver framework with policy-declaration enforcement for AC/session/callback sources. `build_entity_resolver()` factory generates resolver policies scoped to specific entities; AC/callback wrappers now verify entity-source declarations, closing a policy-declaration loophole. Deterministic, side-effect-free; no routing integration yet (Phase 1). CodeRabbit review addressed source-policy specificity; resolver behavior unchanged. **Not flag-gated** — structural code, unwired.

- **PR #552 — BUG-157: atomic fingerprint claim in `ActionGateway.propose_action()`**
  (`core/action_gateway.py`; documentation:
  `docs/architecture/action-gateway/BUG-157_ATOMIC_FINGERPRINT_CLAIM_20260805.md`)
  — `propose_action()` performed fingerprint lookup and save as two
  non-atomic steps; a scheduler-thread caller (`lead_recovery.py`/
  `followup_engine.py`) racing a webhook-thread caller on the same business
  fingerprint could each create their own pending contract. Fixed with a new
  compare-and-set (`claim_fingerprint_cas`/`release_fingerprint_claim`) under
  the existing `ExecutionLedger._lock`, with a bounded retry loop on claim
  loss. No behavior change under single-caller (the common case). 18/18 new
  tests including an 8-thread barrier race. **Not flag-gated** — merged to
  `main`, not yet deployed or production-verified.
- **PR #550 — BUG-153/154/155/156: PR #546 staging validation fixes**
  (`core/router/router.py`, `core/action_gateway.py`, `app.py`; four
  documentation files under `docs/architecture/action-gateway/`) — four bugs
  found during PR #546 staging validation (03/08/2026), fixed the same day:
  explicit `create_task` retry after rejection now opens a new contract
  instead of being blocked as replay (BUG-153); `date_marker.start()` guarded
  against `None`, supporting "ל-\<date\>" phrasing without crashing (BUG-154);
  TTL-expired callback now looks up the `ActionContract` by `contract_id`
  instead of a recomputed fingerprint (BUG-155); `due_time` excluded from the
  `create_task` business-identity fingerprint and the user is now told the
  time wasn't saved, Option B (BUG-156). All four fixes touch the same
  unflagged, always-on `parse_deterministic_create_task()` routing path.
  **Not flag-gated** — merged to `main`, not yet deployed or
  production-verified.
- **PR #549 — docs: clarify explicit retry versus rejected replay**
  (`CHANGELOG.md`,
  `DETERMINISTIC_TASK_ROUTING_AND_REPLAY_POLICY_20260802.md`) —
  documentation-only follow-up distinguishing an explicit new create request
  (should get a new approval attempt) from autonomous replay of an already-
  rejected contract (must remain blocked).
- **PR #546 follow-up — deterministic task routing and replay policy**
  (`core/router/router.py`, `core/action_gateway.py`, `app.py`; documentation:
  `docs/architecture/action-gateway/DETERMINISTIC_TASK_ROUTING_AND_REPLAY_POLICY_20260802.md`)
  — documented prefix/wrapper normalization, structured title/date/time
  identity, clarification-before-Agent behavior for uncertain input, and
  single-speaker suppression when pending was already sent to the same chat.
  It also records the policy gap introduced by PR #540: autonomous rejected
  replay must remain blocked, but an explicit new create request should get a
  new approval attempt while the rejected contract stays immutable. Turn
  Coordinator documentation is intentionally excluded because it was updated
  separately.

> **Note:** PRs #348–#353 (PA-01 saga — see `ROADMAP.md`'s 16/07/2026 entry) are not yet itemized here individually; this is a known, separate documentation gap, not addressed in this update.
> **Note:** PRs #472–#478, #481–#489, #491 (Context Librarian planning/hardening/Consumption-Enforcement-planning trail) are documentation/tooling-planning only and are tracked exhaustively in `ROADMAP.md`'s N17 section rather than itemized here individually — same batching convention as the PA-01 note above.

- **PR #548 — docs: record deterministic routing and replay policy** (documentation only; merge `9a62e6f`) — documented the canonical create/update/complete-task deterministic routing behavior now live in production unflagged (core/turn_coordinator_runtime routing path), including single-speaker and policy gaps discovered during PR #546–548 development (explicit retry vs. rejected replay distinction, scope carve-outs for Agent fallback). Verified directly against `origin/main` code paths, not inferred from PR titles. Closes the routing-policy documentation gap introduced by PR #545's end-to-end wiring.

- **PR #547 — docs(turn-coordinator): align merged PR and workstream status** (documentation only; merge `d26c564`) — updated `docs/architecture/turn-coordinator/README.md` to reflect WS1/WS2/WS3 merged PRs (#536–546), implementation sequence, and current state of Task Coordinator v1/v2/v3 planning. Bridges the gap between PR #545's code merge and its documentation reflection in the canonical architectural readme.

- **PR #545 — feat(turn-coordinator): wire canonical task lifecycle end to end** (`app.py`, `core/router/router.py`, `core/action_gateway.py`, `test_turn_coordinator_end_to_end.py`; merge `46db9af`) — connected deterministic task create/update/complete routing from `parse_deterministic_create_task()` through Turn Coordinator's full task lifecycle, replacing Agent-mediated task fallbacks with structured routing via `core/turn_coordinator_runtime`. Task status, ownership, and approval routing now flows through the canonical TC2 (`TaskRuntimeBuilder`) and TC2-lifecycle (`ActionGateway`) integration, completing the WS2/WS3 runtime wiring. **Not flag-gated** — live in production; behavior identical to pre-PR Agent path for unambiguous structured requests, but ambiguous/textual phrasings now defer to Agent per policy (PR #548 documented the policy gap clarified by this PR).

- **PR #544 — Codex/ws2 lifecycle evidence runtime** (`core/action_gateway.py`, `core/router/route_decision.py`, test support files; merge `228ddec`) — WS2 (lifecycle and evidence projection) runtime integration: `_compose_status_reply_unified()` now imports WS2 projected evidence for comparison/audit and calls `_ws2_evidence_projection()` internally. Result is still discarded in live code (WS2 wiring is incomplete — PR #545 handles the actual behavioral integration), but this PR establishes the internal plumbing and test harness. Part of the Turn Coordinator rollout workstreams (WS1=contracts, WS2=lifecycle, WS3=surfaces). Code is present but zero behavioral effect on approval routing until WS2 is formally wired by a later PR. **Not flag-gated** — live but inert.

- **PR #543 — feat(router): add narrow task proposal integration** (`core/router/router.py`, `core/router/intent_router.py`, supporting test files; merge `4843800`) — Turn Coordinator WS1 narrow-task-proposal integration: structured intent ownership contracts for task creation proposals that don't meet create/update/complete criteria (ambiguous phrasing, agent-deferred cases). New `_narrow_task_proposal_contracts()` returns lightweight `OwnershipContract` stubs for routing to Agent with explicit closure context instead of silent fallback. Narrows the "unhandled task phrasing" category and reduces Agent's scope when deterministic routing can't apply. Part of WS1 (contracts) rollout. **Not flag-gated** — code present, behavioral effect gated by deterministic routing adoption in PR #545+.

- **PR #542 — feat(ws3): integrate WS2 lifecycle and evidence with MessageContract** (`core/action_gateway.py`, `core/message_contract.py`, `test_ws3_*.py`; merge `1901580`) — WS3 (surfaces/adapters) integration with MessageContract: new pure functions mapping `ActionFact`/lifecycle projections to canonical `MessageContract` states. Adapters follow the same pattern as PR #511/C — deterministic, side-effect-free, never validating against live state. Zero production wiring yet (Phase 3), but establishes the adapter glue for WS2/WS3 evidence and lifecycle to flow through MessageContract to the final formatter. Test suite (12/12 passing) covers all transition paths and serialization. **Not flag-gated** — pure code, unwired.

- **PR #540 — fix(router): prevent Agent fallback and rejected-action replay** (`core/router/router.py`, `core/action_gateway.py`, `app.py`, test files; merge `6779c03`) — hardening for create-task deterministic routing: `parse_deterministic_create_task()` now canonicalizes task title identity (normalization, prefix stripping, deduplication) to prevent Agent fallback on cosmetic variations of already-pending requests, and enforces that rejected approvals cannot be autonomously replayed. New `_canonicalize_task_fingerprint()` and `_prevent_rejected_replay()` guards. Closes the replay vulnerability discovered during PR #531 staging. **Not flag-gated** — code live and unflagged, active immediately on merge.

- **PR #539 — feat(router): add WS2 ownership contracts** (`core/router/router.py`, `core/router/ownership_contracts.py`, new test files; merge `abf02ae`) — Turn Coordinator WS2 lifecycle ownership contracts: new `OwnershipContract` variants for lifecycle states (pending, processing, completed, rejected, cancelled) with explicit authorship (Agent vs. Gateway vs. User action). Establishes the semantic base for WS2's lifecycle-evidence projections (PR #544+). Contracts are registered but not yet integrated with routing logic (wiring deferred to PR #544). Part of the TurnCoordinator multi-workstream rollout. **Not flag-gated** — structural code, zero behavioral effect until #544 wires it.

- **PR #538 — feat(turn-coordinator): add deterministic task builders** (`core/turn_coordinator_runtime.py`, `core/router/route_decision.py`, new test suite; merge `809ac0f`) — TC2 workstream builders: `TaskRuntimeBuilder`, `TaskLedgerEntry`, and supporting factories for structured deterministic task creation. Builders are deterministic pure functions; no runtime state mutation. Code present, not yet called (Phase 1: defer to PR #545's end-to-end integration). Part of WS1 (contracts/builders). **Not flag-gated** — pure code, unwired.

- **PR #537 — test(ws3): add cross-surface MessageContract verification harness** (`test_ws3_messagecontract_harness.py`, supporting utilities; merge `4b7fd4f`) — new comprehensive test harness for WS3 surface integration: verifies `MessageContract` output consistency across Telegram and WhatsApp adapters, tests all `MessageState` transitions, and ensures serialization determinism. 18/18 tests passing. No production code changes. Pure test infrastructure for verifying the Phase 3 formatter and adapter wiring (still pending). **Not flag-gated** — test only.

- **PR #536 — feat(turn-coordinator): add intent ownership foundation contracts** (`core/router/ownership_contracts.py`, `core/router/router.py`, new test files; merge `b1c0602`) — TC1 (contracts) workstream foundation: new `OwnershipContract` base schema, `OwnershipDecision` enum (Agent/Gateway/User), and TC1-level intent ownership contracts for create-task, update-task, complete-task. Contracts capture authorship, scope, and authority boundaries; pure data structures, no routing integration yet. Foundation for PR #539–#545 lifecycle layering. **Not flag-gated** — structural code, unwired.

- **PR #534 — docs(context-librarian): add pre-merge receipt ledger** (documentation only; merge `fb4ab4a`) — new `docs/context_librarian/ledger-premerge-approval-ux.json` recording the Phase 2 approval UX design receipt (ownership, scope, evidence, decision dates) as a durable approval record for design-level traceability. Part of Context Librarian governance formalization (PR #525 added post-merge refresh gates; this documents the pre-merge approval step). No code, no runtime change.

- **PR #533 — fix(approval): normalize deterministic status routing** (`core/router/router.py`, `core/action_gateway.py`, supporting test files; merge `843fcf3`) — normalization pass on deterministic status routing: deduplicated pending-query and approval-callback intent routers, unified terminal-status classification, and consistent fingerprinting for cancelled/rejected/completed states. Closes edge cases in the status-routing pipeline discovered during PR #531–532 staging. **Not flag-gated** — code live and unflagged.

- **PR #532 — fix(approval): route cancelled action status deterministically** (`core/router/route_decision.py`, `app.py`, test files; merge `7f4f0e8`) — cancellation handling: new `parse_deterministic_cancelled_task()` intent router recognizing Hebrew/English cancellation phrasings, routing through deterministic ActionGateway path instead of Agent. Prevents Agent from attempting to "fix" a cancelled approval and ensures idempotent cancellation semantics. **Not flag-gated** — code live unflagged.

- **PR #531 — fix(tasks): route unambiguous task creation through canonical gateway** (`core/router/router.py`, `core/action_gateway.py`, `app.py`, new test files; merge `9cbf579`) — root fix for create-task Agent fallback regression: structured `create_task()` calls with title and date now recognize and route through deterministic `parse_deterministic_create_task()` instead of falling back to Agent. Rediscovers and closes BUG-104-adjacent issue (structured input meant for approval was being rerouted to Agent for interpretation). **Not flag-gated** — live in production, effective on merge.

- **PR #530 — [codex] preserve rejected terminal status and replay** (fixes and test support; merge `988c921`) — regression fix: rejected-action terminal status is now correctly preserved across reruns and replay attempts don't incorrectly resurrect rejected contracts. **Not flag-gated** — live.

- **PR #529 — test(approval): align terminal replay regression** (test infrastructure; merge `2a5b2d2`) — test coverage for terminal-status replay regressions: new assertions ensure rejected/cancelled/completed contracts remain terminal under retry/resubmit scenarios. Part of the PR #531–533 staging validation cycle.

- **PR #528 — D-018/D-019: deterministic status routing, bounded reads, and pending-lock lifecycle** (`core/action_gateway.py`, `core/router/route_decision.py`, new architecture docs; merge `da6ab21`) — deterministic status routing foundation (decision D-018/D-019): new `_query_execution_status()` logic, bounded pending-contract reads (24h default), and pending-lock lifecycle semantics to prevent status-query storms. Architecture documented in new `docs/architecture/action-gateway/D-018_STATUS_ROUTING_AND_BOUNDED_READS_20260802.md` and `D-019_PENDING_LOCK_LIFECYCLE_20260802.md`. Code live, not yet integrated with broader router (wiring in PR #531+). **Not flag-gated** — live unflagged.

- **PR #526 — docs: regenerate AI_CONTEXT.md daily briefing, close ROADMAP/CHANGELOG gap for PR #521-524** (documentation only; merge `9048cca`) — first daily briefing regeneration cycle: `AI_CONTEXT.md` rewritten against `origin/main`, ROADMAP.md and CHANGELOG.md updated for PRs #521–#524. Established the recurring documentation-gap-closure pattern later expanded in PR #547 (through #548). Documentation only, no code.

- **PR #525 — feat(context-librarian): harden gates and post-merge refresh** (`tools/context_librarian/librarian.py`, `.github/workflows/ci.yml`, `AGENTS.md`, new governance docs; merge `6c98695`) — Context Librarian governance hardening: new post-merge catalog refresh automation (`refresh-after-merge` CLI, CI gate on main pushes), planning-gate policy formalization (`PLANNING_GATE.md`), and pre-session branch-state validation. Establishes durable, fail-closed bundle freshness guarantees. Adds 475 lines of new documentation and tooling, 91 new test cases. **Not flag-gated** — infrastructure, live on merge.

- **PR #524 — D-018: fix reconfirmation tool_name leak (D-017's OQ4)** (`core/action_gateway.py`, `test_pr0_pending_approval_context_safety.py`; merge `7c3833a`) — `_describe_contract_for_reconfirmation()` fell back to `f"{contract.tool_name} / {table}"` for any non-Airtable tool, leaking raw `tool_name` unconditionally for 9 of the 11 `requires_approval=True` tools, across three surfaces already live in production: `describe_pending_queue()`'s list, `describe_superseded_reason()` (Hotfix E), and `_resolve_single_contract()`'s reconfirmation prompt (`route_confirmation_word()`, BUG-PENDING-APPROVAL-B). Fixed by delegating the final fallback to `_safe_contract_business_description()` (already has full coverage for all 11 tools plus a safe generic fallback), keeping the richer Lead/Task-specific branches unchanged. **Not flag-gated, unlike D-014–D-017 — this changes text already live in production, effective on merge/deploy.** Fixed one stale test assertion (`test_pr0_pending_approval_context_safety.py`'s DoD3/9) that had used `"gmail_send_draft"` in the reply as a proxy for "not just the contract_id," which the fix now correctly avoids.

- **PR #523 — F52 D-014 through D-017: approval_pending wording ratification + Approval Pending Batch Migration** (`core/agent_message_formatter.py`, `core/action_fact_message_adapter.py`, `core/action_gateway.py`, `docs/architecture/f52-unified-approval-runtime/decisions/DECISION_LOG.md`, new `APPROVAL_PENDING_BATCH_MIGRATION_SCOPE_V1.md`, new `RECONFIRMATION_TOOL_NAME_LEAK_FIX_SCOPE_V1.md`, `docs/architecture/f52-unified-approval-runtime/spec/UNIFIED_MESSAGE_UX_STANDARD.md`, `test_agent_message_formatter.py`, `test_f52_status_reply_reconciliation.py`; merge `96a8313`) — **D-014**: owner ratified Option B — "לאשר? כן / לא" stays the canonical, cross-channel wording regardless of inline-button availability; no code change (already shipped). **D-015**: split `approval_pending` wording into a new-prompt variant (`_render_approval_pending()`, task-noun-aware) vs. a new status-query variant (`_render_approval_pending_query()`, registered under a new `STATE_APPROVAL_PENDING_QUERY` constant deliberately outside `core.message_contract.MessageState` — no schema change, no live caller yet). **D-016**: wired `query_execution_status()`'s single-pending-contract branches to a new `ActionGateway._render_pending_query_reply()` (off/shadow/on pattern, direct `format_agent_message_with_meta` call, not through the ActionFact adapter). **D-017**: resolved all five open questions (OQ1–OQ5) from the batch-migration scope doc — count=0 → clean `STATE_IDLE` message, count=1 → D-016's singular wording, count≥2 → shared `STATE_APPROVAL_PENDING_BATCH` renderer via two new helpers (`_render_pending_empty_reply()`, `_render_pending_batch_reply()`); "ActionContracts"/legacy-queue naming dropped from target text entirely; both `describe_pending_queue()` and `query_execution_status()`'s multi-contract branch converge on the same target wording. Raw `tool_name` fallback (OQ4) explicitly deferred to a separate follow-up (D-018, see PR #524). All of D-014–D-017 remain behind `FEATURE_UNIFIED_STATUS_FORMATTER` (shadow/off) — no live runtime change. Includes a CodeRabbit-flagged correction of D-014–D-017's ratification dates (mistakenly dated 02/08/2026, corrected to 01/08/2026) and a markdownlint fence-language fix.

- **PR #522 — fix PR E pending shadow parity gap: task-aware description in MessageContract adapter** (`core/action_gateway.py`; merge `ef55cd9`) — a production shadow probe found `text_differs=True` for outcome `pending` (`legacy_len=56`, `unified_len=83`). Root cause: `_compose_status_reply_unified()` described every pending contract via the generic, table-agnostic `_safe_contract_business_description()` ("הוספת רשומה: ..."), while the real send site (`app.py`'s `_queue_approval_detailed_impl()`, via `build_approval_lifecycle_result().safe_user_message`) already special-cases Task-creation contracts to show the task's own title. Fixed by scoping the same `is_task_creation` check into the adapter's description resolution — content-only fix, no change to approval logic, ownership, queue, evidence authority, routing, or shadow observability. `FEATURE_UNIFIED_STATUS_FORMATTER` remains off; no flag changed.

- **PR #521 — regenerate `AI_CONTEXT.md` daily briefing, close ROADMAP/CHANGELOG gap for PR #517–#520** (`AI_CONTEXT.md`, `ROADMAP.md`, `CHANGELOG.md`; merge `a2be204`) — `AI_CONTEXT.md` rewritten to the 2-page briefing structure (Executive Summary / Current System State / Completed Since Last Update / Next Priorities), verified directly against `origin/main` (`9f203f4`) rather than the previous stale pointer. Added catch-up entries to `ROADMAP.md`/`CHANGELOG.md` for PRs #517–#520 (daily digest lead-temperature summary, PR E MessageContract pending-outcome wiring, Context Librarian budget-enforcement fix), which had landed without the usual documentation entries. Documentation only, no runtime change. Superseded by the 02/08/2026 `AI_CONTEXT.md` regeneration (this same catch-up pattern, now for PR #521–#524).

- **PR #520 — Context Librarian budget enforcement fix + notes-rendering refactor** (`tools/context_librarian/librarian.py`, `docs/context_librarian/task_profiles/profiles.json`, `test_context_librarian.py`; merges `ae7aaba`/`b700fa4`) — fixed token-budget enforcement in the librarian and refactored notes rendering to avoid slicing. Dev tooling only, no runtime change to the business bot. Verified directly against `origin/main` (`9f203f4`).

- **PR #518 ("PR E") — wire pending ActionFact through MessageContract** (`core/action_gateway.py`, `test_f52_status_reply_reconciliation.py`; merge `952bb02`) — `_compose_status_reply_unified()` now routes outcome `pending` (in addition to the existing `failed` from PR D) through the `MessageContract`/`ActionFact` adapter. Still behind `FEATURE_UNIFIED_STATUS_FORMATTER` (shadow/comparison only) — no change to evidence or approval authority. Scope explicitly limited to `pending` alongside `failed`; a broader "PR D" scope attempt did not land.

- **PR #517 — daily digest: complete lead temperature summary** (`daily_digest.py`, `test_daily_digest_scoring_summary.py`; merge `0ddd91c`) — new `_lead_temperature_counts()`/`_leads_scoring_summary()` add a HOT/WARM/COLD lead-count line to the daily digest. Not feature-flagged — active as soon as deployed. **Not yet deployed** — the last documented live Render deploy (`5ec37b8`) predates this commit.

- **PR #516 — documentation catch-up for PRs #506–#515** (`ROADMAP.md`, `CHANGELOG.md`; merge `5ec37b8`) — registered previously-unlogged Context Librarian/MessageContract PRs.

> PRs #517, #518, #520 above were merged with `pre_session_gate.sh --force` (explicit user authorization) while `claude/rp5-staging-fault-injection-v4akit` — an intentional, documented staging-only branch (see its own note earlier in this file) — remained unmerged. No other unmerged `claude/*` branches were left behind by these merges.

- **PR #515 — Context Librarian Non-Raising Dry-Run Estimate Mode** (`tools/context_librarian/librarian.py`, `tools/context_librarian/__main__.py`, `tools/context_librarian/refresh_after_merge.py`, `test_context_librarian.py`; merge `15fe851`) — new `estimate_bundle()` returning frozen `BundleEstimate(task_type, query, fits: bool, actual_tokens, token_budget)` instead of raising on overflow, paired with `estimate_all_profiles()` for batch estimation across all profiles. Pure dry-run (`FEATURE_CONTEXT_LIBRARIAN_ESTIMATE=off` by default, no runtime wiring), no schema/node/boundary change. New CLI subcommand `estimate` with `--task-type` (single profile, default behavior) or `--all-profiles` (mutually exclusive), tab-separated output, exit code 0 if all fit, 2 if any overflow. `_check_budget_overflow()` refactored to call `estimate_bundle()` instead of `build_bundle()` + exception-text matching, with new docstring clarifying why it deliberately doesn't use `estimate_all_profiles()` (needs per-profile error handling + independent continuation on failure). All docstrings translated to Hebrew per repo convention (previous PR used English). Test coverage: 10 new tests for `estimate_bundle/estimate_all_profiles` behavior (99→104, all passing); `test_cli_estimate_all_profiles` fixed to verify that the test query actually over-budgets (was computing but never requiring the condition). **Merged and CI-verified; not yet staging/production-verified (feature-flag-off by default).** Phase 2 (enable flag for real tasks, meter/gate consumption) not in scope. Addresses the need for dry-run estimation without side effects (BUG-104's refresh-after-merge tool needed it; planning gates will also use it).

- **PR #514 — PR D Schema Alignment (Planning Gate SPEC)** (documentation only; merge `61fd89d`) — new `docs/architecture/f52-unified-approval-runtime/rollout/PR_D_SCHEMA_ALIGNMENT_PLANNING_GATE_SPEC.md`. Explicit pre-implementation gate for ActionFact/GatewayReply adapter (PR C) — establishes the three pre-conditions and three decision points before any code change. No runtime change, no code, no node registration.

- **PR #513 — Context Librarian Hebrew Translation Fix** (`test_action_fact_message_adapter.py`, `test_f52_status_reply_reconciliation.py`, `core/action_gateway.py`; merge `aaf8b05`) — translated English comments/docstrings to Hebrew (3 locations in `action_gateway.py`, 4 in test files) to match repo convention. Part of PR #515's broader translation pass. No runtime change.

- **PR #512 — PR C Catalog Registration and Budget Adjustment** (`docs/context_librarian/layers/ux_f52.json`, `docs/context_librarian/task_profiles/profiles.json`; merge `8274dae`) — registered PR C's code_paths/test_paths/canonical_docs into `ux_f52` layer; raised token budgets for `rp5_evidence_mismatch` (4200→4400) and `ux_f52_message` (5700→5900) to accommodate new sources. Measured headroom (~130 tokens margin) to avoid razor-thin fits. No schema change, profile data only.

- **PR #511 — PR C Pure ActionFact Adapter** (`core/action_fact_message_adapter.py`, `test_action_fact_message_adapter.py`, `core/message_contract.py`; merge `f09ce28`) — new pure adapter `from_action_fact()` converting `ActionFact` to `MessageContract` without importing any authority/execution/dispatch modules. Deterministic, side-effect-free mapping: outcome→lifecycle_state; evidence_status + execution_verified → MessageState; error_code as reason_code. Never validates against live `ActionGateway` state or tool availability. User-facing serialization redacts tool names, contract IDs, Airtable record IDs. Test suite covers all outcome/evidence combinations, round-trip serialization, and a module-level import audit proving zero dispatch/approval/authz authority imports. Pending merge: none. **Not yet wired into any live path** (pure function, awaits Phase 3 routing wiring). See `docs/architecture/f52-unified-approval-runtime/rollout/MESSAGE_CONTRACT_ENVELOPE_MIGRATION_PLAN.md` PR C section.

- **PR #510 — Context Librarian Dangling Commit Reference Repair** (`docs/context_librarian/layers/approval_ux.json`, `docs/context_librarian/decisions/canonical_boundaries.json`; merge `ef1ad65`) — repaired dangling `last_verified_commit` references and `approval_ux`'s token budget overflow after PR #509 — no structural change, metadata cleanup.

- **PR #509 — PR B Pure ApprovalLifecycleResult Adapter (Planning Gate + Unwired Registration)** (`core/approval_lifecycle_message_adapter.py`, `test_approval_lifecycle_message_adapter.py`, `docs/context_librarian/decisions/canonical_boundaries.json`; merge `1c51570`) — documentation-only planning gate (`docs/architecture/.../PR_B_APPROVAL_LIFECYCLE_ADAPTER_PLANNING_GATE_SPEC.md`) and pure, unwired adapter registration. `from_approval_lifecycle_result()` delegates to `build_message_contract()` with zero runtime change or feature-flag wiring (Phase 3). Adapter is deterministic and side-effect-free, but only invoked by planning documents and tests, never by live code. Test suite (17/17 passing) covers round-trip serialization and module audit. Registered in `canonical_boundaries.json`'s `message_contract_envelope_authority` decision notes.

- **PR #508 — Context Librarian Catalog Freshness Automation (Refresh-After-Merge Tool)** (`tools/context_librarian/refresh_after_merge.py`, `test_context_librarian.py`; merge `3a77b50`) — new mechanical post-merge catalog refresh: `resolve_commit()` for symbolic refs, `compute_changed_files()` for a diff, `plan_refresh()` pure planning, `apply_refresh()` for disk writes. Narrow scope: bumps `last_verified_commit` only for nodes whose already-registered paths changed; never adds, removes, or edits catalog entries. Fail-closed: any change requiring human judgment (`REVIEW_REQUIRED`, `ERROR`) is left untouched. New CLI subcommand `refresh` with `--base` (before merge), `--head` (after merge), and `--check` (dry-run) or `--apply` (write). 20 new regression tests.

- **PR #507 — Context Librarian Documentation Refresh (Stage 3A Merge)** (documentation only; merge `fd1d559`) — updated `docs/context_librarian/decisions/canonical_boundaries.json` to record the Stage 3A merge (PR #506, `core/message_contract.py` canonical) in decision notes. No code, no nodes, no runtime change.

- **PR #506 — Message Contract Foundation: Core, Schema, Tests (Stage 3A)** (`core/message_contract.py`, `test_message_contract.py`, `docs/context_librarian/layers/ux_f52.json`; merge `6201e86`) — implemented the frozen `MessageContract` (V1/D-012 schema, 15 MessageState values), pure builder `build_message_contract()`, and core test suite (41/41 passing). Zero production wiring (Phase 3), zero feature-flag imports/logic, zero output change. Defines the unified message state contract consumed by TurnCoordinator, RP5, and the UX Formatter. Evidence_status is frozen as optional non-authoritative metadata (D-013) — never determines MessageState, fails closed on unknown values. Foundation for PR B/C adapters and Phase 3 formatter wiring.

- **PR #492 — PR2: Deterministic Approval Cost Cuts** (`app.py`, `core/action_gateway.py`, `core/action_contract_repository.py`, new `core/approval_turn_metrics.py`, `feature_flags.py`, `.env.example`, new `test_pr2_deterministic_approval_cost_cuts.py`; merge `db51afc`) — new `_resolve_pr2_deterministic_approval()` resolves callback-free approve/reject/pending-query/`יצרת?` before Lead Capture, Session, Router, Business Memory, or the Agent, reusing one canonical `ActionContracts` snapshot already established at `run_agent`'s start (the old duplicate later fetch was removed). New anchored grammar (`_PR2_PENDING_EXISTENCE_RE`/`_PR2_CREATED_QUERY_RE`, `fullmatch`/lookahead only, never substring) so ordinary business text is never misclassified as lifecycle intent. New `FEATURE_DETERMINISTIC_APPROVAL_COST_CUTS` flag, default off, composed with `FEATURE_SINGLE_SPEAKER_APPROVAL_UX` in `is_enabled()` so PR2 can never independently change reply ownership. `route_confirmation_word()`/`route_cancellation_word()` gained keyword-only, default-preserving parameters — all pre-existing legacy call sites pass neither, so legacy behavior (including multi-contract mutate-all on reject) is structurally unchanged when the flag is off. New `find_recent_terminal_by_user(max_age_seconds=...)` bounds terminal-contract replay to 24h (`_LIVE_CONTRACT_STALE_SECONDS`), deliberately not reusing the unbounded `find_most_recent_by_user()`. New request-local (contextvars-scoped) `core/approval_turn_metrics.py` counters, content-free logging, observability only. Built from a Context-Librarian-produced reading pack (`docs/architecture/f52-unified-approval-runtime/audits/PR2_READING_PACK.md`) and a preflight audit (PR #491, `12e2a45`); Cross-Layer Impact Matrix recorded in `PR2_IMPLEMENTATION_CROSS_LAYER_IMPACT_MATRIX.md`. A first independent review round found and fixed 3 deviations from the reading pack's binding constraints before commit; CodeRabbit's review of the resulting PR then found and fixed 7 further real defects, including one that silently defeated the already-fixed 24h replay bound (`recent_terminal or find_most_recent_by_user()` fell back to unbounded history whenever `recent_terminal` was explicitly `None`) — fixed with an explicit sentinel. All 10 findings independently re-verified (not just re-reported) before merge. Out of scope, per the reading pack: Phase 3 CI wiring for `verify-consumption`, RP5, TurnCoordinator implementation, `MessageContract` runtime.

- **PR #490 — Context Librarian Consumption Enforcement, Phase 1** (`tools/context_librarian/librarian.py`, `tools/context_librarian/__main__.py`, `test_context_librarian.py`, `docs/context_librarian/task_profiles/profiles.json`; merge `7ee5c5b`) — new `consumption_checklist()` and a `## Consumption Checklist` bundle section (added to every existing `build` call, not opt-in) enumerating the mandatory-tier sources (primary/required-dependency code+tests, mandatory canonical documents/decisions, `required_for_conclusion`-marked expansions, production evidence only with `--production-claim`). New `verify_consumption()` and a `verify-consumption` CLI subcommand: fail-closed ledger verification computing `unreviewed_sources` itself, validating top-level and per-item identity (including `production_claim`), rejecting self-approved waivers, forged computed fields, unknown/duplicate `item_id`s, and a `path` that doesn't match its own `item_id`. New optional `required_for_conclusion` field on `bounded_local_expansions` (default `true`, backward compatible). The new checklist section pushed 5 profiles over their existing token budgets — bumped only the numeric budgets (documented in `ROADMAP.md`'s N17 section), no schema change. 35 new regression tests (112/112 total). Implements exactly the Phase 1 scope of the owner-approved `docs/context_librarian/CONSUMPTION_ENFORCEMENT_PLAN.md` (PR #488, `abf2804`); an independent review before merge found and fixed 4 real gaps in the original `verify_consumption()` (path/item_id binding, `production_claim` bypass, unvalidated `required_sources`, unknown/duplicate item acceptance). Phase 3 (CI blocking gate, `AGENT_CONSUMPTION_CONTRACT.md` hard-must) intentionally not implemented — deferred until Phase 1 sees real-task use, per the plan's own rollout order.

- **PR #480 — F52 Message Contract Envelope: D-012 owner decisions, `MessageContract` frozen for planning** (`docs/architecture/f52-unified-approval-runtime/decisions/DECISION_LOG.md`, new `spec/MESSAGE_CONTRACT_ENVELOPE_CONTRACT_V1.md`, new `rollout/MESSAGE_CONTRACT_ENVELOPE_MIGRATION_PLAN.md`; merge `11e58df`) — documentation only, no runtime change. Resolves D-011 (PR #471 introduced `ApprovalLifecycleResult` as a parallel formatter alongside `GatewayReply`/`compose_status_reply()`, drifting from D-010's single-API requirement) by reconciliation, not deletion: `MessageContract` is approved as the sole canonical input to the final UX formatter; `ApprovalLifecycleResult`/`GatewayReply`/`ActionFact` remain valid internal fact/result contracts that must reach the formatter only through adapters. `turn_id` is nullable (no `TurnCoordinator` dependency), never synthesized from `chat_id`/session ID/`contract_id`/channel message ID. Adapter rollout is 3 separate PRs (envelope/registry only; `ApprovalLifecycleResult` adapter; `ActionFact`/`GatewayReply` adapter), never combined. **Implementation not authorized by this PR.**

- **PR #479 — approval-UX business-facing wording, Airtable table-name redaction** (`app.py`, `core/action_gateway.py`, `core/agent_message_formatter.py`, several `test_*.py`; merge `e663818`) — `_describe_contract_for_reconfirmation()` no longer includes the Airtable table name in the generic `airtable_add`/`airtable_update` fallback wording (only a true non-Airtable-tool fallback still names the tool, since there is no table to redact there). Closes a real, live-confirmed leak: `docs/architecture/f52-unified-approval-runtime/rollout/SINGLE_SPEAKER_APPROVAL_UX_PRODUCTION_VERIFICATION_PLAN.md`'s Claim 4 found this exact path exposing raw tool names and Airtable record IDs to real staging users (test date 2026-07-27); this PR (merged 2026-07-28) closes it — independently confirmed by reading current `core/action_gateway.py` directly, not assumed from the PR title.

- **PR #471 — Single-Speaker Approval UX Base** (`5e2c244`, CI follow-up `dadf851`, merge `c64da20`) — introduced `ApprovalLifecycleResult` as the canonical approval-facing lifecycle result and assigned Gateway ownership to approval lifecycle replies under `FEATURE_SINGLE_SPEAKER_APPROVAL_UX`. Agent processing stops after Gateway ownership is selected; Telegram and WhatsApp share the same lifecycle-to-message semantics; repeated completion/rejection, approve/reject callbacks, textual approval/rejection, pending/no-pending, stale callbacks and cross-chat delivery now produce one terminal user-facing surface. Callback reject now resolves the canonical `ActionContract` (BUG-144), and same-chat callbacks edit the existing message rather than emitting a second final message (BUG-145). User-facing approval text unconditionally removes raw tool names, contract UUIDs, ActionContract record IDs and Airtable business record IDs, including while the rollout flag is off (BUG-118). Callback correlation remains inline and fail-closed: maximum generated Telegram payload is 53 UTF-8 bytes against the 64-byte limit; IDs are never truncated and no correlation store was added. `ActionContracts` remains the only lifecycle source of truth. Safe rollout posture: code default and `.env.example` are both `false`; no staging/production flag activation is part of this merge. The post-review CI fix preserved owner-only approval denial instead of mapping it to a generic failure and restored legacy rejection rendering when the rollout flag is off. Final PR checks: `backend-ci` and `frontend-ci` passed. **Merged and CI-verified; not yet staging/production-verified.** Deterministic cost cuts, counters, queue redesign, Sessions/Business Memory, RP5, CETERRA, durable memory and formatter migration remain deferred.

- **PR #449 — staging findings (23/07/2026): warm-cache TTL consistency, sibling-rejection disclosure, pending-approval query routing** (`core/action_gateway.py`, `app.py`) — a manual staging test (`my-bot-approval-staging`, real Telegram session) surfaced 7 findings, later cross-referenced against a real owner-supplied `ActionContracts` export + Render logs. **Fixed:** `ExecutionLedger.find_live_by_user()` only applied `CONTRACT_PENDING_TTL_SECONDS`/`_is_expired()` on the cold-cache/repository-recovery path, not once the RAM cache warmed up — now consistent on both paths (named narrowly as a warm-cache consistency fix, **not** a TTL-window policy decision — the existing 24h value, set for TMA's async approvals, was not revisited, and the report's specific 12h-old incident would still not have been caught by this fix alone: proven with real data — 3 of 6 disambiguated siblings were 27-38h old and would now be hidden, but the one that actually executed was 14.65h old, inside the 24h window). Added a per-item age warning (`⚠️ ממתין מ-N שעות`) on every multi-contract listing as the practical mitigation for that gap. `route_disambiguation()`/`route_combined_word()` now disclose to the user when picking one item auto-rejected others (count is grounded in `reject()`'s own confirmed-transition return, verified not to overclaim on a partial failure). New `ActionGateway.describe_pending_queue()` answers "מה ממתין לאישור"-style questions deterministically from `ActionContracts` instead of the general agent guessing an Airtable table — reply text explicitly scopes itself to `ActionContracts` only (this codebase also has `app.py`'s `_pending_approvals` dict and `event_bus.py`'s legacy `PendingActionsStore` as separate pending-action stores, not covered by this fix). Fixed a PII leak in that same log line (previously wrote raw user text/reply content — now only `pending_count`/`scope=action_contracts`/`result_code`). **Explicitly deferred, documented but not implemented** (all require either an owner decision or the frozen TurnCoordinator contract's own approval, to avoid a parallel/competing mechanism): the `route_disambiguation()`/`route_combined_word()` sibling-reject-all semantics itself (§21) for genuinely-unrelated pending items; a real delete-intent flow for contacts/leads (`DESTRUCTIVE_ENTITY_CLARIFICATION`, TurnCoordinator contract §3.2); and a newly-discovered gap where "add a contact" always creates a Lead (with the role-noun embedded in the name field and lead-funnel metadata attached) because `core/lead_candidate_handler.py` never consults the Router's own correct `Intent.CREATE_CONTACT` classification — confirmed to be TurnCoordinator contract scenario 7, verbatim. Manual action item: Airtable record `recK8RdYkdDmTGdob` (a legitimate-looking contact-add request that executed 14.65h after being requested, not garbage data) needs owner confirmation, not automatic deletion. `claude/rp5-staging-fault-injection-v4akit` (staging deploy branch, intentionally never merged to `main`) was rebased onto `main` past this PR and force-pushed so staging actually runs this fix. 33 new tests (`test_staging_23jul_findings.py`), full existing regression suite clean. See `docs/architecture/action-gateway/STAGING_23JUL_TTL_DISAMBIGUATION_AUDIT.md` for the full Cross-Layer Impact Matrix and root-cause writeup.

- **BUG-129, BUG-135 — `core/ingress_classifier.py` extracted fake lead names from self-quoted bot text and delete commands instead of the real name (or no name at all)** — blocked the owner's RP5 testing. **BUG-129** (previously registered, root-cause-only): pasting the bot's own `"📋 זיהיתי ליד: *X* (phone)"` confirmation text back extracted `"זיהיתי"` instead of the real name, because `_extract_name_from_window()` returns on the first regex match that clears validation and never reaches the second, correct match. **BUG-135** (new): `"תמחק איש קשר <phone>"` (delete command, no real name in the text) extracted `"תמחק איש קשר"` as a fake candidate name — this module has no delete-intent handling of its own (the only delete intent in the router, `Intent.DELETE_TASK`, covers tasks, not contacts/leads), so delete text falls through to the same generic name+phone extraction as create/update. Fixed by adding `"זיהיתי"` (BUG-129) and the delete-verb set (`"תמחק"`/`"מחק"`/`"הסר"`, mirroring the router's own `DELETE_TASK` verb vocabulary, BUG-135) to `_NAME_STOP`, plus a narrow exact-phrase reject (`_GENERIC_NAME_PHRASES = {"איש קשר"}`) for the bare role-noun remainder — confirmed with the owner that the existing, working `"תוסיף איש קשר בדיקה טלפון X"` → `"איש קשר בדיקה"` extraction must NOT change, so `"איש"`/`"קשר"` were deliberately not made blanket stop-words. New `test_bug135_command_verb_name_stop.py` (10 assertions). Full `test_*.py` sweep (166 files) + `smoke_tests.py` + `test_integration.py` + `compileall` clean — the only 2 failing files in the sweep were confirmed pre-existing on `main` (reproduced via `git stash`), not a regression from this change. **Out of scope:** this patch changes name extraction only — it does not change create-vs-update routing or existing-record resolution; **BUG-130** (explicit update intent routed into new-lead preview) remains open and unfixed. Branch `claude/zihuiti-name-extraction-22qitu`. **Correction (23/07/2026):** this line previously said "not yet merged" — confirmed via `git merge-base --is-ancestor` that commit `9285106` is an ancestor of `main` (merged as PR #444, `3f69b1d`). Stale-status correction only, no code change. See `CHANGE_CONTROL_LOG.md` C168, `BUG_AUDIT_LOG.md` BUG-129/BUG-135.

- **BUG-131 — a test file was silently writing real records into the production Interaction Log table** (`test_bug104_tma_lead_event_bridge.py`) — the owner noticed an abnormal volume in the live Airtable "Interaction Log" table (705 records, 43% under one fake lead ID) and asked how it got there. Traced directly against live data: 310 records, split exactly 155/155 between `lead_outcome`/`lead_patch`, all referencing `"recLEAD001"` — not even a valid Airtable record ID format, the test's own fixture ID. Root cause: `tma_api.py` binds `_gw_create` to the real `airtable_gateway.airtable_create` at import time; the test's mock (`airtable_gateway.airtable_create = _counting_create`) only overwrites the attribute on the `airtable_gateway` module, never touching `tma_api`'s already-bound name — so `_audit()`'s Interaction Log write escaped the mock and hit production on every "success" test case. Same bug family as BUG-128, different mechanism (wrong mock target vs. an escaped `with patch()` scope). Fixed by mocking `tma_api._gw_create` directly (same pattern already used for `_at_patch`), plus closing a smaller, read-only sibling leak in the same block (`tma_api._at_list`, the GET-lead Interaction Log timeline lookup, observed making a real but harmless `403`/no-data-written network call). New regression assertions prove `_audit()`'s write is actually captured by the mock. 50/50 tests pass (was 46/46), full `test_*.py` sweep + `smoke_tests.py` + `compileall` clean. The 310 existing polluted records were deleted directly from Airtable with the owner's explicit approval — a one-time data cleanup, separate from the code fix. PR #442. **Correction (23/07/2026):** this line previously said "not yet merged" — confirmed via `git merge-base --is-ancestor` that `d565cae` (the PR #442 merge commit) is an ancestor of `main`. Stale-status correction only, no code change. See `CHANGE_CONTROL_LOG.md` C163.

- **BUG-127A — Ingress Context Gate's primary and fallback safety mechanisms failed together on a stale in-RAM ActionContract lifecycle version** (`core/action_gateway.py`) — a live staging transcript showed `[CRITICAL]` dual-failure log lines: both `mark_context_interrupted()` and its own fallback `mark_context_integrity_unknown()` failed on the same contract in the same turn. Root cause: both call into `ActionContractRepository.transition()` with an `expected_version` that can go stale if the in-memory cache drifts from the durable store (e.g. a concurrent write elsewhere) — a pure version mismatch (not a genuine status divergence) was treated identically to a real conflict, failing both layers closed when a simple re-fetch would have healed it safely. Fixed with a new `_refresh_stale_contract_cache()`: on `ActionContractTransitionConflictError` specifically, re-fetches the canonical record from the repository and retries the transition once with the refreshed version (still checking `expected_status` unchanged) — falls through to the original fail-closed behavior if the refresh or the retry also fails, so a genuine status conflict still fails safely, only stale-version noise self-heals. 10 new tests (`test_bug127a_stale_lifecycle_version_retry.py`), no regression across 90 existing. Merged via PR #429. **Not yet verified in production/staging.** See `CHANGE_CONTROL_LOG.md` C158.

- **BUG-125 — a bare ✅ was classified as a success claim in RP5's shadow evidence finalizer, even for plain factual/math answers** (`core/turn_evidence.py`) — RP5 fault-injection staging tests showed `"25 ✅"`/`"8 ✅"` (answers to arithmetic questions with no tool call at all) classified as `response_claim=success`, `mismatch=True` — a false positive with no real evidence-vs-claim conflict. Root cause: `_MUTATION_SUCCESS` treated a bare checkmark as sufficient evidence of a completion claim on its own. Fixed: bare ✅ is no longer a standalone trigger — success is only classified when an actual completion verb/phrase co-occurs, including Hebrew feminine/plural conjugations that don't substring-match their base form (e.g. `"הושלמה"` is not a substring match of `"הושלם"` — the same class of gap BUG-119 hit for a different verb pair), plus a new deletion-verb family. 13 new regression tests, plus an existing precedent test (`test_a32_approval_prose_suppression.py`) updated openly to match the corrected classification. Shadow-only — `FEATURE_EVIDENCE_FINALIZER=off`, no runtime/production behavior change. Merged via PR #428. See `CHANGE_CONTROL_LOG.md` C157.

- **PATCH 3B completed: Steps 5, 5.1, 6 + a CI-hardening prerequisite merged and production-verified; TMA frontend fix pending merge** (`core/emergency_stop_bootstrap.py`, `app.py`, `gunicorn.conf.py`, `feature_flags.py`, `core/emergency_stop.py`, `adapters/airtable_emergency_stop_store.py`, `cost_monitor.py`, `tma_api.py`, `.github/workflows/ci.yml`, `tma-frontend/src/*`) — the atomic cutover completing the Emergency Stop durable-persistence work after Steps 1–4. **Step 5** (PR #427): `app.py` bootstrap wired through an explicit `run_startup_sequence()` (called from `gunicorn.conf.py`'s `post_worker_init`, not as an `import app` side effect — the owner caught a false "no import-time I/O" claim in the first PR version and required a fix), bootstrap-before-scheduler ordering made structural, and exception handling narrowed to let only two documented outcomes (`unavailable`/`invalid`) pass silently — anything else propagates and blocks the scheduler. **Step 5.1**: `gunicorn.conf.py` pinned to `workers=1` (empirically verified — a real `workers=3` run booted 3 workers, then reverted) since the in-process scheduler has no cross-worker dedup; an unexpected `store_status` now raises instead of logging and continuing. **Prerequisite** (PR #432): while verifying Step 5, found unit CI silently reading the **live production** Airtable Emergency Stop Flags table via a test-isolation bug (assertions running outside their mock context) — fixed the bug, then removed real Airtable credentials from CI entirely (fake placeholders + `/etc/hosts` network block) as defense-in-depth. **Step 6** (PR #433): `is_enabled()`/`set_flag()` now intercept the 5 canonical `EMERGENCY_STOP_*` names — reads delegate to the durable manager (fail-closed to blocked if unconfigured), writes to `set_flag()` raise `EmergencyStopLegacyWriteBlocked`; the old `/tmp/emergency_flags.json` mechanism is gone entirely. New `_env_force_stop_provider()` (only the literal `"true"` forces a stop, reads `os.environ` directly). `cost_monitor.py` and `tma_api.py`'s stop endpoint migrated to `set_emergency_stop()`; a new `POST /api/health/emergency/clear` endpoint (optimistic concurrency via `operation_id`, 409 on a stale value, success gated on `ok+verified`). **Verified directly in production by the owner** (not just tests): `stop_email` via the TMA survived a real Render restart still active — proving durable persistence actually works — then cleared successfully with the current `operation_id`. **TMA frontend fix** (PR #436, pending merge): added a Stop AI button and per-flag Clear buttons wired to the new endpoint, removed the now-provably-false "restart to clear" text, verified in a real browser (Playwright/Chromium against a throwaway local backend, no production access). See `CHANGE_CONTROL_LOG.md` C159–C162.

- **PATCH 3B Steps 2–4 merged (PR #425) + real production verification, not just tests** (`adapters/airtable_emergency_stop_store.py`, `core/emergency_stop.py`, `core/emergency_stop_preflight.py`, `core/predeploy.py`, `feature_flags.py`, `tools/airtable_gateway.py`) — continues the Emergency Stop durable-persistence work after Step 1 (PR #421). The owner created the `Emergency Stop Flags` table in the live Airtable base by hand with the 7 required fields, seeded all 5 flag records (`EMERGENCY_STOP_ALL`/`WHATSAPP`/`EMAIL`/`AUTOMATION`/`AI`, all `Enabled=false` — matching observed production state, since no `EMERGENCY_STOP_*` env var is currently set in Render), then ran both `python -m core.emergency_stop_preflight` and `python -m core.predeploy` directly on the live Render service (not locally, not mocked) — both exited 0, including two real Airtable HTTP calls (200 OK) and a successful run of the existing `core.database_migrations` migration, confirming `predeploy` didn't change that command's standalone semantics. This satisfies all three preconditions the owner set for switching Render's Pre-Deploy Command (table created, records seeded, successful manual run) — the actual dashboard switch itself is a separate, still-pending owner action outside this repo. Everything remains inert: `configure_emergency_stop_manager()` still has zero production callers, `app.py`/`scheduler.py`/`tma_api.py`/`cost_monitor.py` untouched (Step 5 — not yet started). See `CHANGE_CONTROL_LOG.md` C156 for the full command output.

- **N16 — Git Audit descoped from the business bot; N12 reverted** (`scheduler.py`, `daily_git_audit.py`, `feature_flags.py`, `.env.example`, `test_c86_scheduler_emergency_matrix.py`) — the owner received a "🚫 AUDIT ABORTED (GOV-02 STOP)" Telegram message from the business bot and asked why a repo-auditing tool was sending through it. Investigation found N12 (PR #108) had wired `daily_git_audit.py` (the GOV-02 tool) into the bot's own `scheduler.py` (`_job_daily_git_audit`, daily at 06:45, gated by `GIT_AUDIT_SCHEDULER`) and given it a direct send path via the bot's own `TELEGRAM_TOKEN`/`ELIYAHU_CHAT_ID` — meaning the bot's production Python process on Render was reading git state and pushing the result to Telegram, duplicating a Claude Code Routine that already does the identical job (read the repo, run GOV-02, report via push/email) through completely separate infrastructure. Not a wording collision — a real architectural duplication, and live evidence the flag was in fact on (or the script was run manually with the bot's Telegram credentials present) since the message arrived at all. Owner decision: git auditing is the Routine's job exclusively; the business bot should have no repo/Git access at all and sends only the digest and business alerts. Fixed: `_job_daily_git_audit`/`git_audit_time`/its schedule registration removed entirely from `scheduler.py` (not left flag-gated — flag-off alone had already failed to prevent the message that triggered this). `daily_git_audit.py`'s `_send_telegram()` removed completely (both call sites — the GOV-02-abort path and the final report — now just `print()`); the script has zero dependency on `TELEGRAM_TOKEN`/`telebot` now. The now-orphaned `GIT_AUDIT_SCHEDULER` flag removed from `feature_flags.py`'s registry and `GIT_AUDIT_TIME` from `.env.example`. `audit_truth_gate.py` (GOV-02) itself is untouched — stays a read-only repo tool for the Routine to invoke directly. Full `test_*.py` sweep + `smoke_tests.py` + `compileall` clean. See `CHANGE_CONTROL_LOG.md` C155, `ROADMAP.md` N16 (and N12's amended entry). Merged to `main` via PR #424 (commit `99981fb`).

- **BUG-124 — context-pronoun substitution defeating the Tier-4 table/export gate, plus a same-day follow-up closing a second trigger class** (`app.py`) — a live staging incident showed an ordinary message containing "זה" ("כמה זה 5 כפול 7") silently blocked as `tier=4/table_separator`. Root cause: `resolve_context_pronouns()` (C60) splices a quoted tool-result summary into free text via raw substring replace, and real tool summaries commonly contain `" | "` — injecting 2+ pipes into an otherwise ordinary sentence, which `core.ingress_classifier._TABLE_RE` reads as a pasted table. Fixed with a `_sanitize_for_free_text()` helper applied to both substitution points, covering all 7 `CONTEXT_PRONOUNS` entries. **Same-day follow-up** (owner asked how far the bug reached): `_is_tier4()` has 7 independent trigger classes — the pipe-only sanitizer left the Airtable-record-id class (`_AIRTABLE_ID_RE`) open, since `airtable_add`/`airtable_update`/`tma_write` (3 of the 4 tools `_MEMORABLE_TOOLS` persists) all embed a raw `recXXXXXXX` id in their success message. A follow-up like "תעדכן גם את זה" right after any of those 3 tools was still silently blocked. Fixed properly with `_safe_context_quote()`: checks the quoted snippet against the real `_is_tier4()` before splicing it in, falling back to a plain reference ("הפעולה האחרונה שביצעת") when quoting would trip Tier-4 — self-healing against any future trigger the classifier gains, not another hand-picked character. 28/28 tests (`test_bug124_context_pronoun_table_false_positive.py`). Full `test_*.py` sweep + `smoke_tests.py` + `compileall` clean. Merged to `main` via PR #422. **Not yet verified in production/staging** — needs a live follow-up-command test after deploy. See `CHANGE_CONTROL_LOG.md` C154.

- **BUG-121/122/123 — three separate staging-observed bugs found while verifying `/status` and the pending-approval flow** (`app.py`, `event_bus.py`) — **BUG-121**: `/status` crashed with `ApiTelegramException: can't parse entities` (env var names containing `_` defeat Telegram's legacy Markdown parser), masked by the unrelated BUG-120 exception-handler bug so it looked like the same failure kept recurring; fixed by falling back to a plain-text resend on parse failure. **BUG-122**: a confident, unambiguous new command (e.g. `create_task`, `confidence=0.95`) got swallowed by a generic "לא הצלחתי לבצע את הפעולה" fallback whenever the user had pending `ActionContracts` — root cause was `sanitize_agent_response()`'s Single-Speaker gate blanket-replacing any action/pending-shaped free text, even when nothing had actually been attempted; fixed to detect this specific case and reply with an explicit queue-resolution message instead. **BUG-123**: an approval-request message rendered a broken placeholder (`"הוסף ל-?:"`) and leaked raw contract/record IDs to the user; `_describe_tool_call()` rewritten to fail closed with a readable fallback instead of a raw `"?"`, and `record_id`/`draft_id`/`action_id` removed from all user-facing approval text (still available via `callback_data`/logs). 8/8 + 23/23 + 20/20 new/updated tests. Full `test_*.py` sweep + `compileall` clean. Merged to `main` via PR #420. **Not yet verified in production** — needs the owner's own live Telegram/WhatsApp test after deploy. See `CHANGE_CONTROL_LOG.md` C152/C153.

- **Truth Reset (docs only)** — before starting the Post-N15 work plan, verified every planned item directly against `origin/main` (commit ancestry, grep, and running the relevant tests) instead of trusting `BUG_AUDIT_LOG.md`/`ROADMAP.md`'s own claims. Found 6 items marked "not merged"/"pending" that were actually already merged and live: BUG-058 (Tier-2 batch resolver — not gated by any flag, previously production-verified via real log evidence, but its own entry said "pending push/PR"), BUG-071 (WhatsApp media adapters, moved from `providers/` to root in a follow-up commit the log never reflected), BUG-BATCH-DISCARD (`BatchQueueStore`, merged 17/07 via `ba579f2`), BUG-007 (CORS preflight, confirmed with a real Flask test client — 204 on all 4 routes), and BUG-049 (CI silent-pass fix, confirmed with a real run — "6 passed, 0 skipped"). Also found and fixed a second, separate stale `/status`-decorator claim in `ROADMAP.md`'s other known-gaps table, and a stale "blocked on C81-FU–C83" label on C87 (all three are already closed). Clarified BUG-118's actual remaining scope: most of the "preview leaks raw tool_name/record_id" concern was already fixed elsewhere (disambiguation labels, the executed-status branch) — only `route_confirmation_word()`'s legacy success reply still leaks. No production code changed. See `CHANGE_CONTROL_LOG.md` C151.

- **Post-N15 Work Survey (docs only)** — a full, unabridged survey of open work outside RP5/F52/`FEATURE_ACTION_GATEWAY`/anything else currently blocked, produced by 4 parallel research agents covering `ROADMAP.md`, `BUG_AUDIT_LOG.md`, `feature_flags.py`, and general tech debt/governance drift. Saved to `docs/audit/POST_N15_WORK_SURVEY_20260720.md`. While cross-referencing against this week's verified work, found and fixed 3 stale claims in `ROADMAP.md` itself: C84 and C86 both marked "not yet done" when they were actually already merged/already covered by an existing passing test, and a "Known Issues" table row claiming `/status`'s command decorator was removed (it wasn't — confirmed directly while fixing BUG-120/BUG-121 this week). No production code changed. See `CHANGE_CONTROL_LOG.md` C150.

- **Day-3 flag pre-activation prep — `FEATURE_WEEKLY_SUMMARY` grouping bug fix + `FEATURE_LAST_TOOL_RESULT_SHADOW` test coverage** (`weekly_summary.py`, `core/output_gateway.py`) — `_group_by_domain()` grouped Business Memory entries by `Tags[0]`, but `cmd_update.py` never writes domain into `Tags` (domain goes only to the dedicated `Domain` field, e.g. `"Real Estate"`) — every entry was silently misgrouped. Fixed to read `BusinessMemoryFields.DOMAIN` and normalize it to match `_DOMAIN_LABELS`' internal keys; new `test_weekly_summary_domain_grouping.py` (11 checks). Separately, `FEATURE_LAST_TOOL_RESULT_SHADOW` had no dedicated test file — added `test_last_tool_result_shadow.py` (23 checks) covering the shadow store itself plus all three real call sites (`dispatch_tool()`'s finally-block, `tma_api.py::_shadow_record_tma()`, `core/output_gateway.py::_shadow_record_send()`); caught a real minor bug while writing it — `_shadow_record_send()` interpolated the `OutputChannel` enum member directly into its diagnostic string instead of `.value`, producing `"OutputChannel.TWILIO_WHATSAPP"` instead of `"twilio_whatsapp"` (diagnostic-only, no control-flow impact) — fixed. Full `test_*.py` sweep + `smoke_tests.py` + `test_integration.py` + `core/router/test_router.py` + `compileall` all clean. **Merged to `main`** via PR #421 (branch `claude/n15-owner-decision-p73c3k`).

- **BUG-110 residual — fix stale `status=="converted"` read-side checks** (`ad_attribution.py`, `audience_intelligence.py`) — BUG-110 (17/07/2026) fixed the two write sites to write `status=done`+`Business Outcome=converted`, but explicitly left two read consumers on the old literal `status=="converted"` as deferred tech debt: `ad_attribution.py::build_attribution_report()` and `audience_intelligence.py::_parse_records()`. Leads converted *after* the BUG-110 fix were silently dropped from attribution reports and "champion"/converted segmentation — no crash, a silent wrong answer. Fixed by also checking the canonical `Business Outcome` field against the `LeadOutcome.CONVERTED` constant (not a hardcoded literal, given the field's baked-in trailing space), OR'd with the legacy check since there's no backfill for old data. `ad_attribution.py::mark_converted()`'s deferred gateway migration (the other half of BUG-110's tech debt) remains deliberately untouched — it would break `mark_converted()`'s `bool` return contract against `test_response_contract_fixes.py`, as already evaluated in BUG-110 itself. `test_bug105_non_canonical_converted_status.py` (10/10) and `test_response_contract_fixes.py` (19/19) re-run clean; full `test_*.py` sweep + `smoke_tests.py` + `test_integration.py` + `core/router/test_router.py` + `compileall` all clean. **Merged to `main`** via PR #421 (branch `claude/n15-owner-decision-p73c3k`).

- **C81-FU / C82-FU — ROADMAP docs cleanup (both were already resolved) + a real CI-silent-pass fix found while verifying** — both items had sat marked 🔴 urgent in `ROADMAP.md` despite being fixed in code some time ago; re-verified directly against `main` rather than trusting the stale docs. C81-FU (recovery completion honesty): `tools/approval_actions.py::send_recovery()` already requires verified `owner_delivery.delivery_success` before returning `ok=True`, and never writes `recovery_count` at all (confirmed via repo-wide grep) — the original bug ("recovery_count grows even when the customer never received the message") no longer exists. C82-FU (central emergency-stop gate for scheduler jobs): `scheduler.py::_automation_guard()` now wraps every single registered job (confirmed via `grep '\.do(' scheduler.py | grep -v _automation_guard` — zero unwrapped registrations across 23 jobs), not just followup/payment-reminders as the original problem described. **Real finding surfaced during verification, unrelated to either fix's correctness:** `test_c81_recovery_truth.py` was missing its `if __name__ == "__main__":` runner block, so `python3 test_c81_recovery_truth.py` — this repo's own convention for running any `test_*.py` file, and what `ci.yml`'s "Run test_*.py scripts" step actually executes — silently ran zero assertions (import-only, exit 0). Same bug class as the already-fixed BUG-049/CI-SILENT-PASS-DOCUMENT-CONVERTER. Fixed with the identical pattern; `python3 -m pytest test_c81_recovery_truth.py` already passed 4/4 even before the fix, confirming the tests themselves were always correct — only the direct/CI execution path was silently broken. No production code changed.

- **C84 — TMA Approvals TTL/freshness check** (`tma_api.py`) — a TMA Approvals row could stay actionable forever (no expiry), unlike the Telegram inline-button flow (BUG-112), which independently enforces a 600s TTL right before dispatch. Added a mirror of that same principle to `_claim_and_execute_approval()` — the single choke point both `act_on_approval()` and `bulk_approve()` route an "approve" decision through: once `time.time() - ActionContract.created_at` exceeds a new `_TMA_APPROVAL_TTL_SECONDS` (24h — a TMA dashboard row is reviewed asynchronously, not a push-notification button checked within minutes), the contract is rejected (verified via re-fetch, not assumed — mirrors `_reject_stale_telegram_approval()`'s existing pattern) instead of executed, and the caller gets back `{"ok": False, "status_code": 410, ...}`. **Deliberate deviation from ROADMAP C84's literal spec** (add a new `expires_at` field to the Approvals record): used the existing `ActionContract.created_at` (a required dataclass field on every real contract) instead — `ApprovalsFields`'s own docstring documents Approvals as a "non-authoritative TMA display projection" of ActionContracts, so the canonical contract is the source of truth for "how old is this really," not a new field that would need manual Airtable schema creation + a `schema_cache.json` resync. The Approvals projection (`STATUS`/`PROJECTED_LIFECYCLE_STATUS`) is synced to the rejection too — but never consulted for the freshness decision itself, only `ActionContract.created_at` is — so the TMA UI stops showing a stale "pending" row without becoming a second source of truth. `bulk_approve()` gets this for free (it already calls `_claim_and_execute_approval()` per item) — an expired item lands in the existing "failed" bucket, other items in the same batch are unaffected. A missing/invalid `created_at` fails **closed** (treated as expired, never as fresh) — deliberately the opposite of BUG-112's Telegram check, which treats a malformed timestamp as fresh; acceptable for a single push-notification button, not for an approval that can also be bulk-executed. New `test_c84_tma_approval_ttl.py` (44 checks): fresh-contract baseline, expired-contract refusal (never reaches `approve()`, ends `rejected`/410), an exact-boundary case (age just under TTL still executes — the check is a strict `>`), projection-sync verification, a mixed `bulk_approve()` batch (one expired, one fresh) proving only the expired one is affected, and 6 missing/invalid-`created_at` variants (None/empty string/non-numeric string/negative/zero/bool) all failing closed. Also updated 3 pre-existing test files' `_FakeContract` doubles (`test_approval_concurrency.py`, `test_phase_4b2_wiring.py`, `test_pr0c0_tma_approval_truthfulness.py`) to carry a `created_at` field (defaulting to "now") — none of them had one before, so the new check would have raised `AttributeError` the moment any of their existing tests reached `approve()`; every pre-existing test in those files still passes unchanged. Full `test_*.py` sweep (143 files), `smoke_tests.py`, `compileall` all clean. Does not touch RP5/F52/EvidenceFinalizer/UnifiedStatusFormatter, approval-authority semantics, `ActionGateway.approve()`/`reject()`'s own lifecycle logic, or the separate Telegram-button TTL path (BUG-112).

- **BUG-117 — Tier-2 batch lead-preview hijacked by the same disambiguation BUG-115 fixed for Tier-1** (`app.py`, `core/lead_candidate_handler.py`) — investigated separately from BUG-115 (shared root contributing factor — pending `ActionContracts` never expire, BUG-114 §2 Q6 — but a genuinely different code path: a batch lead preview has no `ActionContract` at all, so BUG-115's per-contract bookmark can't cover it). Production sample: a "📋 זיהיתי 2 לידים אפשריים בקבוצה... ענה כן לשמירת כולם" batch preview, followed by "כן", fell into `ActionGateway`'s generic disambiguation of 9 old, unrelated contracts instead of confirming the batch. Root cause: `app.py`'s `_CONFIRM_WORDS` branch checked Tier-1 (`find_live_contracts()`) unconditionally, before ever considering the Tier-2 batch preview (`resolve_pending_lead_preview()`) further down — the code's own comment documented "Tier 1 always wins when both exist simultaneously" (`core/lead_candidate_handler.py`'s `resolve_pending_lead_preview()` docstring, BUG-058), an assumption BUG-115 already broke for Tier-1-vs-Tier-1 but never fixed for Tier-1-vs-Tier-2. Fixed with a new `core.lead_candidate_handler.should_prefer_batch_preview(canonical_user_id, chat_id)`, comparing recency between the Tier-2 preview's existing `set_at` timestamp (1800s TTL) and BUG-115's Tier-1 bookmark's `set_at` (600s TTL) — whichever prompt is genuinely more recent wins; called in `app.py` *before* the unconditional Tier-1 gate, falling through unchanged when it returns false. Neither existing TTL mechanism was modified. Required widening the character-window in `test_c89_preview_confirmation.py`'s existing structural invariant test (`test_app_py_confirm_word_checks_gateway_before_flag_branch`, 3000→5000/5000→6500) since the new check pushed later content further from its marker — the invariant itself is unchanged, mirroring an identical widening BUG-058 already made once for the same reason. New `test_bug117_batch_preview_precedence.py` (11 checks); `test_c89_preview_confirmation.py` (9) and `test_bug115_confirmation_routing_bookmark.py` (22) re-run clean as explicit non-regression proof. Full `test_*.py` sweep (140 files), `smoke_tests.py`, `compileall`, `git diff --check` all clean. Explicitly out of scope: BUG-114's fix, BUG-115's bookmark mechanism itself, `route_disambiguation()`, and the `_CANCEL_WORDS`/`route_cancellation_word()` branch (a separate, more consequential existing behavior — cancels *all* live Tier-1 contracts when any exist — not touched here). Merged via PR #405 (`main` `4546880`). **✅ Production-verified (19/07/2026)**: a real 2-lead batch dictation entered with `live_contracts=9` (9 old contracts pending), the batch preview showed correctly, and "כן" confirmed both leads with real record ids (`recoLSXsLQNKQG6Gy`, `recgwDYidGrTc9KEU`) — no disambiguation hijack. See `docs/architecture/action-gateway/BUG-117_BATCH_PREVIEW_PRECEDENCE_HIJACK.md`.

- **BUG-116 — Tier-4 `_AIRTABLE_ID_RE` false-positives on plain English words** (`core/ingress_classifier.py`) — investigated and fixed separately from BUG-114/BUG-115 (a Tier-4 ingress-classification bug, unrelated to `ActionGateway`/`ActionContract` routing). Production sample: `"צור ליד חדש לדומיין recruitment \nיהודה גרוס  0533968395"` — a plain lead sentence with a real name and phone — classified as `tier=4 reason=airtable_id` and was blocked with "📄 זה נראה כמו טבלה/ייצוא/פלט מודבק" before lead extraction ever ran, on three separate attempts. Root cause, confirmed by direct execution: `_AIRTABLE_ID_RE = re.compile(r"\b(?:fld|rec)[A-Za-z0-9]{8,}\b")` has no upper bound or shape constraint, so any English word starting with `rec`/`fld` followed by 8+ letters matches — `recruitment` = `rec` + `ruitment` (8 letters). A first attempt at an exact-length fix (matching this codebase's other Airtable-ID regexes, `rec[A-Za-z0-9]{14}`) was rejected: it would have broken `test_c89_tier4_precedence.py`'s existing "Airtable rec ID" fixture (`recABC1234567890`, a 13-char tail, not 14). Fixed instead by requiring at least one digit in the matched run via a lookahead — `re.compile(r"\b(?:fld|rec)(?=[A-Za-z0-9]*\d)[A-Za-z0-9]{8,}\b")` — verified programmatically against every genuine-ID fixture already relied on across the test suite (`recABC1234567890`, `recRvK6hFTNgyj8ag`, `rec3YS5Zcr2FenX7z`, `rec62b86WqBpaWPaG`, etc. — all mix letters and digits; a real English word never does). Explicitly out of scope: `core/agent_message_formatter.py`'s separate, similarly-shaped regex used for redacting record IDs from *agent-facing output* (different risk profile, not touched). New `test_bug116_airtable_id_word_false_positive.py` (15 checks); `test_c89_tier4_precedence.py` (13 checks, unmodified) re-run clean. Full `test_*.py` sweep (138 files), `smoke_tests.py`, `compileall`, `git diff --check` all clean. Merged via PR #404 (`main` `0ef018f`). **✅ Production-verified (19/07/2026)**: a differently-worded real sample (`"צור ליד חדש domain recruitment \n\nיונתן כהן - 0534820022"`) classified correctly as tier=1, produced the lead preview, and completed to `"✅ בוצע: יצירת ליד: יונתן כהן, 0534820022, general | מזהה: recNhWVHDd9Noeql1"` — no more Tier-4 block. See `docs/architecture/ingress-classifier/BUG-116_AIRTABLE_ID_REGEX_WORD_FALSE_POSITIVE.md`.

- **BUG-115 fix — "כן"/"מאשר" confirmation hijacked by unrelated stale ActionContracts** (`session_store.py`, `core/lead_candidate_handler.py`, `app.py`, `core/action_gateway.py`) — implements the two-part narrow fix from the same-day audit. (1) A short-lived (600s, matching `_PENDING_APPROVAL_TTL`'s own advertised window) "last prompted contract" bookmark, reusing `session_store.py`'s existing `pending_lead_preview` pattern (new `set_last_prompted_contract`/`get_last_prompted_contract`/`clear_last_prompted_contract`), set whenever a real approval prompt is actually shown (the lead-preview path in `core/lead_candidate_handler.py`, and `app.py`'s `_queue_approval_detailed_impl()` general approval flow — only after the prompt is *proven* sent, not merely attempted). `route_confirmation_word()` checks this bookmark before the live-contract count; if it names a still-pending contract for the same user, that contract resolves directly, regardless of how many other unrelated contracts are also live. A new `_resolve_single_contract()` helper factors out the existing reconfirmation/context-poisoning safety check (BUG-PENDING-APPROVAL-B) so a bookmark can never bypass it; the bookmark is deliberately **kept** (not cleared) when the outcome is "please reconfirm" rather than an actual approve, so the next confirm word still resolves the same contract. (2) The disambiguation list no longer shows raw `tool_name`/contract ids. First attempt generalized the existing `_describe_contract_for_reconfirmation()` helper directly and broke an unrelated, already-passing test (`test_stage_b_full_suite.py`'s DoD20, which asserts the *executed*-completion message — a different, pre-existing call site of that same shared helper — does show the tool name) — caught by the full regression sweep, not by design. Corrected: a new, separate `_describe_contract_for_disambiguation()` function, used only by the disambiguation-list loop; the original shared helper is untouched. New `test_bug115_confirmation_routing_bookmark.py` (22 checks). `test_bug114_context_interrupt_amplification.py` (12) and `test_bug_reconfirmation_oneshot_fsm.py` (27) re-run green as explicit non-regression proof. Full `test_*.py` sweep, `smoke_tests.py`, `compileall`, `git diff --check` all clean. Does not touch BUG-114's fix, F52, EvidenceFinalizer, `route_disambiguation()`, `TurnEnvelope`, or `message_kind`. **✅ Production-verified (19/07/2026)**: a sample with `[TurnEnvelope] case_c_signal kind=C1 detail=live_contracts=10` (10 contracts pending simultaneously) showed a "כן" on a freshly-shown lead preview resolve directly to the correct contract (`approved: contract=4c7b539b...` → `Dispatch airtable_add → POST /Leads 200 OK → executed: external_id=rec34IdTmCFVbRABo` → `"✅ בוצע: יצירת ליד: יצחק גלבר, 0527696084, general"`) with no disambiguation list shown — the exact `live_contracts>1` condition needed to distinguish the bookmark actually working from the pre-existing single-live-contract path. Also a clean F52 executed-shadow sample (`outcome=executed mapped_state=success`, no leak flags); not counted as an RP5/EvidenceFinalizer sample since no matching `EvidenceFinalizerShadow` line was present for the same turn.

- **BUG-115 audit (docs-only) — "כן" confirmation hijacked by unrelated stale ActionContracts** — investigated separately from BUG-114 per explicit instruction, despite sharing a root contributing factor (pending `ActionContracts` never expire — BUG-114's own §2 Q6). Production sample: a lead-creation preview ("📋 זיהיתי ליד...לשמור? ענה כן") followed by "כן" fell into `ActionGateway`'s generic multi-contract disambiguation, listing 8 unrelated old pending contracts by raw tool name and internal id instead of confirming the lead just shown. Confirmed the lead preview genuinely creates a real Tier-1 `ActionContract` (`_propose_lead_write()`, deliberate BUG-056 design) — not a Tier-1/Tier-2 mixup. Root cause: `route_confirmation_word()` (`core/action_gateway.py:1010`) assumed exactly one live contract at a time and has no notion of "which contract this specific reply is about" — once old abandoned contracts accumulate, its `len(live)>1` branch always shows a generic list. Also confirmed (but ruled out as the actual cause): `TurnEnvelope.active_queue_id` does prefer `action_gateway` over `lead_capture` by priority, but that mechanism is Phase-0 observation-only and isn't consulted by the real routing code at all; `message_kind` doesn't exist as a working mechanism yet (documented as Phase 4). New `docs/architecture/action-gateway/BUG-115_CONFIRMATION_ROUTING_HIJACK_AUDIT.md` proposes a two-part narrow fix — a "last prompted contract" bookmark reusing the existing `session_store.py` preview-bookmark pattern, and reusing the existing `_describe_contract_for_reconfirmation()` helper for human-readable disambiguation labels instead of raw tool names/ids — neither implemented yet, pending owner decision. No runtime code changed. Does not touch BUG-114's fix.

- **BUG-114 fix — ActionContracts context-interrupt call amplification** (`core/action_gateway.py`) — implements the narrow fix proposed by the same-day audit (`docs/architecture/action-gateway/BUG-114_CONTEXT_INTERRUPT_CALL_AMPLIFICATION_AUDIT.md`), investigated and fixed separately from BUG-111/112/113/PR #393/#399/#400 per explicit instruction not to mix. Root cause: `ExecutionLedger.mark_context_interrupted()` re-marked every "pending" contract for the user on every unrelated inbound message, including ones already `context_interrupted=True` (a production sample showed one unrelated message with 6 live contracts triggering 19 Airtable calls). Fixed with an added filter condition — but **not** the audit's originally-sketched `and not c.context_interrupted`: writing the test for the `reconfirmation_required` case caught that the naive form would silently break the existing one-shot reconfirmation FSM (a contract's second interruption needs a real "superseded" transition even though `context_interrupted` is already `True` from its first interruption). The implemented condition is `and (c.reconfirmation_required or not c.context_interrupted)`. Pure in-memory check, zero new Airtable calls; does not touch `ActionContractRepository.transition()`'s TOCTOU-safe pre-PATCH read or post-PATCH readback verification. New `test_bug114_context_interrupt_amplification.py` (12 checks, all 5 planned scenarios); `test_bug_reconfirmation_oneshot_fsm.py` (27 checks, unmodified) re-run as independent proof the correction doesn't regress the FSM. Full `test_*.py` sweep, `smoke_tests.py`, `compileall`, `git diff --check` all clean. **✅ Production-verified (19/07/2026)**: no repeated per-contract re-marking burst observed after already-interrupted contracts. Two recommendations from the audit (reusing the PATCH response body instead of a readback GET; a TTL/expiry policy for stale pending contracts) remain explicitly deferred, not implemented.

- **PR #400: TurnOwnershipShadow — new shadow invariant for agent-text leaks in gateway-owned turns** (`c31c219`) — purely observational, not flag-gated, no behavior change: when `approval_queued=true` and `reply_owner="gateway"` (meaning A32 should have fully suppressed the agent's own text) but `final_reply` is still non-empty, logs `[TurnOwnershipShadow] violation=agent_spoke_in_gateway_owned_approval_turn` plus a `pattern_class` (approval_invite/pending_status/action_status/unknown), computed against `core/anti_hallucination.py`'s existing patterns (including the `_strip_markdown_emphasis()` normalization from PR #399, so bold/italic wrapping can't hide the classification either). `OwnershipSignal` gains two new fields (`final_reply_nonempty` in the routine log, `leaked_pattern_class` only on the WARNING line). No `app.py`, `ActionGateway`, or `EvidenceFinalizer` taxonomy changes — the needed inputs were already being passed. New tests bring `test_turn_envelope.py` to 95 checks (25 new). **Production-verified**: a same-day sample (see PR #393's runtime note below) showed `final_reply_nonempty=false` and no violation — confirms the invariant correctly recognizes the clean case, not just the leak case.

- **PR #399: BUG-113 follow-up — fix A32 markdown-emphasis and masculine-form gaps** (`72414c3`) — a new production sample after PR #396 still showed the duplicate-message bug, with different phrasing: `שלח **מאשר**` (bold Markdown, two asterisks — the original `\*?` only tolerated one) and `⏳ ...ממתין לאישור` (bare masculine form — `ממתין` ends in Hebrew final-nun `ן` (U+05DF), a different Unicode character from the regular nun `נ` (U+05E0) inside `ממתינה`/`ממתינת`, so an optional suffix could never match it). Fixed with a new `_strip_markdown_emphasis()` helper (strips `*`/`_` for matching only, unbounded count — closes the door on `***מאשר***`/`_מאשר_` too, not just the exact reported case) applied at all four pattern-check sites in `sanitize_agent_response()`, and an explicit `(?:ממתינ[הת]|ממתין)` alternation replacing the broken optional-suffix approach. Extended `test_a32_approval_prose_suppression.py` to 28 checks (10 new). **Production-verified**: a post-deploy sample showed a single clean message, no duplication (the exact double-asterisk variant wasn't re-hit, but the fixed pipeline was proven end-to-end). See `BUG_AUDIT_LOG.md`'s BUG-113 entry (Round 2).

- **PR #396: BUG-113 — A32 suppress approval-invite prose duplicating a queued approval prompt** (`2d86de6`) — production evidence showed a turn that queued a real approval (ActionGateway's own "⏳ בקשת אישור" prompt sent) also let the agent's own text through unsuppressed: "✅ המשימה מוכנה להוספה...\n➡️ הצעד הבא המומלץ: שלח מאשר כדי לאשר...", which `EvidenceFinalizerShadow` correctly flagged as `response_claim=success` against `evidence_status=approval_pending` (`mismatch=true`) — a real Single-Speaker gap, not a taxonomy issue. Root cause: this phrasing matched neither `_AGENT_ACTION_STATUS_PATTERN` nor `_AGENT_PENDING_STATUS_PATTERN`, the two patterns gating A32's existing suppression block; it did match the pre-existing `_AGENT_APPROVAL_INVITE_PATTERN`, but that pattern was only consulted at the NO-TOOL-EVIDENCE gate and only when NO approval was queued — the mirror-image evidenced case fell through untouched. Fixed with a new suppression branch gated on `_gateway_active` AND a real `__approval_queued__` sentinel present this turn, suppressing to `""` rather than a fallback string; also widened `_AGENT_APPROVAL_INVITE_PATTERN` with a "הצעד הבא ... אשר" alternative. Does not touch BUG-111, BUG-112 TTL, F52 formatter states, `ActionGateway` execution semantics, or feature flags. New `test_a32_approval_prose_suppression.py` (18 checks). **Production-verified post-deploy**: log evidence shows suppression firing, `ownership_signal` reporting `reply_owner=gateway`/`agent_claimed_approval=false`, and `EvidenceFinalizerShadow` reporting `response_claim=sent_for_approval`/`mismatch=false` — the user-visible output contained only the gateway prompt. See `BUG_AUDIT_LOG.md`'s new BUG-113 entry (closed).

- **PR #395: docs — regenerate `AI_CONTEXT.md` as daily briefing (main `440234f`)** (`951b1b2`) — refreshed the living production-state doc for PR #388–#393, verified against `main` via git log per `AGENTS.md`'s post-merge protocol; flagged `ROADMAP.md`/`CHANGE_CONTROL_LOG.md` as stale relative to `main` in the doc header.

- **PR #394: BUG-112 production follow-up — normalize stale/missing-callback UX to one message** (`8ac0c93`) — a repeated press on an already-TTL-expired Telegram approval button produced three overlapping "this didn't happen" phrasings for what reads as one event. Confirmed two genuinely distinct callback paths: `_reject_stale_telegram_approval()` (BUG-112 proper — a known pending item past the 10-minute advertised TTL, unchanged, already production-verified separately) vs. a second press where `bus.pop()` finds nothing at all — previously routed through the generic `_notify_stale_or_resolved_callback()` helper, producing a third overlapping phrase. Fixed with a dedicated `_notify_missing_or_expired_callback()` using one literal phrase identically across the popup, persistent message, and edited message. Zero execution-semantics change. Does not touch F52/RP5, BUG-111 lead parsing, or `FEATURE_UNIFIED_STATUS_FORMATTER`. Extended `test_bug112_telegram_approval_ttl.py` to 30 checks (up from 22). **Merged, tests green — defensive/idempotency cleanup, not yet independently production-verified** (no explicit missing/already-consumed-callback sample observed after this deploy). See `BUG_AUDIT_LOG.md`'s BUG-112 entry (Round 2).

- **PR #393: F52 PR6 follow-up — widen EvidenceFinalizer taxonomy for mixed read+approval_pending turns** (`53eb19d`) — production validation of PR6 surfaced one more case: a turn that also performs a verified read before queuing the approval classifies as `evidence_status="mixed"` (not `"approval_pending"`), so PR6's compatibility check still reported a false mismatch. Narrowly widened `core/turn_evidence.py::compare_shadow_final_status()`: `"sent_for_approval"` is now also compatible with `status=="mixed"` specifically when the only non-success contributor is `approvals_pending` itself. Does not touch BUG-111/BUG-112, `ActionGateway`, or the formatter flag's default/enablement. Extended `test_f52_pr6_pending_shadow.py` to 55 checks (up from 50). **Runtime note (updated 19/07/2026):** production logs confirmed the adjacent case (`evidence_status=mixed response_claim=mixed mismatch=false`) early on, and the exact new branch this PR added has since been observed too: `evidence_status=mixed response_claim=sent_for_approval mismatch=false verified_reads=1 approvals_pending=1` (a task-status-check-then-update turn) — both branches now production-verified.

- **PR #392: F52 PR6 — route approval_pending prompt through unified formatter shadow + fix EvidenceFinalizer/ownership plumbing** (`38c2820`) — closed three gaps on the approval_pending surface PR4/PR5 never covered: (1) `_queue_approval_detailed_impl()` sent its own hardcoded "⏳ בקשת אישור..." text directly via `bot.send_message()`, invisible to `FEATURE_UNIFIED_STATUS_FORMATTER=shadow`; (2) `core.turn_evidence._classify_response_claim()` read A32's correct `""` suppression as `response_claim=empty`, a false EvidenceFinalizerShadow mismatch; (3) `build_ownership_signal()`'s app.py call site never set `reply_owner="gateway"` when the agent's text was suppressed. Fixed with new `ActionGateway._render_pending_prompt()`, a new `approval_prompt_sent` parameter threaded through `core/turn_evidence.py`, and app.py's `owner_notified`/`_approval_prompt_sent_this_turn` plumbing. Does not touch BUG-111/BUG-112, does not change the formatter flag's default, does not change A32's suppression decision itself. New `test_f52_pr6_pending_shadow.py` (50 checks). **Runtime shadow evidence observed**: production logs show `[UnifiedStatusFormatterShadow] outcome=pending mapped_state=approval_pending` firing as designed. See `docs/architecture/f52-unified-approval-runtime/PR6_APPROVAL_PENDING_SHADOW.md`.

- **PR #391: docs — sync `BUG_AUDIT_LOG.md`/`CHANGELOG.md` for PR #385–#390** (`8b4e444`) — appended entries for merged work not yet reflected in either governance log: BUG-111 (PR #386 + #390), BUG-112 (PR #387), F52 PR4 (#385) and F52 PR5 (#389), and the AI_CONTEXT.md sync (#388). All entries verified present on `main` via grep before writing.

- **PR #390: BUG-111 follow-up — compact/newline-stripped WhatsApp paste still producing a fake lead name** (`4635bcd`) — a new production sample, seen *after* PR #386 below, showed a WhatsApp paste with no newline between the batch-creation header ("צור 3 לידים חדשים") and the first chat-export timestamp still produced one fake-named candidate (`name="לידים חדשים"`) and created a real Airtable Lead with the wrong name. Three compounding gaps in `core/ingress_classifier.py`: `_BLOCK_SEP` required a literal `\n` before every boundary type including the chat-export header; `_SENDER_LINE_RE` required true line-start even for its bracket-timestamp-prefixed form; `_NAME_STOP` only listed the singular "ליד"/"חדש" (from PR #386), never the plural "לידים"/"חדשים" used in batch headers. Fixed by relaxing `_BLOCK_SEP`/`_SENDER_LINE_RE` to not require a preceding newline for a chat-export header, adding the plural stop-words, and a new defense-in-depth safety net in `_classify_ingress_core()`: a single extracted candidate is discarded (degrading to Tier 5 clarification) whenever the raw text contains more than one distinct phone number. Verified end-to-end for both production samples: all phones detected, no fake name, batch clarification, correct 3-lead preview on resolution. Does not touch F52/RP5/`ActionGateway`/flags. New `test_bug111_followup_compact_text.py` (29 checks); full `test_*.py` sweep, `smoke_tests.py`, `compileall`, `git diff --check` all clean. See `BUG_AUDIT_LOG.md`'s BUG-111 entry (Round 2) for full root-cause detail.

- **PR #389: F52 PR5 — route approval cancellation/rejection replies through unified formatter shadow** (`9973cc5`) — a production log showed rejected/cancelled approvals never emitted a `[UnifiedStatusFormatterShadow]` line: PR4 (`#385`) wired `ActionGateway.compose_status_reply()`'s shadow logging into the executed/status-query path only, but `reject()`/`route_cancellation_word()`/`route_combined_word()`'s cancel branch built a hardcoded legacy string with zero `FEATURE_UNIFIED_STATUS_FORMATTER` involvement. Added `ActionGateway._render_rejection_reply(contract, legacy_text)`, applying the exact same off/shadow/on machinery PR4 already built (`_compose_status_reply_unified()`/`_action_fact_to_message()`/`_log_shadow_comparison()`, unmodified), wired into `route_cancellation_word()` and `route_combined_word()`'s cancel branch — not into `reject()` itself, since three callers branch on `result.startswith("🚫")` as an internal control-flow signal that must stay untouched. `"rejected"` continues to map to the existing `"failure"` canonical state — no new formatter state added. Does not cover `app.py`'s Telegram inline-button reject path (a separate, pre-existing legacy path with its own documented gap) or `route_disambiguation()`'s sibling-contract rejections (never independently shown to the user). Does not change `FEATURE_UNIFIED_STATUS_FORMATTER`'s default (`off`) or set it to `shadow`/`on` anywhere; does not touch BUG-111 lead parsing or RP5/Evidence Finalizer. New `test_f52_pr5_rejection_shadow.py` (35 checks). See `docs/architecture/f52-unified-approval-runtime/PR5_REJECTION_CANCELLATION_SHADOW.md`.

- **PR #388: docs — sync `AI_CONTEXT.md` for PR #385–#387** (`f545075`) — refreshed the living production-state doc (Executive Summary, Current System State, Completed Since Last Update, Next Priorities) to reflect F52 PR4 (`#385`), BUG-111 (`#386`), and BUG-112 (`#387`) below, verified via grep against `main` per `AGENTS.md`'s post-merge verification protocol.

- **PR #387: BUG-112 — enforce Telegram approval button TTL before execution** (`f639c33`) — the Telegram inline approval button advertises "פג תוקף בעוד 10 דקות" (expires in 10 minutes, `app.py`'s `_PENDING_APPROVAL_TTL`), but `_handle_approval_callback_impl()` only enforced `event_bus.py`'s own, separate, longer `PendingActionsStore.PENDING_TTL_MINUTES` (30 minutes) — a button press between minute 10 and minute 30 still executed the tool. Added an independent TTL check against the popped item's `"created"` timestamp for every "approve" callback, right after popping from the bus and before any dispatch decision; new `_reject_stale_telegram_approval()` never dispatches, durably rejects the matching `ActionGateway` contract when one can be positively verified, notifies the approver with a persistent chat message ("⏰ פג תוקף — הפעולה לא בוצעה") rather than only the transient callback-query popup, edits the original message so it no longer looks actionable, and never falls through to legacy dispatch. Only `app.py` touched; no change to BUG-111 lead parsing, F52/RP5, `FEATURE_UNIFIED_STATUS_FORMATTER`, or tool execution semantics beyond stale-approval blocking. New `test_bug112_telegram_approval_ttl.py` (22 checks). See `BUG_AUDIT_LOG.md`'s BUG-112 entry.

- **PR #386: BUG-111 — fix lead batch parsing for domain context and sender-prefix lines** (`6bb3b61`, follow-up `7b2cd5c`, CI-isolation fix `c3499f5`) — `core/ingress_classifier.py`'s live lead-extraction path mistook command/context text and WhatsApp-style sender prefixes for a lead's name (`"צור ליד דומיין גיוס ..."` → `name="דומיין גיוס"`), and didn't recognize two-separator phone formats (`05X-XXX-XXXX`, `+972 XX-XXX-XXXX`) or no-year WhatsApp export timestamps (`[D.M, HH:MM]`) at all, causing valid input to fall through to the Agent's own tool_use loop and hit the LeadsWriteGate denial. Fixed via new `_PHONE_RE` alternatives, an optional year group in `_CHAT_EXPORT_TIMESTAMP`, a timestamp-aware `_JSON_BLOCK_RE`, and new `_extract_domain_hint()`/`_strip_domain_hint()` recognizing "דומיין X"/"לדומיין X" as a routing annotation rather than a name. Same-PR follow-up fixed a phone-only batch (no names at all) collapsing to a single phone with 2 silently dropped — `_maybe_start_lead_clarification()` rewritten to use `.finditer()` for all phones and branch into a new batch name-clarification flow (`_resolve_batch_name_clarification()`). A CI-only follow-up fixed 9/50 tests failing in CI-but-not-locally due to `session_store.py` persisting to real Airtable with colliding literal `chat_id`s across rapid consecutive CI runs — fixed by uuid-suffixing test chat_ids, no product change. New `test_bug111_lead_domain_and_sender_prefix.py` (50 checks). See `BUG_AUDIT_LOG.md`'s BUG-111 entry (Round 1) for full root-cause detail and before/after table.

- **PR #385: F52 PR4 — shadow verification for `ActionGateway.compose_status_reply()`** (`1432eba`) — prepared and verified one real user-facing path (ActionGateway action-status replies) for a safe formatter cutover without changing default production behavior; strengthened shadow-mode observability with safe-fields-only logging (`_log_shadow_comparison()`/`_shadow_leak_flags()`). `FEATURE_UNIFIED_STATUS_FORMATTER` default unchanged (`off`). 13 new checks added to `test_f52_status_reply_reconciliation.py` (21 total). See `docs/architecture/f52-unified-approval-runtime/PR4_ACTION_STATUS_SHADOW_VERIFICATION.md`.

- **PR #372: BUG-110 — replace non-canonical `status="converted"` writers** (`fa1506e`) — `lead_conversion.py` and `ad_attribution.py::mark_converted()` wrote `status="converted"`, not a member of `LeadStatus.ALL`/not a live Leads.status option. Both now write `status=LeadStatus.DONE` + `Business Outcome=LeadOutcome.CONVERTED` using existing constants — no new Airtable option, no rename, no backfill. `ad_attribution.py` deliberately left on `tools.airtable_tools.airtable_update` (not the canonical gateway) — migrating would have changed its `dict`-based return contract and broken the existing `test_response_contract_fixes.py` regression test; disclosed as separate tech debt. New `test_bug105_non_canonical_converted_status.py` (10/10) — filename/PR/commit use "BUG-105," the label originally assigned before a numbering collision with an existing, unrelated BUG-105 was found in `BUG_AUDIT_LOG.md`; governance docs use **BUG-110** instead (owner decision), code artifacts were not renamed retroactively.

- **PR #371: BUG-104 Phase 2A.1 — Current State Policy SPEC** (`fde5c51`) — new `docs/architecture/bug-104/PHASE_2A1_CURRENT_STATE_POLICY_SPEC.md`. Audit + SPEC only, no code. Documents (with file:line citations) that `Business Outcome` is not read anywhere in today's live reasoning chain (`leads_reasoning_projection.py` → `LeadsAdapter` → orchestrator) — only `status` feeds it, and only 2 of 10 live status values are handled intentionally. Proposes a precedence policy (Business Outcome terminal overrides status; Score never determines state; Lead Events feed confidence only) for a future code PR, pending owner approval on open questions (state enum naming, terminal-administrative mapping, `status=done` without Business Outcome).

- **PR #370: BUG-104 Phase 2A.0 — Leads Schema Canonicalization SPEC** (`8b4d51e`) — new `docs/architecture/bug-104/PHASE_2A0_LEADS_SCHEMA_CANONICALIZATION_SPEC.md`. Audit + SPEC only, no code/schema/frontend change. Live Airtable schema + value inventory (39 fields, 92 records) plus a full code read/write map; found 5 independent uncoordinated tier/temperature implementations, 2 non-canonical `status="converted"` writers (root cause behind PR #372/BUG-110), 3 mutually contradictory doc claims about the `tier` field, and that `Domain category`/`Domain risk assessment`/`Domain summary` are a misapplied Airtable AI feature reading the internal business-domain slug as if it were an internet domain name.

- **PR #369: fix(tma) — remove temporary auth debug logging** (`62be9a8`) — `tma_api.py`'s `_log_projects_auth_debug()` and its 5 call sites inside `require_tma_auth()` removed (19 lines), introduced 10/07 (`9b51537`) and never removed since. No other behavior change — HMAC validation/401 responses/`resolve_identity()` untouched.

- **PR #364: governance docs sync for #354–#362** (`1d31aab`, merge `80fdfae`) — updated `ROADMAP.md`, `CHANGELOG.md`, and `CHANGE_CONTROL_LOG.md` with the nine merged work items below. Explicitly preserved the separate historical boundaries instead of silently expanding scope: this changelog still lacks individual #348–#353 entries, and Change Control still lacks #327–#353 after C111.

- **PR #363: AI context daily briefing refresh** (`28d4f09`, merge `60991c1`) — changed `AI_CONTEXT.md` only. It summarized #354–#362 but did not update `ROADMAP.md`, `CHANGELOG.md`, or `CHANGE_CONTROL_LOG.md`; the actual governance-doc fix was PR #364.

- **PR #362: PR-RP4 — shadow evidence finalizer** (`3a3edbe`) — new `core/turn_evidence.py`, wired into `app.py`; `FEATURE_EVIDENCE_FINALIZER` off/shadow/enforce, default `off`. Even `"enforce"` is comparison-only for now (real enforcement deferred to RP5). New `test_turn_evidence_shadow.py`.

- **PR #361: TMA — temporarily hide saas-domain card from Projects Hub** (`bee46b5`) — `tma_api.py::_get_project_cards()` now skips any ProjectsHub record whose `domain == "saas"` from the `GET /api/projects` response only. The Airtable record, its `slug`, and its `domain` field are untouched — display-layer filter, not a data change. Opened via `--force` past `pre_session_gate.sh` (3 unrelated unmerged branches at the time, approved explicitly).

- **PR #360: BUG-104 TMA Lead Event Bridge** (`0a0c331`) — new `core/lead_event_writer.write_tma_lead_event(lead_id, action, applied_fields, lead_domain=None)`, best-effort and never raising. Wired into `tma_api.py`'s owner-immediate `patch_lead()`/`set_lead_outcome()` (after a successful `_at_patch`) and `tools/approval_actions.py::tma_write()` (after a successful `patch` on the `Leads` table only — covers manager-approved `patch_lead`/`set_lead_outcome` and `update_lead_status`, which always routes through approval). Root cause: none of the three TMA lead-write paths ever wrote to `Lead Events` — the BUG-104 reasoning projection's evidence table was never populated by the primary (TMA) CRM workflow. Domain is read from the Lead's own `domain` field (not a project_slug mapping) and validated against the live `Lead Events.Domain` allowlist (`real_estate/saas/import/recruitment/general/media/crm`), falling back to `general` — no schema change, no domain-slug mapping table needed (raw `Leads.domain` values already match the live enum 1:1, verified via Airtable MCP). `Channel` is a literal `"tma"` (not `identity.channel`, which resolves to `"telegram"` for every TMA-authenticated identity). Explicitly not gated by `LEAD_CAPTURE` and does not reuse `capture_lead_event()`'s inbound-message inference. 46 new tests (`test_bug104_tma_lead_event_bridge.py`), full regression green.

- **PR #359: PR-RP3 — enforce unavailable-tool schema filtering** (`59eafd1`) — `FEATURE_TOOL_AVAILABILITY_FILTER=enforce` now actually hides role-allowed-but-locally-unavailable tool schemas from the Agent (RP2's shadow diagnostics promoted to a real filter); default remains `off`.

- **PR #358: PR-RP2 — shadow tool availability diagnostics** (`1b17337`) — per-role tool availability shadow logging in `tool_registry.py`/`context.py`, `FEATURE_TOOL_AVAILABILITY_FILTER=shadow`; log-only, no schema exposure change. 194-line `test_tool_availability_shadow.py`.

- **PR #357: BUG-104 Phase 1.1 — lead status/linkage contract hardening** (`08ad671`) — `LeadsAdapter._normalise_status()` now returns literal `DecisionStatus` values (`Open`/`Decided Yes`/`Decided No`/`Cancelled`) instead of a parallel `OPEN`/`DECIDED_YES`/... vocabulary that never matched what the shared Attention/Orchestrator engines compare against — same classification, corrected literals only, proven by invoking the real (unstubbed) engines. Added two-sided Lead Events linkage validation: an event's own `Lead` field must explicitly contain the current `lead_id`, not just be reverse-link-reachable. No public contract change (`PROJECTION_VERSION` stays 1, `events` stays `{available, count}`). 57 new tests (`test_bug104_phase1_1_contract_hardening.py`), 102/102 existing projection tests still green.

- **PR #356: PR-RP1 — tool registry invariant enforcement** (`b29fbcb`) — `tool_registry.py` gains `validate_tool_invariants()`/`ToolRegistryInvariantError`, an always-on (not flag-gated) structural check of the registry itself. 120 new tests (`test_tool_registry_invariants.py`).

- **PR #355: PR-RP0 — Runtime Reliability & Permission Hardening planning docs** (`4efb61b`) — docs-only: `RUNTIME_RELIABILITY_AND_PERMISSION_HARDENING_SPEC.md`, `BOSS_PRODUCTION_RUNTIME_MAP.md`. Opened via `--force` past `pre_session_gate.sh` (explicitly authorized).

- **PR #354: BUG-104 P1-A/B/C re-audit** (`71f04fb`) — three fixes from an independent audit (FIX_REQUIRED verdict), no scope change: (P1-A) Lead Events lookup now queries by the linked records' own `RECORD_ID()` keyed from the Lead's reverse-link field, replacing a formula that matched the linked record's *display value*, not its ID; empty link → `[]` with zero reads, real failure → explicit unavailable. (P1-B) `_normalize_lead_snapshot()` maps live lowercase Lead fields to the CamelCase names `LeadsAdapter` expects, before the adapter runs, without mutating the record — fixes `status=lost` wrongly defaulting to `OPEN`, `domain=real_estate` collapsing to `general`, `channel=email` collapsing to `whatsapp`. (P1-C) `readiness` is now an explicit honest state (`unknown`/`unavailable` + reason), never copied from lifecycle `phase`. Rewritten test suite, 102 assertions including a real Flask test-client section.

- **PR #347: approval-callback context-interrupt, stale fallthrough, and duplicate-result fixes** — three follow-up defects found during production verification of PR #345's batch behavior (confirmed correct there: each deferred task promoted into its own pending `ActionContract`, tasks approved individually through separate Telegram buttons, one Atomic Claim + one Airtable POST each, no promoted task executing automatically). (1) **BUG-APPROVAL-CALLBACK-CONTEXT-INTERRUPT**: the global ingress context gate (`_apply_ingress_context_gate`) marked `context_interrupted` on every inbound callback, including the `approve:`/`reject:` button press itself — a legitimate resolution event, not an unrelated message. Fixed by exempting callback events whose data starts with `"approve:"`/`"reject:"`, the same principle already applied to text confirm/cancel via `is_own_resolution_event()`. (2) **BUG-STALE-CALLBACK-FALLTHROUGH**: a stale/replayed/unlinked Telegram approval callback (no live or terminal `ActionContract` found for its fingerprint at all) fell through to legacy direct `dispatch_tool()` with no Gateway approval and no Atomic Claim behind it — live-incident evidence, a 15:55:39 callback reached direct dispatch with zero Gateway involvement. Fixed: when `FEATURE_ACTION_GATEWAY` is on and no contract is found, the callback now fails closed (deterministic expired/already-resolved reply, zero dispatches); flag-off behavior is unchanged. (3) **BUG-DUPLICATE-PLAIN-STRING**: `tools/dispatcher.py`'s `airtable_add` dedup check returned a plain string on a duplicate hit, which `verify_execution()` misclassified as `"expected structured result dict; got plain string"` — a false execution failure for a correct no-op. Fixed to return a structured `{ok: True, outcome: "already_exists", external_id, evidence, user_message}` result, presented as "already exists, no duplicate created," not a failure. Two existing tests that had locked in the old buggy behavior as expected were corrected. 14 new tests (`test_bug_approval_callback_hardening.py`); full 111-file suite passes, including `test_bug_batch_approval_preserved.py` (33/33) confirming PR #345's batch mechanism is unaffected.

- **PR #345 (BUG-BATCH-DISCARD): restored multi-task batch preservation** — a single message requesting multiple tasks (e.g. "צור לי 5 משימות") only preserved and approved the first; the rest were silently discarded, a regression traced via `git log -S` to commit `9ab4af7` (30/06/2026, BUG-043/BUG-V1-MULTI-PENDING-PAYLOAD-CONTAMINATION), which blocked any second mutating-approval tool call in the same agent turn outright to guard against a payload-contamination risk that was never actually reproduced (each `_queue_approval()` call already made its own independent copy/contract, both before and after that fix). A naive "just remove the block" fix was implemented, tested empirically, and rejected before finalizing: once >1 contract is simultaneously live, `route_confirmation_word()` no longer directly executes a plain "מאשר" (falls into the `len(live)>1` disambiguation branch), and `route_disambiguation()`/`route_combined_word()` deliberately reject every sibling contract the moment one is picked by number (§21, commit `6752ec0` — a design for disambiguating between alternative interpretations of *one* request, not for preserving independent batch items; proven live that this rejected the other 4 tasks instantly). Fixed instead with a small queue: every mutating tool call is durably preserved (new `event_bus.BatchQueueStore`), only the first becomes a live `ActionContract` + Telegram notification, and the rest are promoted one at a time (`app._promote_next_batch_item()`, called after every approval-resolution attempt) — never more than one contract live per identity for a batch. `ActionGateway.approve()`/`reject()`/`_execute_contract()`/Atomic Claims untouched. 33 new tests (`test_bug_batch_approval_preserved.py`).

- **PR #344: canonical tool selection + post-completion callback fallthrough fixes** — two live-incident fixes in the approval path. (1) **Canonical tool wiring**: `resolve_canonical_tool()` (`core/action_gateway.py`) existed and was unit-tested in isolation (DoD19) but was never actually called from `propose_action()`/`_queue_approval()` — a "create task" request could create a genuine `sheets_append` `ActionContract` (an unconstrained Agent tool_use choice), which Atomic Claims then correctly executed, failing on missing Google OAuth exactly as expected for the wrong tool (not a routing bug). Wired in at both `propose_action()` (the single contract-creation entry point) and `_queue_approval()` itself, resolved once and consistently before the dedup fingerprint/button label/legacy bus payload are built — resolving only inside `propose_action()` would have left the legacy bus item pointing at the original hint while the contract stored the resolved one. (2) **Post-completion callback fallthrough**: a stale Telegram approval button (pressed after the same action already completed via text "מאשר") fell through to a legacy direct-dispatch path and re-attempted the tool, surfacing a misleading `"expected structured result dict; got plain string"` failure. Root-caused to `app.py` calling the nonexistent `bus.get()` (`EventBus` only ever exposed `pop()` — every call raised `AttributeError`, silently logged as "non-blocking," so the pre-check never actually ran) plus a missing terminal-status backstop before the legacy-dispatch decision. Fixed with a real `EventBus.peek()` and an authoritative terminal-contract short-circuit, independent of the earlier pre-check. 18+24 new tests.

- **PR #343 / #341: Single-Speaker contradictory fallback and duplicated/redundant approval text** — two related live-incident findings in the approval reply path (`contract_id=9c6ff34e-4714-46c0-a385-0ac860c8c3e0`). `sanitize_agent_response()` replaced the agent's post-approval text with a generic failure fallback even though a pending-approval message had already been sent this turn — falsely implying failure on an action that was, correctly, still pending. `ActionGateway._execute_contract()` separately appended the executor's own C53-A success text as a second line after `compose_status_reply()`'s canonical message, producing two success statements in one reply. Both fixed via the existing `__approval_queued__` sentinel: the Single-Speaker gate now suppresses (rather than replaces with a false-failure message) the agent's redundant text when a real approval was queued this turn, and only `compose_status_reply()`'s text is ever surfaced (the executor's raw response stays available internally via `ActionFact.raw_tool_response` for audit). A same-day follow-up (#343) extended the same gate to also catch the agent's *pending/ready-for-approval* narration, not just false-completion claims — the original completion-verb-only pattern missed it in multi-turn flows (a field completed on a later turn). 27 new tests (`test_single_speaker_fallback_and_duplication.py`).

- **PR #339 / #340: `test_stage_b_full_suite.py` identity + executor-signature fixes (test-file only)** — the fixture never passed a resolved `identity=` to `propose_action()`, and its 14 local mock executors didn't accept the `identity`/`claim_execution_id` keywords the real atomic executor passes — both harmless while `FEATURE_ATOMIC_CLAIMS` is off, but surfaced as real failures once run against Render's `FEATURE_ATOMIC_CLAIMS=true` stabilization environment (predates PR #335/#336, not a regression from them). No production code changed. Found and left unfixed as a genuine architectural interaction, not a fixture bug: the override-reexecution feature (Req6) collides with the atomic-claims one-time-execution guarantee, since `route_override_word()`'s re-execution of the same contract derives its idempotency key from `contract_id+approver` alone, colliding with the first execution's already-claimed row.

- **PR #336 / #335 / #337 / #338: Phase 4B rollout tooling + planning** — report-only tooling for the Phase 4B rollout/cutover of the durable TMA approval flow: `tools/phase_4b_rollout_readiness.py` (`--mode preflight`/`active` GO/NO-GO/WARNING), `tools/phase_4b_reconciliation.py` (17 anomaly checks across ActionContracts/Approvals/PostgreSQL claims), `tools/phase_4b_mark_legacy_approvals.py` and `tools/phase_4b_repair_projections.py` (mutation paths gated behind both `--apply` and an exact `--confirm` token), `tools/phase_4b_canary_verify.py` (requires `--expected-outcome`, compares requester/approver via stable IDs). No Render env var, feature flag, Airtable record, or production PostgreSQL row touched by any of it. #337 established the F52 Unified Approval Runtime migration program (docs only); #338 clarified that #335's verification-hardening work sits on top of #336's tooling, not as a competing implementation.

- **PR #334: Phase 4B-2 — Approvals becomes a non-authoritative ActionContracts projection** — TMA business writes now flow through `ActionGateway.propose_action()`/`approve()`/`reject()` via a new internal `tma_write` dispatcher tool, instead of direct Airtable writes from `tma_api.py`. `Approvals` rows are now a pure display mirror (`action_contract_id`/`legacy_read_only`/`projected_lifecycle_status`); legacy pre-migration rows are refused before any contract lookup, both for reads and approve/reject. Closed a direct-dispatch bypass with genuine PostgreSQL claim verification (`execution_context` checked against a live `EXECUTING` claim row via `get_claim()`, not a forgeable caller-constructed dict). `tma_write` now requires `FEATURE_ATOMIC_CLAIMS=true` to execute at all — an intentional consequence of making PostgreSQL claims the sole execution-ownership primitive for this tool, not a regression (the flag remains off in production pending staged rollout).

- **PR #333 / #332 / #329: Phase 4B-1/2 durable ActionContract persistence (schema/persistence layer, no live wiring yet)** — proposal persistence + restart recovery (#329), lifecycle transition persistence with version/read-back validation for approve/reject/completed/failed/outcome_unknown (#332), and Approvals-projection schema prep (#333: three additive `Approvals` fields plus a pure, side-effect-free `core/approvals_projection.py` lifecycle-mapping module with no Airtable/PostgreSQL I/O). PostgreSQL atomic claims remain the sole execution-ownership boundary throughout — none of this wires TMA/Approvals routing to the new persistence yet (that came later, in #334 above).

- **PR #328, reverted same day by #331**: a documentation merge-status correction (BUG-077/BUG-DH-03/04) was itself found to be based on an initial check run without `git fetch` first; reverted rather than left half-verified. Net no lasting change — recorded here only so the churn isn't silently missing from history.

- **Phase 4B0.1D P0 Fix (PR #326, C111): unhashable Identity crash in atomic-claims execution wrapper** — Staging-verified real PostgreSQL issue. `execute_with_atomic_claim()` called the executor function using positional arguments; the real executor's signature placed `contract_id` in position 3 but the wrapper was passing `identity` there instead. The mutable `Identity` dataclass (deliberately unhashable) silently bound to the `contract_id` parameter, then `ExecutionLedger.find_by_id()` tried to use it as a dict key inside `self._store.get(contract_id)`, crashing with `TypeError: unhashable type: 'Identity'`. Fixed in three layers: (1) all executor_fn() calls now use keyword arguments (`contract_id=..., identity=...`); (2) `_execute_contract()` reconstructs `Identity` fail-closed from the frozen `ActionContract` fields (tenant_id, actor_user_id, actor_role, actor_external_id — all four required, else immediate contract failure); (3) `_make_dispatch_executor()` accepts explicit `identity=` keyword parameter (used when supplied from atomic wrapper, derived from contract_id when omitted for legacy path). Separately fixed a latent gap: real `dispatch_tool()` never returns structured `DispatcherOutcome`; added classification logic wrapping raw tool results (dict/string) via `verify_execution()` to properly transition claim status (completed/failed/outcome_unknown) instead of always failing closed. New file `core/dispatcher_outcome.py` (immutable @dataclass with result/user_message/external_id/error/raw_response). New regression suite `test_p0_unhashable_identity_atomic_wrapper.py` (18 tests) verifies against pre-fix code — all pass post-fix. Staging logs show identity preservation, real provider execution, and claim completion. `FEATURE_ATOMIC_CLAIMS` production OFF (pending staged rollout plan). See C110/C111 in `CHANGE_CONTROL_LOG.md`.

- **Phase 4B0.1C Atomic Claims Wiring (PR #325, C110): confirm-intent wiring convergence** — All four approval pathways (direct-confirm, undo-intent-verb, await-context, self-confirm-lead-write) incorrectly routed through a single bypass gate (`_propose_as_approved()`) instead of calling `ActionGateway.propose_action()`. This meant permission gates were checked too late (after the caller already decided the intent), identity fail-open fallback was applied (readonly user could pass through), and DispatcherOutcome success classification never happened (all outcomes treated as equally failed). Converged all four pathways through the central `propose_action()` gate, with carve-outs only for `self_confirm=True` leads (capture-time self-write, no approval needed). Added `contract_id` tracking through confirmation routing for end-to-end evidence chain. 26 new tests (11 wiring regression, 15 result classification). See C110 in `CHANGE_CONTROL_LOG.md`.

- **BUG-PENDING-APPROVAL-B (PR #311, #312, #313, #314): pending lead-approval "context poisoning" — fixed and verified in production end-to-end.** An unrelated message arriving between a lead-write preview and the user's "כן" could let that confirmation silently approve the older, now-stale action instead of the one the user actually meant. Closed across four PRs, each merged only after live production testing exposed the next gap in the previous fix:
  - **PR #311**: `ActionContract` gained `context_interrupted`/`reconfirmation_required`; `route_confirmation_word()`'s single-live-contract branch re-shows a readable business description and requires an explicit second "כן" instead of executing directly once an intervening message arrived.
  - **PR #312**: production testing showed the interrupt-marking hook lived only inside `run_agent()`, missing every message handled by a different code path (slash commands, callback buttons, wizard text/file capture, general media, Decision Hub attachment references). Replaced with a single global ingress-context gate applied once per inbound webhook event on both channels (Telegram, WhatsApp Twilio, WhatsApp Meta), after auth/dedup/identity resolution, before any routing. `ActionGateway.is_own_resolution_event()` reuses the real confirm/cancel/disambiguation routing logic to decide the one exemption, so it can't drift from actual behavior. Fail-closed: a marking failure is tagged `context_integrity_unknown` (never silently treated as "context intact").
  - **PR #313**: production testing then showed the legitimate second "כן" (the actual reconfirmation) was rejected by `guards.idempotency` as a duplicate of the first "כן" — the Telegram webhook's dedup key was the raw message text, so two distinct messages with identical text collided. Fixed to key on the provider's own event identity (`update_id:message_id`) instead; WhatsApp's two call sites already used a true unique id and were unaffected.
  - **PR #314**: production testing then showed that after the first reconfirmation prompt was shown once, a *second* intervening action didn't re-arm anything — the next "כן" executed the old contract directly. The boolean design only distinguished "interrupted vs not" and permanently disabled the check once `reconfirmation_required` latched. Replaced with a bounded one-shot FSM: a second interruption after the prompt was already shown supersedes the contract outright (terminal, not re-armed), with a specific "your previous action was superseded, resubmit" message instead of a silent dead end. Separately, `compose_status_reply()`'s execution receipt now reuses the frozen contract's business description instead of showing the bare tool name.
  - **Final verification (12/07/2026)**: a full verbatim production transcript matches the designed behavior exactly across the whole chain — preview, two interruptions, first "כן" (exact reconfirmation wording), a third interruption after the prompt was shown, second "כן" (exact supersede wording), no execution. See `BUG_AUDIT_LOG.md`'s BUG-108/BUG-PENDING-APPROVAL-B entry and its three Follow-ups for full detail.

- **BUG-081 (C101, PR #259/#260/#261): Business Memory `Domain` written to its own field, not overloaded into `Tags`**: `BusinessMemoryFields` had no dedicated `Domain` field (unlike every other table in the schema), so `cmd_update.py`/`media_handler.py` wrote the raw internal domain key (e.g. `"media"`) directly into the generic `Tags` `multipleSelects` field with no guarantee it was an already-defined option — Airtable rejects unknown select options with a 422 (no `typecast=true` in the gateway). Added `BusinessMemoryFields.DOMAIN` and `cmd_update.normalize_business_memory_fields()`, called from both write sites: maps the domain key to a confirmed live `Domain` option and filters `Tags` down to actual valid options. A first-pass mapping sent `"real_estate"` (lowercase) to the `Domain` field — confirmed via live production evidence to be stale (Airtable had 2 duplicate real-estate options; the lowercase one was removed, leaving only Title Case `"Real Estate"`) — fixed in a follow-up commit. A second live 422 then surfaced: `_VALID_TAGS` still listed lowercase `"real_estate"` as a valid standalone tag, so it kept reaching Airtable via `Tags` even after the `Domain` fix; removing it first produced an empty `Tags` instead of the correct value, so a `_TAG_NORMALIZE` map was added to canonicalize it to `"Real Estate"` before filtering. Final behavior for `domain="real_estate"`: `Domain="Real Estate"` and `Tags=["Real Estate"]`, no lowercase value reaches Airtable anywhere. **Known follow-up, not yet built**: `weekly_summary.py::_group_by_domain()` and `tma_api.py`'s Business Memory listing still read `Tags[0]` as the domain and will silently default once new records carry `Domain` instead — deferred, piggybacked on either feature's activation (`FEATURE_WEEKLY_SUMMARY` off, TMA business memory screen not in active use).
- **BUG-080 (C100, PR #258): Date-only Airtable fields no longer sent full datetime strings**: `BusinessMemoryFields.DATE`, `DecisionEventFields.EVENT_DATE`, and `DecisionInboxFields.RECEIVED` are plain `Date` fields in Airtable (confirmed against live field-type metadata across all 4 Decision Hub tables), but all 7 manual write sites (`cmd_update.py`, `media_handler.py`, `cmd_decision.py` ×5) sent a full `datetime.now(...).isoformat()` string (time + microseconds + UTC offset) — rejected with a 422 (no `typecast=true` in the gateway). Changed to `.date().isoformat()` at all 7 sites; two `FileUploadResult(timestamp=...)` sites (an in-memory `session_store` object, not an Airtable field) and one read-only sort-key usage were deliberately left unchanged.
- **C99 (PR #257): extract document text into `/update` Business Memory entries**: photo/document attachments captured mid-`/update` (see BUG-078 below) were only saved with their caption and Drive link — the document's content itself was never read. Added `media_handler.extract_text_if_document(file_bytes, mime_type)`, wrapping the existing `document_converter.convert_document()` (handles temp-file writing/cleanup and unsupported-format/conversion-failure fallback to `None` internally); `capture_photo_or_document` now folds the extracted text into the saved entry alongside the caption and Drive link.
- **BUG-079 (C98, PR #256): `/update`'s free-text step now reaches `capture_text` instead of leaking into `run_agent`**: `app.py`'s webhook only called `bot.process_new_updates()` (the only thing that dispatches `@bot.message_handler`-registered handlers, including `cmd_update.py`'s `capture_text`) for slash-command text — plain free text sent during `/update`'s final step fell through to `idempotency.is_duplicate()` and then the general `run_agent()` pipeline instead, silently abandoning the pending wizard state until its 30-minute TTL expired. This had been broken since `cmd_update.py` (C20) was first added — the wizard's primary use case, not an edge case. Added `cmd_update.has_pending_text_capture()`, checked right after the slash-command branch and before the idempotency check (fail-open on error); when a session is waiting on its text step, the webhook now dispatches through `bot.process_new_updates()` itself.
- **BUG-078 (C97, PR #255): `/update` now claims a photo/document attachment instead of losing it**: `app.py`'s webhook routed `photo`/`document` updates straight to the generic Drive-upload handler (`_handle_telegram_media`) before ever calling `bot.process_new_updates()`, so an attachment sent while an `/update` session was waiting on its free-text step fell through to the generic upload flow — losing the chosen domain/entry_type and orphaning the pending wizard state until its TTL expired. Added `cmd_update.has_pending_file_capture()`/`capture_photo_or_document()`, checked in `app.py` before the generic media handler for `photo`/`document` content types; the file is uploaded via the existing `media_handler` pipeline (best-effort) and saved to Business Memory with the domain/entry_type already selected in the wizard.

- **C94: added `FEATURE_INGRESS_ENVELOPE` kill-switch**: C94 (below) shipped across 4 merged PRs with no feature flag at all. Added one now — a single-line change to the `if` condition in `app.py`'s `run_agent()` (`if _flag_enabled("FEATURE_INGRESS_ENVELOPE") and raw_event_id and channel in (...)`), plus a `feature_flags.py` registry entry. Critically, this flag **defaults to ON, not OFF** (the opposite of nearly every other flag in the system) — confirmed in code before pushing that `is_enabled()` returns `False` for any flag name that isn't defined anywhere, so a normal default-off flag here would have silently disabled C94 the moment this shipped, before anyone could set it in Render. Fixed the same way `IMPORT_DOMAIN` (the only other default-on flag) is: added to `feature_flags.py`'s `_DEFAULTS` dict (`os.environ.get("FEATURE_INGRESS_ENVELOPE", "true")`). Verified before pushing: all 138 C94 tests pass identically whether the flag is left completely unset (simulating "not yet configured in Render") or set explicitly to `true`; separately confirmed `=false` actually suppresses envelope construction (0 calls to the adapter), not a no-op. Zero regression on the full suite.
- **C94: Unified Ingress Envelope + Evidence Trace** (PR #236–#239, all 4 stages): a schema layer sitting *before* `classify_ingress()`/C89/C90 — `core/ingress_envelope.py`'s `IngressEnvelope` (pre-classification, 7 fields + `envelope_id`) and `EvidenceTrace` (post-classification, `envelope_id` FK + `trace_id`/`attempt_no`/`status` + `classification_result`/`classification_error`/`raw_ref`/`approval_contract_id`/`agent_observation`) — two separate dataclasses, never merged into one object (an explicit alternative to collapse them was proposed and rejected mid-build, since that's exactly the anti-pattern the source spec's own "corrected version" was written to prevent). Built and merged one channel at a time, each stage gated on explicit review of the actual diff (not just a pass/fail report) before the next began: **Stage א** (schemas only, plus 3 real schema gaps found and closed before/during Stage ב — `raw_ref` moved from Envelope to Trace since it can only exist after `classify_ingress()` runs; `envelope_id`/append-only `record_classification()`/`classification_error` added so a retried or failed classification attempt still leaves evidence instead of silently vanishing; `trace_id`/`attempt_no`/a computed `status` property added for multi-attempt tracking); **Stage ב** (`core/file_ingress_adapter.py`'s `build_file_row_envelope()`, wired into `app.py`'s `_process_structured_file_upload` — caught and fixed a real pre-existing gap along the way: the row loop never wrapped `classify_ingress()` itself in try/except, so one row's exception silently dropped every remaining row in the file); **Stage ג** (`core/telegram_ingress_adapter.py` + `core/router/capture_router.py`'s `classify_capture_ic()` now wraps its own `classify_ingress()` call — previously an exception there propagated through `app.py`'s `_safe_route()`, whose blanket catch failed the *entire* router closed, Handler.APPROVAL/intent=UNKNOWN, regardless of intent/domain/risk; now it degrades to `capture_ic=None`, same as every existing "not applicable" case, with a sanitized `EvidenceTrace(classification_error=type(exc).__name__)`); **Stage ד** (`core/whatsapp_ingress_adapter.py`, Twilio only — Meta Cloud API's `run_agent()` call deliberately never receives a `raw_event_id` so it's untouched/still gated, on purpose, to avoid repeating BUG-071's exact "diverged silently between WhatsApp providers" mistake in miniature). Every sanitized error value recorded (Trace field and internal log) is `type(exc).__name__` only — never `str(exc)`/`exc_info=True` — to avoid leaking message text/PII. 138 new tests across the 4 stages (57+41+28+12), zero regression against the full existing suite at every stage. No feature flag — deliberately, since every stage is equivalence-preserving (proven per-stage) and degrades gracefully rather than changing behavior; this does diverge from `RELEASE_CHECKLIST.md`'s "feature flag defined, off by default" checklist item, noted explicitly rather than silently skipped. No new Render env vars. Production verification (Render deploy hash, live Telegram/WhatsApp smoke messages) not yet done — see `AI_CONTEXT.md` §0.12.

- **C90: Structured File Capture** (PR #228): file upload is now a new ingress *source adapter* only — not a new capture pipeline, classifier, or write path. `core/file_ingress_adapter.py` (new) parses xlsx/csv into per-row text (openpyxl/`csv`); each row is dispatched, one at a time, through the *unmodified* `classify_ingress()`/`handle_lead_candidate()` pipeline a text message would use — no special-casing, so a row with a clear name+phone legitimately resolves to Tier 1 (not forced to Tier 4). Each row gets its own `raw_ref`/`AgentObservation`; Tier 1-3 rows each create a separate `ActionGateway` contract requiring individual approval (no bulk auto-approve). Gated behind a new `FEATURE_STRUCTURED_FILE_CAPTURE` flag (default off) plus `identity.is_internal` plus xlsx/csv extension/mime detection — everything else falls through to the existing `FEATURE_MEDIA_UPLOAD` behavior unchanged. Caught and fixed a real bug before merging: the row-to-text separator (`" | "`) collided with the existing pipe-table Tier-4 hard marker, force-classifying any row with 3+ populated columns as Tier 4 regardless of content — fixed to `", "`. 37 new tests.
- **C89-RAW-OBS follow-up** (PR #227): the PR #224 fix was functionally correct but `_classify_ingress_core()` still literally wrote `raw_ref=""` in all 8 internal return statements (immediately overwritten by the `classify_ingress()` wrapper, but still visible to a static grep). `raw_ref` now defaults to a private `"__unset__"` sentinel so no code path writes the empty-string literal at all — `grep -rn 'raw_ref=""'` now matches nothing outside the new test's own assertion string. No behavior change; 15 tests (up from 14).
- **C89-RAW-OBS** (PR #224): `IngressClassification.raw_ref` was permanently empty, and `classify_ingress()` never recorded its own classification decision. Now, for every call across all tiers (1-5), `classify_ingress()` saves the raw text to `Tables.DECISION_INBOX` behind a new `FEATURE_RAW_CAPTURE` flag (default off, always returns a non-empty `raw_ref` — a local fallback ref when the flag is off or the write fails) and records an `AgentObservation` (`kind="capture_classification"`) via `ActionGateway`'s existing `record_agent_observation(contract_id=None, ...)` API only — no changes to `ActionGateway`'s core. 14 new tests.
- **BUG-C89-TIER4-PRECEDENCE** (PR #223): `core/ingress_classifier.py`'s `_is_tier4()` (the single Tier-4 gate reused by `router.py` and `lead_candidate_handler.py`) missed several classes of pasted export/status output — CSV/Hebrew table headers without an explicit separator, fixed-width tables, and Airtable status/score readouts (`Status:`, `Score:`, `View in Airtable`, `memory_key`/`@lead`, `owner_dictation`) — which were misclassified as lead dictations and reached the Agent/ActionGateway. Extended the marker set; scoped the bare `"airtable"` marker to require other pasted-content structure so an explicit system-check command still reaches the real `SYSTEM_STATUS` check. 13 new tests.
- **BUG-C89-APPROVAL-IDENTITY** (PR #222): `ActionGateway.propose_action()` was called with `origin_chat_id=identity.memory_key` (e.g. `"boss_hq:eliyahu"`) instead of a real channel external_id. On approval, the dispatch executor re-resolved identity via `resolve_identity(origin_channel, origin_chat_id)`, missed the registry lookup, and silently fell back to `readonly` — so an owner-approved action (e.g. confirming "כן" on a Leads update) was denied by the dispatcher as if a readonly user had requested it. `ActionContract` now preserves the actor identity (role/external_id/tenant/user/domain) resolved at propose time via a new optional `identity=` param; the executor uses it directly instead of re-resolving, with legacy contracts falling back to `resolve_identity()` as before. Also: the C89 lead-write preview now says "מצאתי ליד קיים. לעדכן אותו?" instead of the generic "לשמור?" when the candidate matches an existing lead, and updates to an existing lead always require approval — even when `FEATURE_AUTO_CAPTURE=true`. 37+9+44 tests passing.
- **BUG-SESSIONS-ROOT** (PR #221): `session_store.py`'s Session lookup used to parse `airtable_get()`'s human-formatted string output via regex; any lookup failure (network error, parse miss, unexpected shape) was treated as "not found," letting `_sync_to_db()` unconditionally POST a new Session record even when one already existed — silent duplication on every flaky lookup, not just genuinely empty ones. Added `airtable_get_records()` to `tools/airtable_tools.py` (structured, paginated, raises on HTTP/contract failure) and rewired `session_store.py` to validate records structurally; POST is now only allowed after a confirmed zero-record lookup — any other outcome fails closed. 49 internal + 4 new pytest tests (`test_session_store_contract.py`).
- **BUG-IC-01B** (PR #220): `core/router/intent_router.py`'s `_AMBIGUOUS_PHRASES` (BUG-048/BUG-IC-01) only matched bare ambiguous phrases ("סטטוס", "למלא משימות"); natural-language prefixes ("אני צריך למלא משימות", "צריך סטטוס", "תעזור לי ...") fell through to `Intent.UNKNOWN` → `Handler.AGENT` with full tool access instead of a clarifying question. Added 3 prefix-tolerant patterns reusing the same disambiguation questions. 6 new test cases, 44/44 passing.

- fix: real self-test execution for `test_document_converter.py` via `__main__` guard (BUG-049 / BUG-CI-SILENT-PASS-DOCUMENT-CONVERTER) — CI now actually runs the 6 assertions instead of false-passing. `ci.yml` runs `test_*.py` files as plain scripts (`python "$f"`), not through `pytest`; the pytest-style test functions here (all depending on the `tmp_path` fixture) previously never executed under that invocation, so `python3 test_document_converter.py` silently exited 0 with zero assertions run. Added an explicit `__main__` block calling each of the 6 test functions against a real `tempfile.mkdtemp()` dir (no auto-collect, since `tmp_path` has no plain-Python equivalent); `pytest.importorskip` calls are caught separately as skips. No changes to `ci.yml` or `document_converter/` itself. Verified: 6/6 run for real (`python3 test_document_converter.py`), a deliberate `assert False` break produces a real non-zero exit, and `pytest test_document_converter.py` remains 6/6.

- **C89 Stage 3 Capture Policy + BUG-NEW-12 + BUG-IC-01** (PR #203): (א) `core/ingress_classifier.py` (new) — `IngressClassification` (generic input contract, mirrors `ActionContract` on the output side) + `classify_ingress()` as the single entry point for text-input classification (Tier 1 SIMPLE_CAPTURE / Tier 2 CLEAN_BATCH / Tier 3 MIXED_BATCH / Tier 4 EXPORT-TABLE-LOG — never auto-write / Tier 5 UNKNOWN — falls through). `core/lead_candidate_handler.py` now classifies before parsing. `FEATURE_AUTO_CAPTURE` (default off) gates auto-write; no behavior change in production until explicitly enabled. (ב) BUG-NEW-12: `session_store.py`'s `_find_record_id_in_db` matched a generic `rec\w+` regex that could pick up record IDs embedded in State JSON instead of the Session record itself, missing existing sessions and causing duplicate `airtable_add` calls (14 duplicate rows observed live for one sender) — replaced with `_SESSION_RECORD_RE` scoped to `• [recXXX]` bullet markers; new `_find_best_session_in_db()` always prefers PATCH when any record is found, with explicit `found_count`/`selected`/`action` logs. (ג) BUG-IC-01: bare owner phrases ("סטטוס", "בדיקות מערכת", "מה המצב", "למלא משימות") fell to `Intent.UNKNOWN` → `Handler.AGENT` with full tool access, letting the Agent decide on its own whether to run a live connectivity check — `detect_ambiguous_phrase()` now intercepts these and routes to `Handler.CLARIFY` with a specific disambiguating question instead; explicit action-verb phrases ("בדוק חיבורי מערכת") still route to a real `SYSTEM_STATUS` check.

- **Approval Gateway Safety** (PR #188, C81, BUG-039–044): תיקון סיסטמי ל-6 באגים ב-Section 1 — Pending Approval. (א) Router: `בדיקה`/`test` דורשים anchor מלא — אין עוד collision עם שמות טבלאות. (ב) A32: gate חדש ל-`sheets_append` + gate ל-fake approval state (sentinel `__approval_queued__`). (ג) `ExecutedActionCache`: fingerprint SHA1+TTL-600s מונע re-queue אחרי אישור. (ד) `_mutating_approvals_this_turn`: חוסם שני כלים הדורשים אישור בתור Agent אחד. (ה) `send_recovery.confirmed` handler: P0 — event שנפלט ל-`EventBus` ללא handler ב-`app.py` — תוקן. 25/25 בדיקות חדשות (`test_approval_gateway_safety.py`).

- **C53 Infrastructure** (PR #189, C82, BUG-045–046): (א) `EMERGENCY_STOP_AUTOMATION` נאכף כעת בפועל ב-`run_followup_scan()` ושני scheduler jobs — לפני התיקון הדגל הוצג ב-UI אך לא עצר כלום. (ב) `TOOLS_REQUIRING_APPROVAL` מוגדר כ-`frozenset` יחיד ב-`tool_registry.py`; `event_bus` ו-`dispatcher` מייבאים ממנו — אין עוד שלוש רשימות מנותקות.

- Added Fxx Safe Document Converter: deterministic `convert_document(input_file, input_type, output_type)` API with independent converter modules, fail-closed unsupported/uncertain conversions, Pandoc-first execution where available, Python-library fallbacks for simple Markdown/HTML/TXT/DOCX/CSV/XLSX conversions, usage docs, governance rule, and pytest coverage.

- Branch cleanup: audited 37 unmerged `claude/*` branches against `main` (ancestry, diff content — not just date/name) and deleted 34 (already merged, content-identical to `main`, orphan history, or a past design collision already resolved in favor of a different branch). Two branches contained real unmerged work and were extracted before deletion: N12 (below, PR #108) and the PR #69 docs correction (see C56 in `CHANGE_CONTROL_LOG.md`).
- Added N12 — wired `daily_git_audit.py` into `scheduler.py` behind a new `GIT_AUDIT_SCHEDULER` flag (default off), plus `check_unmerged_vs_roadmap()`, `check_duplicate_schemas()`, `check_recent_commits()`, `check_cors_env_drift()` (PR #108). Extracted from two abandoned branches; fixed a doc-precedence bug found in the original (`BOSS_CURRENT_STATE.md` was checked before `ROADMAP.md`, contradicting `ROADMAP.md`'s own "single source of truth" declaration).
- Recovered `APPROVAL_SYSTEM_AUDIT_AND_C53_SPEC.md` (257-line, code-free architecture audit of the 4 approval/guard mechanisms + C53 test-harness design) from a deleted branch (`claude/spec-c52-implementation-uqmu1g`) directly to `main` (commit `783a680`) — flagged inline as dated 17/06/2026 and not re-verified against current code.
- Fixed BUG-014: `core/anti_hallucination.py`'s NO-TOOL-EVIDENCE gate (`_NO_TOOL_CLAIMS`) never covered Drive — BOSS could claim "אני יכול לחפש ב-Drive" / a file was found with zero `search_drive`/`read_drive_file` calls behind it. Added a 4th gate category (PR #115).
- Docs correction: `BUG_AUDIT_LOG.md` said BUG-011 (PR #110) was "Merged: לא עדיין" — verified via `gh pr view 110` that it's actually merged (`a4c8f27`, ancestor of `main`); corrected, and filled in the previously-placeholder commit hash (PR #116).

- Docs correction: `ROADMAP.md` marked N08 (CI/CD, PR #103), N09 (Monitoring/Alerting, PR #104), and N11 (Finance Pulse `tma_api.py` wiring, PR #77 — an earlier session) as `🔲 PLANNED` despite all three already being merged to `main`. Verified by grepping the live `main` tree (not git log/PR status) before correcting: `.github/workflows/ci.yml` exists and runs on every PR (N08); `core/error_reporter.py` exists and is called from 3 sites in `app.py` (N09); `finance_pulse()` in `tma_api.py` already calls `_build_formula()` with `entity="Payment"` (N11). Also updated `AI_CONTEXT.md` to match. N11's remaining known gap (`PaymentFields.CONTACT`/`NOTES` pointing at nonexistent live `Payments` fields) is in `crm.py`, outside N11's stated scope (`tma_api.py` only) — left open, already tracked below.
- Added `tools/schema_governance.py` (N07 spec, PR #101, `claude/n07-schema-governance`) — standalone, read-only Airtable schema drift detector. Fetches live schema from the Airtable Metadata API and compares it against `airtable_schema.py`'s field-name constants (imported, not parsed) via `schema_audit.py`'s existing `TABLE_CLASS_MAP`/`_class_values` (reused rather than duplicated). Detects: code fields missing from live (whitespace-tolerant match) as ERROR, live-only fields as WARNING, trailing/leading whitespace in field names as WARNING (counted once, not double-reported), trailing/leading whitespace in `singleSelect`/`multipleSelects` choice names as WARNING, and field-type changes as ERROR — the last check compares against the script's own previous `schema_drift_report.json` run as a temporal baseline, since `airtable_schema.py` carries no type metadata to compare against directly. Prints a Hebrew console report, writes `schema_drift_report.json` (added to `.gitignore`, not committed), exits 1 on any ERROR. Zero Airtable writes; does not touch `schema_cache.json` (owned by `schema_validator.py`). Includes a `--self-test` flag using mock schema data, no network calls. Motivated by BUG-008 (`Leads."Business Outcome"` trailing space, found ad-hoc rather than systematically). Companion docs PR #100 (`claude/f16-docs-final`) corrected `ROADMAP.md`/`AI_CONTEXT.md`/`CHANGE_CONTROL_LOG.md` to reflect F16's actual completion state after PR #99 merged.
- F16 Media Layer, Batches א/ב/ג (PR #96, #97, `claude/f16-media-layer`): `voice_stt_adapter.py` (PR #96) — STT now uses OpenAI Whisper as the live primary provider (`OPENAI_API_KEY` already present), with Groq demoted to a commented-out, unwired "Phase 2" stub; error codes corrected to `OVERSIZED`/`STT_FAILED` (no `EMPTY_AUDIO`, not part of the real spec). `drive_adapter.py` (PR #97) — `upload_file(file_bytes, filename, mime_type, parent_folder_id)` now requires `parent_folder_id` explicitly (no default), always cleans up its temp file in a `finally` block, and `_safe_filename` only strips Drive-illegal characters (Hebrew filenames pass through natively). `media_gateway.py` (PR #97) — verified already fully compliant with the spec (table `"Media Files"`, `MediaFileFields`, writes only via `tools/airtable_gateway.airtable_create`, `linked_lead_id` written as a `[record_id]` array, both transcript fields persisted) — no code changes needed. Fixed an identical self-test bug in both `voice_stt_adapter.py` and `drive_adapter.py`: `unittest.mock.patch("module_name.fn", ...)` silently fails to patch the running code when the script is invoked directly (`python3 module_name.py`), because that import path resolves to a second, separate module object distinct from the executing `__main__` — replaced with `patch.object(sys.modules[__name__], "fn", ...)` in both files' self-tests. `test_media_layer.py` updated to match (renamed assertions, added required `parent_folder_id` arg, removed assertions on removed behavior — 33/33 passing).
- F16 Media Layer, Batches ד/ה/ו/ז (PR #98, #99) — **F16 now complete end-to-end.** Batch ד (PR #98, `claude/f16-batch-d`): fixed two real bugs in the pre-existing `media_handler.py` (it had already been implemented in an earlier commit, `ee4d2ed`, predating the batch-correction effort, under different function names than the spec — kept as-is per explicit instruction): `upload_file()` was called with a `domain=` kwarg that doesn't exist on the real `drive_adapter.upload_file()` signature (guaranteed `TypeError` the moment either feature flag turned on); a failed Airtable write after a successful Drive upload was silently reported as `ok=True`. Added `_resolve_drive_folder()`, new error codes `DRIVE_FAILED`/`ASSET_SAVE_FAILED`, Hebrew error messages, and 4 new self-test scenarios covering the previously-untested success path. Batches ה/ו/ז (PR #99, `claude/f16-final`): turned out to already be wired into the live pipeline since the same `ee4d2ed` commit (`app.py`'s `_handle_telegram_media()` already handled voice/photo/document; `tma_api.py`'s `/api/tma/upload` already called `handle_tma_upload()`; `airtable_schema.py` already had `Tables.MEDIA_FILES`/`MediaFileFields`) — only two small gaps were actually missing and got fixed: a `send_chat_action` call before processing in `app.py`, and `linked_lead_id` form-field passthrough in the TMA upload route (`domain` intentionally stays identity-derived, not client-supplied, for tenant-scope safety). 33/33 `test_media_layer.py` passing throughout. `FEATURE_VOICE_NOTES`/`FEATURE_MEDIA_UPLOAD` remain off by default.
- Added `weekly_summary.py` (C22 spec, PR #94, `claude/weekly-business-summary-4crnek`) — read-only weekly digest of Business Memory (last 7 days, grouped by domain), sent via Telegram every Sunday 08:30 (`scheduler.py`'s `_job_weekly_summary`), with a ROADMAP-transfer button for `Event Type=Learning` ("idea") entries. Gated behind `FEATURE_WEEKLY_SUMMARY` (default off, registered in `feature_flags.py`). Fixed two bugs in the original spec against the real Airtable schema: `_DOMAIN_LABELS` referenced a non-existent `recruitment` domain and was missing `media`/`saas` (corrected to the real `real_estate/import/media/saas/finance/general` set from `cmd_update.py`); idea-detection checked for an `Event Type` value `"Idea"` that doesn't exist (corrected to `"Learning"`, the real value `cmd_update.py` writes for "רעיון"). Also fixed `app.py`'s webhook handler: every `callback_query` was being routed unconditionally to `_handle_approval_callback`, so any `callback_data` outside `approve:`/`reject:` silently hit an "unrecognized action" fallback and never reached `@bot.callback_query_handler`-registered handlers — this had already been silently breaking `cmd_update.py`'s `upd_domain:`/`upd_type:` buttons (PR #85/86) without anyone noticing. Non-approval callbacks now route through `bot.process_new_updates([update])`, the same mechanism already used for slash commands.
- Added `scripts/classify_contacts_for_airtable.py` (PR #91/#92, `codex/contact-import-classifier`) — offline, stdlib-only CSV/VCF contact classifier producing review-safe Airtable import artifacts: `contacts_airtable_import_classified.csv` (high-confidence, no review needed), `contacts_review_required.csv`, `contacts_supplier_review.csv`, a matching `.xlsx` (hand-built via `zipfile`/raw OOXML, no external deps) with a Summary sheet, and a `contacts_classification_summary.md`. Classifies `Role Category`/`Specialty` via an extensive Hebrew keyword rule table (broker/expert/client/investor/supplier/partner/operator), with confidence scoring, multi-role/ambiguous-token review flags (e.g. disambiguates unpunctuated `עוד` from `עו"ד`), phone normalization to `+972…` with issue tracking, dedup-by-normalized-phone, deterministic `--batch` hashing, and a `--dry-run` mode. `--owner`/`--referred-by` are required args written into every row (output-only columns — not yet real fields on the live `Contacts` table per `ContactFields` in `airtable_schema.py`). Added `test_classify_contacts_for_airtable.py` (5 tests on the ambiguous-`עוד` disambiguation logic). **Note:** an earlier, simpler version of this same script path (parser reusing `contact_merge.py`, no review/confidence/XLSX logic) was built independently on `claude/claude-md-docs-u8kbsc` before this PR merged — discarded in favor of this version once the collision was found (same file path, two independent implementations); the `claude/*` branch was synced to take this version as-is. Verified against a real 1345-contact `.vcf` export in `/tmp` — no contact PII committed. `output/`, `contacts_merged.csv`, `*.vcf` added to `.gitignore` to prevent future accidental commits of contact exports/derived files.
- Added `contact_merge.py` — standalone offline CLI merging Google Contacts CSV exports and vCard (`.vcf`) files into one Airtable-`Contacts`-ready CSV, deduping by normalized Israeli phone number (PR #88). Admin/import utility — no dispatcher/identity wiring, writes a local CSV for manual import. Fixed a real bug found during review: vCard line-folding (RFC 2425 continuation lines) was dead code (a no-op `pass`), silently truncating wrapped field values — replaced with a proper unfold pass.
- F13 (`core/tenant_config.py` + `providers/{__init__,interfaces,airtable_shim,anthropic_shim,twilio_shim}.py`) — code written and merged (PR #87), **not wired into the live pipeline** (new files only, zero existing import touched). Registered spec-only in `ROADMAP.md` first; the original spec assumed function signatures that don't exist in this codebase (`gateway_add`/`gateway_update`/`gateway_delete`, `send_via_cog`, `airtable_get(max_records=...)` returning `list[dict]`, `_validate_twilio_signature(headers, body)`) — corrected against the real signatures (`airtable_create`/`airtable_patch` + `_resolve_table()`, `send_outbound(OutboundEnvelope)`), logged as `BUG_AUDIT_LOG.md` SPEC-001. F12/F13 overlap on a `providers/` directory is still undecided — do not wire either into the pipeline before that decision.
- Added `inbound_handler.py` (F06) — unified inbound lead gate sitting in front of `lead_capture.py`: dedups by `external_id` (exact, e.g. `gmail:<msg_id>`), updates existing leads by `sender_id`, otherwise creates a new email lead. Added `LeadFields.EXTERNAL_ID`/`SENDER_ID` to `airtable_schema.py`. Rewired `email_inbound.run_email_poll()` to route through the new gate instead of building/approving drafts directly; added `_alias_to_domain()` (To-address → business domain) and `InboundEmail.to` (the Gmail `To` header wasn't previously fetched — without it, domain routing would have always fallen through to `general`). `scheduler.py` already had `_job_email_inbound` wired to `run_email_poll()` — no scheduler change needed. Added `test_inbound_handler.py`. **Manual step required before enabling `EMAIL_INBOUND`:** add `external_id` and `sender_id` (Single line text) to the Airtable `Leads` table, then refresh `schema_cache.json` (`schema_audit.py`) — confirmed via a local test run that `airtable_gateway` currently drops both fields as "unknown field ... not in schema_cache". **Update:** both fields have now been added to the live `Leads` table (verified via Airtable MCP: `external_id` = `fldNAXOruq53qxYKH`, `sender_id` = `fldyEHy5Udasl3G02`); `schema_cache.json` updated to match (targeted addition, not a full `schema_audit.py` resync — no live Airtable credentials in this sandbox to run that script).
- Added `docs/operations/DEPLOYMENT.md` — deploy guide for Render + Vercel.
- Added `docs/operations/RUNBOOK.md` — incident response for 6 common failure scenarios.
- Updated `README.md` — added WhatsApp channel, Twilio env vars, operations docs to documentation map.
- Added Screen Filter Gateway (`SCREEN_CONFIGS` + `_build_formula()`) to `tma_api.py` — `GET /api/leads` now supports `?view=active|monitoring|all`; `_get_project_cards()` / `get_project_dashboard()` wired to the same lead-filtering config (PR #75).
- O4 Finance Pulse + Payments/Expenses migration (`airtable_schema.py`, `tma_api.py`): `Tables.PAYMENTS`/`EXPENSES` and `PaymentFields`/`ExpenseFields`/`PaymentStatus` now use the English field names live in Airtable (manual migration done ahead of this change — verified against the live base via Airtable MCP). `finance_pulse()` rewired onto `SCREEN_CONFIGS["finance_pulse"]` + `_build_formula()` (matching the existing `/api/leads` pattern) — now supports `?view=active|overdue|all` and returns `available_views` in the response. Updated `smoke_tests.py`'s stale Hebrew-field assertions for `Tables.PAYMENTS`/`PaymentFields.STATUS`/`DATE` to match. **Update:** user manually renamed `Payments.Domain`/`Expenses.Domain` → lowercase `domain` and `Expenses.Status` → lowercase `status` directly in live Airtable; re-verified via Airtable MCP and updated `ExpenseFields.STATUS` in `airtable_schema.py` to match (was hardcoded to capital-S `"Status"` per the original spec note, now stale) — this resolves the `_build_formula()` domain case-mismatch previously flagged below. **Known gaps, not fixed in this change (out of spec scope):** `PaymentFields.CONTACT`/`NOTES` (`contact_id`/`notes`) point to fields that don't exist on the live `Payments` table — `crm.py`'s `create_payment()` will fail with an Airtable "unknown field" error if ever called with `contact_id` or `notes` args.

## 2026-06-12

- Added root `README.md` with runtime, environment, and documentation map.
- Added root `CHANGELOG.md`.
- Moved Airtable schema migration notes from `docs/governance/` to `docs/schemas/`.
