# BOSS Production Runtime Map

Status: historical planning snapshot — not current state. Date: 16/07/2026.

> **Current-state pointer:** use `docs/architecture/CURRENT_SYSTEM_EXECUTION_MAP.md`, `ROADMAP.md`, and `AI_CONTEXT.md` for current architecture and rollout truth. This archive is retained for historical context and must not be used to infer present runtime configuration.

> **Historical banner:** the rows below preserve a point-in-time planning view. They are not runtime evidence and are superseded where they conflict with the current execution map or canonical ledgers.

## Reading rules

- **Active** means the freshest supplied project context explicitly identifies the path as production-working, or the path is unconditional and is part of that identified production flow.
- **Off** means the code default is off and there is no supplied evidence that the production environment overrides it.
- **Dormant** means implementation exists but its activation flags are off or production activation is unverified.
- **Broken** means the repository itself structurally blocks activation or documents a missing execution path.
- Environment values in Render are **not observable in this read-only audit**. BOSS reads ordinary flags from an environment variable with the same name unless a runtime override exists (`feature_flags.py:206-225`). `FEATURE_INGRESS_ENVELOPE` defaults on (`feature_flags.py:156-161`); other ordinary flags default off. The former `IMPORT_DOMAIN` entry was removed as a registry-only dead flag.
- Documentation freshness is a known limitation: `AI_CONTEXT.md` says the roadmap/change logs lag roughly 15 merged PRs and none of that newer work was then production-verified (`AI_CONTEXT.md:8-10`). “Last proof” below therefore distinguishes code/default evidence from production evidence.

## Runtime map

| Feature | Flag | Env | Code path | Active/Off/Dormant/Broken | Last proof | Risk | Next action |
|---|---|---|---|---|---|---|---|
| Telegram inbound agent | None for base route | `TELEGRAM_TOKEN`, `ANTHROPIC_API_KEY`, Airtable credentials | `app.py:3124`; `run_agent()` and role/tool context | **Active** | Freshest context says Telegram is live on `main` through Identity → Router → Context → Agent (`AI_CONTEXT.md:14-17`) | Base path is safety-critical and has no single runtime-map owner | Add a read-only startup/runtime status record; do not add a feature flag to the base path |
| Twilio WhatsApp inbound | None for base route; emergency/output controls are separate | Twilio credentials, `ANTHROPIC_API_KEY`, Airtable credentials | `app.py:3343`; identity + `run_agent()` | **Active** | Freshest context says WhatsApp/Twilio inbound with identity resolution is production-working (`AI_CONTEXT.md:27-30`) | Inbound, capture, follow-up, and outbound statuses can be conflated | Report each capability separately in health/doctor output |
| Meta WhatsApp path | `META_OUTBOUND_ENABLED` | `META_OUTBOUND_ENABLED`; Meta webhook credentials | `app.py:3512`; flag gate at `app.py:3634-3637` | **Off / blocked for real outbound** | Flag registry says default false and current outbound is a stub (`feature_flags.py:34-45`); current context says real WhatsApp outbound is blocked on Meta Cloud API (`AI_CONTEXT.md:37-40`) | A misleading flag name currently gates whether inbound reaches the agent while describing outbound enablement | Keep off; later split readiness from outbound permission in a dedicated PR, preserving behavior |
| Airtable Gateway single write path | No feature flag for base gateway | Airtable credentials and schema configuration | `tools/airtable_gateway.py`; agent/TMA/business callers | **Active** | Freshest context calls it the production single, atomic fail-closed write path (`AI_CONTEXT.md:27-30`) | Every business mutation depends on schema/evidence correctness | Keep as Source of Authority; expose readiness in the runtime map/doctor |
| C94 Ingress Envelope | `FEATURE_INGRESS_ENVELOPE` | Same name; default `true` | `app.py:2149-2160` | **Active by default; production reported active** | Registry deliberately defaults it on (`feature_flags.py:122-129`, `feature_flags.py:156-161`); current context lists C94 as production-working (`AI_CONTEXT.md:27-30`) | `normalized_text` is discarded and evidence trace is not durable | Do not expand scope; track those two gaps separately (`AI_CONTEXT.md:34-35`) |
| Lead capture | `LEAD_CAPTURE` | `LEAD_CAPTURE` | `lead_capture.py:188-189`; WhatsApp lead path | **Environment unknown; code default Off** | Flag registry defines capture and standard default-off behavior (`feature_flags.py:18-25`, `feature_flags.py:206-225`); current context reports a WhatsApp lead-extraction pipeline but does not expose Render env (`AI_CONTEXT.md:27-30`) | Documentation can call the pipeline live while the mutating capture flag is actually off | Production owner must record the real env value and one recent record/receipt before marking Active |
| Lead scoring | `LEAD_SCORING` | `LEAD_SCORING` | `lead_capture.py:285-310` | **Dormant** | Freshest context groups Lead Scoring among code-done, flag-off features (`AI_CONTEXT.md:31-34`) | Scores may be assumed available by follow-up/reporting when they are not | Keep off until runtime map has schema readiness and canary evidence |
| Lead memory | `LEAD_MEMORY` | `LEAD_MEMORY` (ignore stale source comments naming `FEATURE_LEAD_MEMORY`) | `lead_memory.py:216`; `lead_capture.py:272-310`; scheduler flush | **Dormant** | Freshest context groups Lead Memory among code-done, flag-off features (`AI_CONTEXT.md:31-34`) | Naming drift between comments and actual `is_enabled("LEAD_MEMORY")` can cause a false activation belief | Standardize docs/env naming before activation; no logic change in that PR |
| Follow-up automation | `FOLLOWUP_AUTOMATION` | `FOLLOWUP_AUTOMATION` (ignore stale source comments naming `FEATURE_FOLLOWUP_AUTOMATION`) | `scheduler.py:114-121`; `followup_engine.py:239` | **Dormant** | Freshest context groups Follow-up among code-done, flag-off features (`AI_CONTEXT.md:31-34`) | A wrong env name silently leaves the job off; delivery truth remains an open priority | Fix documentation/config naming first; then require delivery evidence and emergency gate proof before canary |
| Legacy approval flow | Existing approval path; not solely controlled by Phase 4B flags | Airtable/Telegram configuration | approval callbacks and current Airtable `Approvals` path | **Active** | Freshest context says Airtable `Approvals` remains the real production path (`AI_CONTEXT.md:27-30`) | Multiple approval mechanisms remain, increasing bypass and state-divergence risk | Preserve behavior while finishing F52 design; no big-bang replacement |
| Action Gateway behavior | `FEATURE_ACTION_GATEWAY` | `FEATURE_ACTION_GATEWAY`; default off | `core/action_gateway.py`; many branches in `app.py` | **Historical snapshot — superseded** | Current execution map distinguishes unconditional prefetch/dedup/proposal callers from the flag-gated general-agent enforcement path | This historical row must not be used to call the whole Gateway dormant | Use current execution map and TR-01 ledger |
| Durable ActionContract persistence | `FEATURE_ACTION_CONTRACT_PERSISTENCE` | Same name; default off | repository construction at `core/action_gateway.py:1861-1871`; TMA paired gate | **Dormant** | Current context says code-ready but not activated (`AI_CONTEXT.md:16-18`, `AI_CONTEXT.md:31-33`) | Operators may mistake merged persistence code for canonical production authority | Decide staged rollout model and record production readiness gates before enabling (`AI_CONTEXT.md:58-63`) |
| PostgreSQL atomic execution claims | `FEATURE_ATOMIC_CLAIMS` | Same name; default off; PostgreSQL connection env | startup health `app.py:133-144`; execution gate `core/action_gateway.py:1331-1421` | **Dormant** | Current context says code-ready, flags off, not production-activated (`AI_CONTEXT.md:16-18`, `AI_CONTEXT.md:31-33`) | Without activation, legacy concurrency/authority behavior remains; unsafe activation could block all mutations | Separate readiness, shadow evidence, canary, and activation PRs; never silently fall back (`core/action_gateway.py:1331-1339`) |
| Decision Hub | `FEATURE_DECISION_HUB` | Same name; default off | command registration `cmd_decision.py:37-38`; app paths `app.py:2828`, `app.py:3257` | **Dormant / activation blocked** | Current context says code done, flag off, and activation waits for production evidence (`AI_CONTEXT.md:31-39`) | Product/docs may imply a live decision layer when no production evidence exists | Keep off; define explicit activation evidence outside this reliability hardening series |
| Decision auto-ingestion | `FEATURE_DECISION_AUTO_INGESTION` | Same name; default off | Decision Inbox routing | **Off** | Registry defines default off (`feature_flags.py:76-80`) | Automatic routing could alter channel precedence and duplicate ingestion | Keep off; any future work must honor Input Precedence (`docs/governance/MODULE_RULES.md:73-81`) |
| Media upload | `FEATURE_MEDIA_UPLOAD` | Same name; default off; Drive/STT/provider credentials as applicable | Telegram gate `app.py:2948`; TMA gate `tma_api.py:3553`; `media_handler.py` | **Off / dormant** | Registry says default off (`feature_flags.py:41-45`); current context does not list it as production-active | Upload can create Drive/Airtable side effects and cross approval boundaries | Keep off until readiness, role, evidence, and cleanup contracts are proven |
| Voice notes | `FEATURE_VOICE_NOTES` | Same name; default off; transcription credentials | `app.py:2905`; `media_handler.py` | **Off / dormant** | Registry says default off (`feature_flags.py:41-44`) | Transcription can be mistaken for durable Business Memory or an approved action | Keep off; preserve separate transcription, review, approval, and write states |
| Email inbound automation | `EMAIL_INBOUND` | `EMAIL_INBOUND`; email credentials | scheduler job `scheduler.py:356-384` | **Broken/structurally blocked** | Flag registry says the approved action has no adapter/dispatcher path and hard-blocks activation (`feature_flags.py:191-223`) | Enabling via env creates a false belief while the runtime refuses it | Keep blocked; either implement the full adapter in a future scoped project or retire the flag |
| Abandoned-lead automation | `ABANDONED_LEADS` | `ABANDONED_LEADS` | scheduler job `scheduler.py:402-426` | **Broken/structurally blocked** | Flag registry says `send_bounce` has no execution path and hard-blocks activation (`feature_flags.py:191-223`) | Scheduler activity can appear healthy while the business action can never execute | Keep blocked; resolve ownership/product decision before implementation |
| Scheduler process and fixed jobs | Per-job flags plus `EMERGENCY_STOP_AUTOMATION` | schedule-time envs at `scheduler.py:803-819` | scheduler loop and registration `scheduler.py:760-862` | **Active process; mixed job states** | Current code registers all jobs and each job/gate decides whether work occurs (`scheduler.py:760-862`); current context says delivery/emergency follow-ups remain open (`AI_CONTEXT.md:58-64`) | “Scheduler running” does not mean a job is enabled, safe, or successfully delivered | Add per-job runtime status: registered, flag state, last start, last outcome, delivery evidence |
| Scheduler emergency stop | `EMERGENCY_STOP_AUTOMATION` | Same name or persistent runtime state | `_automation_guard()` `scheduler.py:770-789`; all registrations `scheduler.py:821-847` | **Active — updated 23/07/2026** (was "Implemented; production proof stale/unknown") | `AI_CONTEXT.md` (23/07/2026): "Emergency Stop (PATCH 3B) הושלם ואומת בפרודקשן ישירות ע"י הבעלים" — 5 durable `EMERGENCY_STOP_*` flags in Airtable, survive real restart, full TMA Stop/Clear UI including Stop All. This is `AI_CONTEXT.md`'s own claim, not independently re-verified against live Airtable/logs in this pass — treat as one level more current than this table's original 16/07 snapshot, not as fresh runtime evidence | Docs/code previously disagreed on this row; now aligned with the newer source | Still worth the structural matrix test (every registered job actually wrapped) this row originally called for — not done here either |
| Runtime schema provider | `FEATURE_AIRTABLE_RUNTIME_SCHEMA_PROVIDER_STATE` | `off` / `shadow` / `enforce`; default `off` | `core/runtime_schema_provider.py`; gateway consumers | **Off** | Accessor defaults unknown/unset values to off (`feature_flags.py:228-239`) | Schema drift can remain latent; premature enforce could block valid writes | Run shadow-only later with discrepancy reporting; enforcement requires a separate approval |
| Select-value validation | `FEATURE_AIRTABLE_SELECT_VALUE_VALIDATION_STATE` | `off` / `shadow` / `enforce`; default `off` | `tools/airtable_gateway.py`; runtime schema dependency | **Off** | Accessor defaults unknown/unset values to off (`feature_flags.py:242-250`) | Invalid selects can fail providers; premature enforcement can drop whole fields | Shadow after schema provider is reliable; enforce only with field-level evidence |
| Financial commitment output gate | `FINANCIAL_COMMITMENT_GATE` | Same name; default false | Customer Output Gateway / financial gate | **Shadow** | Registry defines false as logging-only shadow and true as escalation (`feature_flags.py:81-84`) | Operators may assume commitments are blocked when they are only logged/escalated | Keep shadow until false-positive review is recorded; activation is a separate policy decision |
| Model selection | No rollout flag | Anthropic credentials; model names are code constants | `_select_model()` `context.py:159-171` | **Active, provider-specific** | Current code selects Sonnet/Haiku by role and owner prefix (`context.py:163-171`) | Scheduler and interactive runtime do not have a general provider identity/pinning contract | Add metadata/pinning contracts only; do not introduce a new provider runtime in this hardening scope |

## Required production evidence fields

Any future version of this map should record, per row: observed deployment revision, observed env/flag value, last successful invocation time, evidence/receipt ID, last known failure, and verifier identity. A code default or merged PR is not production proof; the current project context explicitly separates code-ready work from production verification (`AI_CONTEXT.md:16-23`, `AI_CONTEXT.md:45-54`).
