# AI CONTEXT

> קרא אותי לפני כל דבר אחר. זהו מסמך תדרוך (briefing), לא תיעוד מלא.
> למקור אמת מלא: `ROADMAP.md` (מתוכנן), `BUG_AUDIT_LOG.md`, `CHANGE_CONTROL_LOG.md`.
> `CANONICAL_STATE.md` **לא קיים** בריפו — אין מקור בשם הזה, לא CRITICAL.
> `BOSS_CURRENT_STATE.md` ארכיון היסטורי (עודכן לאחרונה 26/06/2026, `main` head שם `d249147`)
> — **לא** מקור אמת נוכחי, מפגר בעשרות PRs; `main` + `ROADMAP.md` גוברים עליו בכל סתירה.
> `ROADMAP.md`, `CHANGE_CONTROL_LOG.md`, `CHANGELOG.md` ו-`BUG_AUDIT_LOG.md` סונכרנו כולם עד `main`
> `587d1fe` (PR #396) בעדכון ההוא. הערת גבולות שנשארת: `CHANGELOG.md` עדיין חסר itemization נפרד
> ל-#348–#353 (PA-01), ו-`CHANGE_CONTROL_LOG.md` עדיין חסר רשומות #327–#353 אחרי C111 — פערים
> היסטוריים ישנים, מסומנים במפורש, לא backfilled בסבב הזה.
> **פער נוסף, נפתח מאז ולא נסגר כאן:** §2-4 למטה (State/Completed/Priorities) לא רוענו מעבר ל-PR
> #396 — main התקדם עשרות PRs מאז (כולל #414-419/#421 של `claude/n15-owner-decision-p73c3k`,
> #420 BUG-121/122/123, #422 BUG-124). `BUG_AUDIT_LOG.md`/`CHANGE_CONTROL_LOG.md`/`CHANGELOG.md`
> עצמם **כן** מעודכנים (ראו הכותרת החדשה למטה) — רק §2-4 של המסמך הזה ספציפית לא, backfill מלא
> מחוץ לסקופ הסבב הנוכחי; לפרטי #420/#422 ראו הכותרת למטה + `CHANGE_CONTROL_LOG.md` C152-C154.

> **כלל תהליך (חדש, 19/07/2026):** **Runtime evidence > main code > docs > memory.**
> אין להסיק מצב flag פרוס/runtime מ-ברירת המחדל ב-`feature_flags.py` בלבד — ברירת מחדל בקוד
> אומרת מה קורה **בהיעדר** override, לא מה בפועל רץ ב-Render. אם לוגי production מציגים
> `[UnifiedStatusFormatterShadow]` או `[EvidenceFinalizerShadow]`, יש לסווג את ה-runtime כ-
> **shadow-observed**, גם אם ברירת המחדל בקוד היא `off`. אם התיעוד סותר לוגי production —
> **התיעוד** מסומן stale, **לא** מורידים את סטטוס ה-runtime.

**עודכן:** 21/07/2026 (PATCH 3B הושלם במלואו — backend + frontend, שניהם Production Verified) · **main:** `8ac1091` (מיזוג PR #436 — TMA frontend; `main` המשיך מאז עם PRs #437/#438 לא-קשורים) · **סטטוס:** PATCH 3B — Emergency Stop durable persistence — **הושלם ו-✅ Production Verified** (backend וגם frontend, ע"י הבעלים ישירות, לא רק shadow/tests). §2-4 למטה **לא רועננו** מעבר ל-PR #396 (חוב תיעודי ישן ונפרד, ראה הכותרת העליונה) — עדכון זה מוסיף רק בלוק "עדכון סבב זה" חדש, לא backfill מלא.

**עדכון סבב זה (21/07/2026, PATCH 3B Steps 5/5.1/6 + prerequisite + TMA frontend):** ✅ **5 דגלי `EMERGENCY_STOP_*` עברו ממנגנון in-process/`/tmp` (שנמחק בפועל אחרי restart) לדביק במלואו (Airtable, קריאה+כתיבה) — הבעלים אימת ישירות בפרודקשן, לא רק בבדיקות.** `is_enabled()`/`set_flag()` מיירטים את 5 השמות (delegate ל-`evaluate_emergency_stop()` / חסימת `set_flag()` עם `EmergencyStopLegacyWriteBlocked`); `app.py` בונה ומזריק `EmergencyStopManager` דרך `run_startup_sequence()` מפורש (לא side-effect של import) לפני שה-scheduler מותחל; `tma_api.py` חושף `GET /api/health` עם `{enabled, operation_id}` לכל דגל ו-`POST /api/health/emergency`(`/clear`) לכתיבה דביקה עם optimistic concurrency (`operation_id`, 409 על stale). **הוכחת production ישירה מהבעלים:** `stop_email` דרך ה-TMA → HTTP 200 → **נשאר פעיל אחרי restart אמיתי של Render** (ההפך המדויק מהמנגנון הישן) → `clear` עם `operation_id` נכון → הצליח, TMA חזר לבריא; לוג bootstrap אישר סדר `hydration→5 flags→scheduler`. תוך כדי אימות Step 5 נמצא ותוקן גם באג test-isolation אמיתי (CI קרא בשקט מטבלת Airtable **החיה** בפרודקשן) — unit CI כבר לא מחזיק credentials אמיתיים בכלל (PR #432). **TMA frontend (Stop AI + כפתורי Clear + טיפול 409, PR #436) ✅ ממוזג ו-✅ Production Verified** — הבעלים אישר במפורש (21/07/2026) שכל 5 כפתורי ה-Stop וה-Clear המתאימים עובדים בפרודקשן החי, **כולל Stop All**, עם תמונות מסך שהראו את מסלול `stop_email`→"חירום פעיל"→`clear`→"כל המערכות תקינות". **גבול-דיוק, לא ממצא:** תרחיש ה-409 עם `operation_id` שגוי אומת רק ב-harness מקומי (לא בפרודקשן החי עצמו), ו"Stop All חוסם פעולה guarded אמיתית" אומת כ-round-trip UI, לא כניסיון guarded-action מפורש באותו חלון-זמן. ראה `ROADMAP.md`'s 21/07/2026 entry ו-`CHANGE_CONTROL_LOG.md` C159–C162 לפירוט מלא.

**עדכון קודם (20/07/2026, אחרי #420-#422):** ✅ **מוזג ל-main:** PR #420 (branch `claude/bug121-pending-approval-queue-ux`) — BUG-121 (`/status` crash), BUG-122 (pending-approval queue מדכא פעולה חדשה), BUG-123 (placeholder שבור + מזהים גולמיים בהודעת אישור); PR #421 (branch `claude/n15-owner-decision-p73c3k`); PR #422 (branch `claude/bug125-context-pronoun-table-false-positive`) — BUG-124 (מילת-הצבעה "זה" חוסמת Tier-4 כוזבת) + תיקון-המשך אותו יום שסגר פער נוסף (`_AIRTABLE_ID_RE`, לא רק pipes) שהתיקון הראשוני השאיר פתוח. **כל השלושה (BUG-121/122/123/124) קוד תוקן+נבדק (full test sweep ירוק), טרם נבדק בפרודקשן/staging בפועל** — ממתין לבדיקת המשתמש בטלגרם/WhatsApp חי אחרי deploy. פרטים מלאים: `BUG_AUDIT_LOG.md` (BUG-121 עד BUG-124) + `CHANGE_CONTROL_LOG.md` (C152-C154). **C84 (TMA Approval TTL):** רשומת בדיקה קודמת נפסלה (המשתמש ערך ידנית את ActionContracts לצורך בדיקת BUG-122/123 באותו יום) — המשתמש יצר רשומת test חדשה ונקייה, ✅/❌ עדיין פתוח, ממתין לבדיקה שלו בעוד 24 שעות ממועד היצירה.

**ענף פעיל פתוח (לא ממוזג):** `claude/c81-c82-roadmap-docs-cleanup` — ניקוי docs-only ל-C81-FU/C82-FU (שניהם נסגרו כפתורים-מראש, ראו `CHANGE_CONTROL_LOG.md` C144) + תיקון CI-silent-pass ב-`test_c81_recovery_truth.py`. **אין שינוי קוד production.** **ענף פעיל פתוח נוסף, staging-only, לא ממוזג בכוונה (PR #407):** `claude/rp5-staging-fault-injection-v4akit` — RP5 staging-only fault-injection helper, ✅ **VERIFIED ב-staging** (smoke test 19/07/2026 — ראו `CHANGE_CONTROL_LOG.md` C144/C145). **הענף עצמו (הקוד) לא ממוזג ולא יתמזג** — רק הסיכום התיעודי כאן ו-`BUG_AUDIT_LOG.md`/`CHANGE_CONTROL_LOG.md` הועתקו ל-`main` (עדכון הזה) לפני שהענף נמחק, כדי שהראיה לא תאבד. **עודכן כעת (20/07/2026):** הענף רוּבֵּייז מחדש על `main` `5262327` (אחרי #422) כדי לבדוק fault-injection מול הקוד החי העדכני ביותר; התרחיש הבא המוצע לבדיקה: `write-timeout`.

---

## 1. Executive Summary

- הבוט חי בפרודקשן (Telegram + WhatsApp/Twilio), Identity→Router→Context→Agent, Airtable כ-CRM יחיד. אין שינוי במסלול הזה.
- **BUG-111 (lead batch parsing) — ✅ VERIFIED IN PROD.** סגור בשני סבבים: PR #386 (טלפונים עם מפרידים, domain-hint, batch clarification) + PR #390 round-2 (paste קומפקטי בלי newline לפני header של batch/chat-export עדיין הפיק שם-ליד מזויף; תוקן + safety-net חדש: candidate יחיד מבוטל-אוטומטית ל-clarification אם יש יותר מטלפון אחד בטקסט הגולמי). **הוכחת production:** paste קומפקטי/WhatsApp עם 3 טלפונים (`0533968395`/`0533123482`/`0534185481`) כבר לא יצר ליד מזויף בשם "לידים חדשים" — BOSS זיהה את שלושת המספרים ושאל לשמות במקום ליצור Lead שגוי.
- **BUG-112 (Telegram approval TTL) — מנגנון הליבה ✅ VERIFIED IN PROD.** PR #387: `_handle_approval_callback_impl()` אוכף TTL של 10 דקות לפני dispatch. **הוכחת production:** לחיצה על כפתור שפג תוקף הציגה `"⏰ פג תוקף — הפעולה לא בוצעה"`, הפעולה לא בוצעה, וכפתור האישור נעלם (edited-message). **PR #394 (נפרד, ראה למטה)** הוא defensive/idempotency cleanup לנתיב **אחר** (callback חסר/stale/כבר-נצרך) — merged, tests green, **לא** נבדק production בנפרד עדיין (הדגימה שהובילה אליו נצפתה *לפני* המיזוג; דגימת ה-expiry האחרונה שהוכיחה את BUG-112 גם מסירה את מסלול הלחיצה-הכפולה הרגיל כי הכפתור נעלם אחרי הלחיצה הראשונה).
- **F52 (Message Contract Foundation) — קוד flag-gated `off`, אך runtime shadow observed בלוגי production.** עכשיו 6+1 PRs (#381–#385, #389, #392, #393): שכבת ה-shadow logging של `ActionGateway` מכסה executed/status-query, rejection/cancellation (PR5, #389), ו-approval_pending prompt (PR6, #392) — כולל תיקון עוקב (PR6-FU, #393). **הוכחת production (לוגים אמיתיים, לא רק קוד):** `[UnifiedStatusFormatterShadow] outcome=executed mapped_state=success ...`, `outcome=rejected mapped_state=failure ...`, `outcome=pending mapped_state=approval_pending ...` — שלושתם נצפו. `FEATURE_UNIFIED_STATUS_FORMATTER` ברירת המחדל בקוד היא `off`, אך זו ברירת-מחדל-בהיעדר-override, לא הוכחה ש-shadow לא רץ — הלוגים מוכיחים שהוא כן רץ. **לא** הופעל `on` — שומרים shadow.
- **PR #392 חשף וסגר 3 פערים אמיתיים**: `_queue_approval_detailed_impl()` שלח טקסט hardcoded ישירות (בלי לעבור דרך ActionGateway — shadow לא ראה את זה בכלל); `_classify_response_claim()` קרא את דיכוי-הטקסט התקין של A32 (Single-Speaker gate) כ-false mismatch; `build_ownership_signal()` לא סימן `reply_owner="gateway"` כשה-agent דוכא. שלושתם תוקנו ללא שינוי בהתנהגות הבפועל (A32 suppression עצמו לא שונה).
- **PR #393 (F52 PR6 follow-up, mixed taxonomy) — ✅ VERIFIED IN PROD (19/07/2026, exact branch confirmed).** הרחיב את `compare_shadow_final_status()` כך ש-`"sent_for_approval"` תואם גם `evidence_status="mixed"` כשה-non-success היחיד הוא `approvals_pending`. **הענף המדויק שהיה חסר עד עכשיו — נלכד:** `[EvidenceFinalizerShadow] state=shadow evidence_status=mixed response_claim=sent_for_approval mismatch=false code=match counts={'classification': 'mixed', 'verified_reads': 1, 'verified_writes': 0, 'failed_calls': 0, 'outcome_unknown': 0, 'approvals_pending': 1, 'unverified_effects': 0}` — turn שביצע `airtable_get` מאומת (בדיקת סטטוס משימה) ואז העלה `airtable_update` requires_approval באותו turn; `response_claim=sent_for_approval` בדיוק (לא `mixed`), `mismatch=false`. הגיע יחד עם דגימת ה-adjacent שכבר הייתה מוכרת (`response_claim=mixed`). שני הענפים כעת מאומתים.
- **PR #396 (BUG-113, A32 duplicate approval-pending prose) — ✅ VERIFIED IN PROD, closed.** ראה §3 למטה לפירוט מלא + evidence.
- **BUG-114 (ActionContracts context-interrupt call amplification, PR #402) — ✅ VERIFIED IN PROD, closed.** `ExecutionLedger.mark_context_interrupted()` סימן מחדש כל contract pending גם כשכבר `context_interrupted=True`, גורם ל-GET+PATCH+GET מיותר על כל הודעה בלתי-קשורה. תוקן עם `and (c.reconfirmation_required or not c.context_interrupted)` (לא ה-filter הנאיבי, שנתפס כשובר FSM האישור-החוזר BUG-108 תוך כדי כתיבת בדיקות). דיווח production מפורש: אין יותר burst חוזר. ראה §4 item 8.
- **BUG-115 (confirmation-word routing hijacked ע"י contracts ישנים) — ✅ VERIFIED IN PROD, closed.** אותה דגימת production שחשפה BUG-114 חשפה גם ש-"כן"/"מאשר" אחרי תצוגת "📋 זיהיתי ליד..." (contract Tier-1 אמיתי) נחטף ל-disambiguation גנרי של contracts ישנים ולא-קשורים, כי `route_confirmation_word()` סופר contracts חיים בלבד בלי לזהות לאיזה contract ה-"כן" מתייחס. תוקן עם bookmark "contract שהוצג לאחרונה" ב-`session_store.py` (TTL 600s) + תוויות אנושיות ל-disambiguation (`_describe_contract_for_disambiguation()`, פונקציה נפרדת). 22 בדיקות חדשות, full sweep ירוק. **הוכחת production (19/07/2026):** `live_contracts=10` בו-זמנית, "כן" נפתר ישירות מול ה-contract הנכון ללא disambiguation. ראה §4 item 9.
- **BUG-116 (Tier-4 `_AIRTABLE_ID_RE` תופס מילים אנגליות רגילות כ-Airtable ID) — ✅ VERIFIED IN PROD, closed.** הודעת ליד תקינה לחלוטין ("צור ליד חדש לדומיין recruitment... יהודה גרוס 0533968395") סווגה כ-`tier=4 reason=airtable_id` וחסמה כל פעולה — כי `_AIRTABLE_ID_RE` הישן תפס כל מילה שמתחילה ב-`rec`/`fld` ומלווה ב-8+ אותיות, כולל מילים אנגליות רגילות (`recruitment` = `rec`+`ruitment`). תוקן עם דרישת ספרה בתוך הרצף התואם (lookahead) — כל ה-fixtures האמיתיים בסוויטה נשארים תואמים כי הם מכילים ספרות; מילה אנגלית לעולם לא. 15 בדיקות חדשות, full sweep (138 קבצים) ירוק. נושא נפרד לגמרי מ-BUG-114/115 — Tier-4 ingress-classification, לא ActionGateway. **הוכחת production (19/07/2026, PR #404 `main` `0ef018f`):** ניסוח שונה ("domain recruitment" באנגלית, בלי `לדומיין`/מקף) — `📋 זיהיתי ליד: יונתן כהן (0534820022)` נכון, "כן" → `✅ בוצע: יצירת ליד... מזהה: recNhWVHDd9Noeql1`. **הערה (עודכנה):** אותה דגימה הייתה עקבית עם BUG-115 עובד, אך לא כללה ראייה של `live_contracts>1` באותו רגע. דגימה נפרדת מאוחרת יותר (`live_contracts=10`) כן סיפקה את הראייה החסרה — BUG-115 עכשיו ✅ VERIFIED IN PROD בנפרד (ראה §1/§4 item 9). ראה §4 item 10 ו-`docs/architecture/ingress-classifier/BUG-116_AIRTABLE_ID_REGEX_WORD_FALSE_POSITIVE.md`.
- **BUG-117 (Tier-2 batch lead-preview נחטף לאותה disambiguation שBUG-115 תיקן עבור Tier-1) — ✅ VERIFIED IN PROD, closed.** batch preview ("📋 זיהיתי 2 לידים אפשריים בקבוצה... כן לשמירת כולם") נחטף ל-9 contracts ישנים ולא-קשורים במקום לאשר את הbatch — כי `app.py`'s `_CONFIRM_WORDS` בדק Tier-1 (`find_live_contracts()`) תמיד ראשון וללא-תנאי, לפני שהגיע בכלל לבדיקת Tier-2 (`resolve_pending_lead_preview()`); בוקמארק BUG-115 לא מכסה batch כי זה לא ActionContract בכלל. תוקן עם `should_prefer_batch_preview()` חדש — משווה recency בין שני הבוקמארקים הקיימים (Tier-2's `pending_lead_preview` מול Tier-1's `last_prompted_contract`), נקרא לפני ה-gate הבלתי-מותנה. 11 בדיקות חדשות, full sweep (140 קבצים) ירוק. **הוכחת production (19/07/2026):** batch dictation נכנס עם `live_contracts=9`, batch preview הוצג נכון, "כן" אישר שני לידים בפועל (`recoLSXsLQNKQG6Gy`, `recgwDYidGrTc9KEU`) ללא disambiguation. ראה §4 item 7e ו-`docs/architecture/action-gateway/BUG-117_BATCH_PREVIEW_PRECEDENCE_HIJACK.md`.
- **BUG-104 (Core Reasoning ל-Leads)** — ללא שינוי: Phase 1/1.1/2A.1/2A.2 ממוזג ומאומת ב-tests, `FEATURE_CORE_REASONING_LEADS_STATE` נשאר off/shadow, Phase 2A.0 (ניקוי סכמה) עדיין SPEC-בלבד וממתין להחלטת owner.
- **RP5 (Evidence Finalizer) — runtime shadow observed בלוגי production, enforcement עדיין חסום.** **הוכחת production:** `[EvidenceFinalizerShadow] state=shadow evidence_status=approval_pending response_claim=sent_for_approval mismatch=false code=match counts={'classification': 'approval_pending', 'verified_reads': 0, 'verified_writes': 0, 'failed_calls': 0, 'outcome_unknown': 0, 'approvals_pending': 1, 'unverified_effects': 0}`. `FEATURE_EVIDENCE_FINALIZER`'s ברירת מחדל בקוד `off` — לא אומר ש-shadow לא רץ; הלוג מוכיח שהוא כן רץ. Enforcement (RP5 עצמו) עדיין לא מוכן — הענף המדויק של #393 כבר נלכד (ראו למעלה), אך שאר הדגימות הנדרשות לכל 9 מצבי הסיווג עדיין לא נאספו במלואן.
- **RP5 staging fault-injection helper — קוד + אינטגרציה, branch נפרד `claude/rp5-staging-fault-injection-v4akit` (PR #407), staging-only, ✅ VERIFIED ב-staging (19/07/2026), הקוד עצמו לא ממוזג ולא יתמזג ל-main.** `core/rp5_fault_injection.py` + חיווט צר ב-`tools/dispatcher.py`/`app.py` — נועד לפתור בדיוק את הפער שהבולט הקודם מזהה ("שאר הדגימות הנדרשות לכל 9 מצבי הסיווג עדיין לא נאספו"): מאפשר להפיק דגימות RP5/F52 shadow אמיתיות (google-401/write-403/write-validation-400/write-timeout/tool-empty-response/tool-malformed-response/connection-reset) מונעות-בוט אמיתי, **רק** ב-Render staging. **לא נוגע ב-production בשום צורה** — ארבעה שערי בטיחות (APP_ENV, flag, allowlist, marker מפורש בהודעה) כולם חייבים להתקיים; ברירת מחדל בקוד `RP5_FAULT_INJECTION_ENABLED=false`. אינו הופך `FEATURE_EVIDENCE_FINALIZER`/`FEATURE_UNIFIED_STATUS_FORMATTER` ל-`on`/`enforce` — אלה נשארים בדיוק כפי שהם. ראו `CHANGE_CONTROL_LOG.md`'s C143–C145 (הקוד/הרשומות המקוריות על ענף ה-RP5 עצמו; C143–C145 כאן ב-`main` הם עותק-תיעודי בלבד, ראו למטה).
  - **C144 (integration fix, 19/07/2026):** דגימת staging חיה חשפה שה-marker לא הוסר לפני ניתוב-אישור — "מאשר [rp5-test:write-403]" נכשל ב-exact-match (`is_own_resolution_event()`/`_CONFIRM_WORDS`) וסימן context_interrupted. תוקן: `begin_turn()` מחזיר טקסט נקי, `clean_text_for_routing()` חדש ל-ingress gate שרץ לפני `run_agent()`, ו-per-turn state הוסב ל-stack (מגן מפני nested `run_agent()` calls שמוחקים את מצב ה-fault החיצוני). מוגבל במפורש ל-Telegram — WhatsApp עדיין לא מתוקן לזה. `test_rp5_marker_stripping.py` (11 חדשות) — קיים רק על ענף ה-RP5, לא כאן.
  - **C145 (staging smoke verification, 19/07/2026):** ✅ שני smoke tests, שניהם PASS — עם marker: `[RP5FaultInjection] scenario=write-403` לפני `airtable_add`, אין POST אמיתי; בלי marker: התנהגות רגילה במלואה (`Dispatch→POST 200→executed→claim outcome=completed`), אין שורת RP5 כלל. מאמת activation + marker-stripping + חסימת כתיבה + אינרטיות מלאה בלי marker, קצה-לקצה ב-staging אמיתי.
  - **BUG-118 (side finding, לא חוסם):** תגובת ההצלחה של המסלול הישן ב-`route_confirmation_word()` מדליפה tool_name/record_id גולמיים — במעקב תחת F52 soak.
  - **BUG-119 (side finding, 20/07/2026, לא חוסם, לא תוקן):** turn נטול tool call לגמרי ("😊") קיבל claim הצלחה שהזכיר במפורש משימה ש-RP5's write-403 חסם אמת turn קודם באותה שיחה — **דריסה פעילה של כישלון מתועד**, לא הזיה גנרית. Contract Chain אומת בהרצת קוד ישירה: `_AGENT_ACTION_STATUS_PATTERN`'s "generic structural safety net" (ב-`core/anti_hallucination.py`, שנועד במפורש לתפוס בדיוק את זה) מכסה רק צורת-יחיד לשישה מתוך שבעה פעלי-השלמה — חסרה צורת ריבוי (נוצרו/בוצעו/נשלחו/נשמרו/עודכנו/הושלמו). **לא קשור ל-RP5/F52 taxonomy** — פער כללי ב-A32, יחול זהה בפרודקשן. ראה `BUG_AUDIT_LOG.md` לפירוט המלא + הצעת תרחיש-רגרסיה (cell2b).
- **פער תיעוד — נסגר בעדכון הזה**: `ROADMAP.md`/`CHANGE_CONTROL_LOG.md`/`CHANGELOG.md`/`BUG_AUDIT_LOG.md` כולם סונכרנו עד PR #396.

---

## 2. Current System State

**עובד בפרודקשן, ✅ VERIFIED IN PROD:** Telegram+WhatsApp inbound עם Identity resolution; Airtable Gateway כנתיב-כתיבה יחיד (fail-closed); Approval flow כולל אכיפת TTL על כפתור טלגרם (BUG-112 מנגנון הליבה — evidence: `"⏰ פג תוקף — הפעולה לא בוצעה"`, 0 ביצוע, כפתור נעלם); Daily Digest; Finance Pulse; TMA read path; Cost Watchdog; חילוץ-ליד מ-WhatsApp כולל תיקוני batch/domain/sender-prefix/compact-paste (BUG-111 סבב 1+2 — evidence: paste 3-טלפונים לא יצר עוד ליד מזויף, ביקש 3 שמות); A32 Single-Speaker suppression כולל BUG-113/PR #396 (evidence: הודעה יחידה למשתמש, לא כפולה); RP1 tool-registry invariants (תמיד פעיל); TMA Lead Event Bridge; `lead_conversion.py`/`ad_attribution.py::mark_converted()` כותבים ערכים קנוניים (BUG-110).

**מיושם וממוזג ב-main, merged/tests green, טרם production-verified בנפרד:**
- BUG-112 UX follow-up (PR #394) — נרמול ניסוח stale/missing-callback ל-`_notify_missing_or_expired_callback()` — defensive/idempotency cleanup, לא נדרש לעצם BUG-112 שכבר verified. אין דגימת "missing/already-consumed callback" מפורשת עדיין (הדגימה שסגרה את BUG-112 הוכיחה את נתיב ה-expiry-ידוע, ומדגם מהעולם האמיתי כעת נדיר יותר כי הכפתור נעלם אחרי הלחיצה הראשונה).

**מיושם חלקית / קוד flag-gated `off`, אך עם runtime shadow evidence בלוגי production (לא רק ברירת-מחדל-בקוד):**
- F52 Message Contract Foundation + PR4–PR6 + PR6 follow-up (#381–#385, #389, #392, #393) — כל שכבת ה-shadow logging קוד מוכן, **וגם** נצפתה רצה בפרודקשן: `[UnifiedStatusFormatterShadow]` עבור `outcome=executed`/`rejected`/`pending` שלושתם נצפו בלוגים. `FEATURE_UNIFIED_STATUS_FORMATTER` ברירת המחדל בקוד `off` — אין להסיק מכך "shadow לא רץ"; הלוגים מוכיחים אחרת. **אין הפעלת `on`** — שומרים shadow, ממתינים לחלון soak נקי (ראו §4).
- RP4/RP5 Evidence Finalizer — קוד מוכן, **וגם** shadow evidence אמיתי בלוגים (`[EvidenceFinalizerShadow] ... mismatch=false code=match`). `FEATURE_EVIDENCE_FINALIZER` ברירת מחדל בקוד `off` — אותה הבחנה. Enforcement (RP5) עדיין חסום — ראו §4.
- BUG-104 Core Reasoning (Phase 1/1.1/2A.1/2A.2) — ממוזג ומאומת ב-tests בלבד, אין evidence runtime שנצפה עדיין. `FEATURE_CORE_REASONING_LEADS_STATE` off/shadow.
- RP2/RP3 Tool Availability Filter — off, אין evidence runtime.
- PA-01 structural enforcement — off, ממתין להחלטת shadow rollout.
- Phase 4B Atomic Claims — off, ללא שינוי.
- BUG-104 Phase 2A.0 — SPEC בלבד; ניקוי שדות `tier`/`Domain category`/`Domain risk assessment`/`Domain summary` טרם בוצע.

**חסום:**
- RP5 enforcement — יש כבר shadow evidence כולל הענף המדויק של PR #393 (`evidence_status=mixed response_claim=sent_for_approval mismatch=false`, נלכד 19/07/2026 — ראו §1), אך שאר הדגימות הנדרשות לכל 9 מצבי הסיווג עדיין לא נאספו במלואן לפני שאכיפה אמיתית יכולה להתחיל.
- Decision Hub activation — ממתין ל-production evidence.
- WhatsApp outbound אמיתי — honest stub, ממתין ל-Meta Cloud API.
- BUG-110 חוב טכני: `ad_attribution.py::mark_converted()` לא עובר דרך canonical gateway; `build_attribution_report()`/`audience_intelligence.py` עדיין קוראים `status=="converted"` הישן.
- חוב UX (מ-BUG-111): `resolve_pending_lead_preview()`/`_handle_batch()` עדיין חושפים record_id inline וכותרת שגויה בהודעת batch — לא תוקן, ממתין ל-cutover של הפורמטר המאוחד.
- PR #341 (Single-Speaker fix), C81-FU/C82-FU, רשומת Airtable `recRvK6hFTNgyj8ag` — לא נבדקו/טופלו הסבב הזה.

---

## 3. Completed Since Last Update (18/07 → 19/07, main `2136a14` → `8bdd504`)

1. **PR #389 — F52 PR5: rejection/cancellation replies דרך unified formatter shadow** (`9973cc5`) — `reject()`/`route_cancellation_word()`/`route_combined_word()`'s cancel branch בנו טקסט legacy קבוע בלי מעורבות formatter; נוסף `ActionGateway._render_rejection_reply()` עם אותה מכונת off/shadow/on. לא נוגע ב-Telegram inline-button reject path (פער נפרד, מתועד). `"rejected"` ממשיך למופה ל-`"failure"` הקיים.
2. **PR #390 — BUG-111 follow-up: compact/newline-stripped WhatsApp paste** (`4635bcd`) — paste בלי newline לפני header של batch/chat-export עדיין הפיק שם-ליד מזויף (`"לידים חדשים"`). תוקן: `_BLOCK_SEP`/`_SENDER_LINE_RE` הורחבו, stop-words ברבים נוספו, ו-safety-net חדש ב-`_classify_ingress_core()` — candidate יחיד מבוטל אוטומטית ל-clarification כשיש יותר מטלפון אחד בטקסט. **✅ VERIFIED IN PROD**: paste 3-טלפונים אמיתי לא יצר עוד ליד מזויף, ביקש 3 שמות. 29 בדיקות חדשות.
3. **PR #391 — docs: sync BUG_AUDIT_LOG.md/CHANGELOG.md ל-PR #385–#390** — סגר חלק מהפער התיעודי.
4. **PR #392 — F52 PR6: approval_pending prompt דרך unified formatter shadow + תיקון EvidenceFinalizer/ownership** (`38c2820`) — `_queue_approval_detailed_impl()` שלח hardcoded text בעקיפין ל-`bot.send_message()` בלי מעורבות formatter כלל; נוסף `ActionGateway._render_pending_prompt()`. במקביל תוקנו שני false-positive: `_classify_response_claim()` קיבל `approval_prompt_sent` param, ו-`build_ownership_signal()`'s call site ב-`app.py` מסמן `reply_owner="gateway"` כשה-agent דוכא ע"י A32. **runtime shadow evidence נצפה** (`[UnifiedStatusFormatterShadow] outcome=pending mapped_state=approval_pending`). 50 בדיקות חדשות.
5. **PR #393 — F52 PR6 follow-up: הרחבת taxonomy ל-turns מעורבים** (`53eb19d`) — `"sent_for_approval"` תואם גם ל-`"mixed"` כשה-non-success היחיד הוא `approvals_pending`. **✅ VERIFIED IN PROD (19/07/2026)** — הענף המדויק (`response_claim=sent_for_approval` על turn מעורב) נלכד: `evidence_status=mixed response_claim=sent_for_approval mismatch=false verified_reads=1 approvals_pending=1`, יחד עם דגימת ה-adjacent (`response_claim=mixed`) שכבר הייתה מוכרת. 5 בדיקות הגנה נוספות (55 סה"כ בקובץ).
6. **PR #394 — BUG-112 production follow-up: נרמול UX ל-stale/missing-callback** (`8ac0c93`) — `_notify_missing_or_expired_callback()` חדש מאחד שלושה ניסוחים חופפים לביטוי אחד עקבי. **merged/tests green, defensive/idempotency cleanup** — לא נדרש לעצם BUG-112 שכבר verified; לא נבדק production בנפרד עדיין. 8 בדיקות חדשות (30 סה"כ).
7. **PR #395 — docs: AI_CONTEXT daily briefing ל-PR #388–#393** (`951b1b2`) — רענון שגרתי.
8. **PR #396 — BUG-113: A32 מדכא פרוזת approval-invite כפולה** (`2d86de6`) — production evidence: turn שמבצע approval אמיתי (הודעת gateway "⏳ בקשת אישור" נשלחה) עדיין הראה גם פרוזת agent חופשית ("✅ מוכנה להוספה... שלח מאשר כדי לאשר") ללא דיכוי — `EvidenceFinalizerShadow` דיווח `response_claim=success` נגד `evidence_status=approval_pending` (`mismatch=true`), פער Single-Speaker אמיתי. תוקן: ענף דיכוי חדש הגדור ב-`_gateway_active` וראיית `__approval_queued__` אמיתית. **✅ VERIFIED IN PROD מיד אחרי ה-deploy**, evidence מדויק:
   ```
   [A32] Single-Speaker: agent emitted approval-invite prose after an approval was already queued this turn — suppressing (not replacing with fallback)
   [TurnEnvelope] ownership_signal ... "approval_queued": true, "agent_claimed_approval": false, "reply_owner": "gateway"
   [EvidenceFinalizerShadow] state=shadow evidence_status=approval_pending response_claim=sent_for_approval mismatch=false code=match counts={'classification': 'approval_pending', 'verified_reads': 0, 'verified_writes': 0, 'failed_calls': 0, 'outcome_unknown': 0, 'approvals_pending': 1, 'unverified_effects': 0}
   ```
   הפלט למשתמש הכיל רק את הודעת ה-gateway — אין יותר כפילות. 18 בדיקות חדשות. **סגור.**
9. **סנכרון תיעוד + reclassification runtime evidence (18:16 סבב)** — `ROADMAP.md`/`CHANGE_CONTROL_LOG.md`/`CHANGELOG.md`/`BUG_AUDIT_LOG.md` סונכרנו עד #396, **וגם** BUG-111/BUG-112 (מנגנון ליבה)/F52 shadow/RP5 shadow שודרגו מ-"לא production-verified"/"קוד off" ל-status מדויק המבוסס על לוגי production בפועל, לפי הכלל "Runtime evidence > main code > docs > memory" (ראו הערה בראש המסמך). PR #393 ו-PR #394 **לא** overclaimed — כל אחד סומן במדויק למה שיש לו הוכחה ולמה שחסר.
10. **PR #399 — BUG-113 follow-up: תיקון פערי markdown-emphasis וצורה זכר ב-A32** (`72414c3`) — דגימת production חדשה חשפה שדיכוי BUG-113 (PR #396) לא ירה עבור `שלח **מאשר**` (bold markdown, שתי כוכביות — ה-regex המקורי סבל רק כוכבית אחת) וגם לא עבור `ממתין` בצורת זכר בלי סיומת (`_AGENT_PENDING_STATUS_PATTERN` דרש סיומת נקבה `ה`/`ת`, אבל `ממתין` הבודד מסתיים ב-nun סופית ן, אות יוניקוד שונה מה-נ הרגילה בתוך `ממתינה`/`ממתינת` — לא רק "סיומת אופציונלית"). תוקן עם `_strip_markdown_emphasis()` חדש (מסיר `*`/`_` לצורך matching בלבד, בלתי-תלוי-בכמות — סוגר את הפתח ל-`***מאשר***`/`_מאשר_` וכו') ואלטרנציה מפורשת `(?:ממתינ[הת]|ממתין)` במקום סיומת אופציונלית שגויה. 28 בדיקות (10 חדשות).
11. **PR #400 — TurnOwnershipShadow: אינווריאנט shadow חדש לזליגת agent ב-turn בבעלות gateway** (`c31c219`) — תצפית טהורה, לא flag-gated: כש-`approval_queued=true` ו-`reply_owner="gateway"` אך `final_reply` (אחרי A32) עדיין לא ריק, נרשם `[TurnOwnershipShadow] violation=agent_spoke_in_gateway_owned_approval_turn` + `pattern_class` (approval_invite/pending_status/action_status/unknown). אין שינוי התנהגות, אין suppression חדש, אין נגיעה ב-ActionGateway/EvidenceFinalizer taxonomy. `OwnershipSignal` קיבל שני שדות חדשים (`final_reply_nonempty`, `leaked_pattern_class`). 95 בדיקות (25 חדשות).
12. **סגירת פער PR #393 (19/07/2026, אחרי #400)** — דגימת production חדשה (Eli: "תבדוק מה הסטטוס של המשימה בדיקת pull request 393... הוסף לה עדכון") לכדה בדיוק את הענף המדויק שהיה חסר: `evidence_status=mixed response_claim=sent_for_approval mismatch=false verified_reads=1 approvals_pending=1`. אותה דגימה גם אימתה `TurnOwnershipShadow` (PR #400) עובד נכון בפרודקשן — `final_reply_nonempty=false`, אין violation, כי A32 דיכא נכון (`[A32] Single-Speaker: agent emitted action-status text after an approval was already queued`). ראו §1 ו-`CHANGE_CONTROL_LOG.md` C127.

**נצפה בו-זמנית, לא טופל בסבב זה — נושא נפרד:** באותה דגימת production נראה burst של קריאות Airtable חוזרות (GET כפול לאותו `contract_id`, מספר קריאות `GET /meta/bases/.../tables`, ו-`SessionStore` טען 18 שורות session עבור sender יחיד). שונה מ-C118-ish read-amplification הישן שכבר תוקן (15/07/2026, commit `a397508`, "Fix Phase 0 read amplification: 3→1") — זו תבנית קריאות חדשה/נפרדת, טרם אובחנה. מחכה להחלטה אם לפתוח audit.

13. **PR #402 — BUG-114 audit + narrow fix: ActionContracts context-interrupt call amplification** (`f0cf2e1` audit, `19717b0` fix, merge `6d62852`) — burst שנצפה ב-item 12 אובחן: `ExecutionLedger.mark_context_interrupted()` (`core/action_gateway.py:558`) סימן מחדש **כל** contract pending, כולל כאלה שכבר `context_interrupted=True`, על כל הודעה בלתי-קשורה. תוקן עם `and (c.reconfirmation_required or not c.context_interrupted)` — **לא** ה-filter הנאיבי `and not c.context_interrupted` שנשקל ב-audit; זה נתפס תוך כדי כתיבת בדיקות כשובר את FSM האישור-החוזר הקיים (BUG-108) — contract עם `reconfirmation_required=True` חייב עדיין supersede אמיתי גם אם `context_interrupted` כבר True מהפרעה קודמת. 12 בדיקות חדשות + 27 בדיקות רגרסיה קיימות ירוקות, אין נגיעה ב-TOCTOU read/readback של `transition()`. **✅ VERIFIED IN PROD** — דיווח מפורש: אין יותר burst חוזר.
14. **PR #403 — BUG-115 audit + fix: confirmation-word routing hijack** (branch `claude/bug115-confirmation-routing-audit`, merge `4ce2fae`) — אותה דגימת production חשפה ממצא נפרד (root cause משותף, לא מעורבב בתיקון): "כן"/"מאשר" אחרי תצוגת "📋 זיהיתי ליד..." (contract Tier-1 אמיתי, BUG-056) נחטף ל-disambiguation גנרי של contracts ישנים ולא-קשורים, כי `route_confirmation_word()` סופר contracts חיים בלבד. `TurnEnvelope.active_queue_id` נבדק ונשלל כגורם בפועל (Phase-0 תצפית-בלבד). תוקן: bookmark "contract שהוצג לאחרונה" חדש ב-`session_store.py` (`set_last_prompted_contract`/`get_last_prompted_contract`/`clear_last_prompted_contract`, TTL 600s, נבדק לפני ספירה גנרית ב-`route_confirmation_word()`), נקבע גם מ-`core/lead_candidate_handler.py::_handle_single_candidate()` (lead preview) וגם מ-`app.py::_queue_approval_detailed_impl()` (ActionGateway approval כללי). תוויות disambiguation גולמיות (`airtable_add`/id) הוחלפו בפונקציה נפרדת `_describe_contract_for_disambiguation()` — **לא** הרחבת `_describe_contract_for_reconfirmation()` המשותפת, כי ניסיון ראשון לעשות זאת שבר את `test_stage_b_full_suite.py`'s DoD20 (נתפס ב-full regression sweep, תוקן). 22 בדיקות חדשות (`test_bug115_confirmation_routing_bookmark.py`), full sweep ירוק. **✅ מוזג ל-main, ✅ VERIFIED IN PROD (19/07/2026)** — דגימה עם `live_contracts=10` אישרה שהבוקמארק פותר את ה-contract הנכון ולא נופל ל-disambiguation. ראה §4 item 7d/9.
15. **PR #404 — BUG-116 audit + fix: Tier-4 `_AIRTABLE_ID_RE` תופס מילים אנגליות רגילות** (branch `claude/action-status-shadow-verification-m1m0ow`, merge `0ef018f`) — דגימת production נפרדת: הודעת ליד תקינה לחלוטין ("צור ליד חדש לדומיין recruitment... יהודה גרוס 0533968395") נחסמה עם "📄 זה נראה כמו טבלה" על 3 ניסיונות, לפני שחילוץ מועמדים בכלל רץ. Root cause מאומת בהרצה ישירה: `_AIRTABLE_ID_RE = re.compile(r"\b(?:fld|rec)[A-Za-z0-9]{8,}\b")` ללא גבול עליון תופס כל מילה אנגלית שמתחילה ב-`rec`/`fld` ומלווה ב-8+ אותיות — `recruitment` = `rec`+`ruitment`. ניסיון ראשון (גבול-אורך מדויק כמו רגקסי-ID אמיתיים במקומות אחרים בקוד) נדחה — היה שובר fixture קיים ב-`test_c89_tier4_precedence.py` (זנב 13 תווים, לא 14). תוקן עם lookahead הדורש ספרה אחת לפחות ברצף — אומת מול כל fixture ID אמיתי בסוויטה (כולם מכילים ספרות; מילה אנגלית לעולם לא). 15 בדיקות חדשות (`test_bug116_airtable_id_word_false_positive.py`), full sweep (138 קבצים) ירוק. נושא נפרד לגמרי מ-BUG-114/115 — Tier-4 ingress-classification, לא ActionGateway/ActionContract. **✅ מוזג ל-main, ✅ VERIFIED IN PROD (19/07/2026)** — דגימה שנייה בניסוח שונה ("domain recruitment") הראתה tier=1 נכון, תצוגת-ליד תקינה, ו-"✅ בוצע" עם `recNhWVHDd9Noeql1`. ראה `docs/architecture/ingress-classifier/BUG-116_AIRTABLE_ID_REGEX_WORD_FALSE_POSITIVE.md`.

16. **BUG-115 production verification (19/07/2026, אחרי מיזוג PR #404)** — דגימה עם `[TurnEnvelope] case_c_signal kind=C1 detail=live_contracts=10` (10 contracts pending בו-זמנית) הראתה "כן" על ליד טרי נפתר ישירות מול ה-contract הנכון (`approved: contract=4c7b539b-3df4-4116-8caa-80b6b7c84843` → `Dispatch airtable_add → POST /Leads 200 OK → executed: external_id=rec34IdTmCFVbRABo` → `"✅ בוצע: יצירת ליד: יצחק גלבר, 0527696084, general"`) ללא תפריט disambiguation — התנאי `live_contracts>1` המפורש שהיה חסר לסגירת BUG-115. **BUG-115 עכשיו ✅ VERIFIED IN PROD / CLOSED.** גם F52 executed-shadow נקייה (`outcome=executed mapped_state=success`, ללא דגלי leak); **לא** נספרת כדגימת RP5/EvidenceFinalizer בהיעדר שורת `EvidenceFinalizerShadow` תואמת לאותו turn — סטטוס RP5 לא עודכן על סמך הדגימה הזו.

17. **PR #405 — BUG-117 audit + fix: Tier-2 batch lead-preview precedence hijack** (branch `claude/action-status-shadow-verification-m1m0ow`, merge `4546880`) — אותה שיחת production שסגרה את BUG-115 חשפה ממצא נוסף, נפרד: batch preview (2 לידים) נחטף לאותה disambiguation גנרית — הבוקמארק של BUG-115 לא מכסה אותו כי batch preview הוא מנגנון Tier-2 נפרד (`session_store.py`'s `pending_lead_preview`), לא ActionContract. Root cause: `app.py`'s `_CONFIRM_WORDS` בדק Tier-1 (`find_live_contracts()`) תמיד ראשון וללא-תנאי לפני שהגיע ל-Tier-2. תוקן עם `should_prefer_batch_preview()` חדש (`core/lead_candidate_handler.py`) — משווה recency בין שני הבוקמארקים הקיימים, נקרא לפני ה-gate הבלתי-מותנה. 11 בדיקות חדשות (`test_bug117_batch_preview_precedence.py`), הרחבת חלון-תווים בבדיקה מבנית קיימת (`test_c89_preview_confirmation.py`), full sweep (140 קבצים) ירוק. **✅ מוזג ל-main, ✅ VERIFIED IN PROD (19/07/2026)** — batch dictation אמיתי עם `live_contracts=9`, "כן" אישר שני לידים בפועל ללא disambiguation. ראה §4 item 7e ו-`docs/architecture/action-gateway/BUG-117_BATCH_PREVIEW_PRECEDENCE_HIJACK.md`.

**פער תיעוד היסטורי שנשאר פתוח (לא נסגר, מסומן בכוונה):** `CHANGELOG.md` עדיין חסר itemization נפרד ל-#348–#353 (PA-01); `CHANGE_CONTROL_LOG.md` עדיין חסר רשומות #327–#353 אחרי C111.

---

## 4. Next Priorities

1. **docs sync ל-runtime evidence** — ✅ הושלם בסבב הזה (עדכון זה עצמו).
2. **shadow soak / חלון תצפית** — להמשיך לצבור דגימות `[UnifiedStatusFormatterShadow]`/`[EvidenceFinalizerShadow]` מפרודקשן על פני חלון זמן, לא רק דגימות בודדות.
3. **ניטור אקטיבי במהלך ה-soak** — לעקוב אחרי כל אחד מהדגלים הבאים בלוגי shadow: `mismatch=true`, `record_id_leak=True`, `tool_name_leak=True`, `contract_id_leak=True`, `fallback_used=True`. כל הופעה דורשת חקירה לפני שממשיכים.
4. **רק אחרי חלון נקי** — לשקול הפעלת `FEATURE_UNIFIED_STATUS_FORMATTER=on` (לא לפני). זו החלטת operator/owner, לא אוטומטית.
5. **RP5 enforcement נשאר חסום** — הענף המדויק של PR #393 **כבר נלכד** (19/07/2026, ראו §1/§3), אך שאר הדגימות הנדרשות לכל 9 מצבי הסיווג עדיין לא נאספו במלואן.
6. **החלטת owner: BUG-104** — הפעלת `FEATURE_CORE_REASONING_LEADS_STATE` (קוד מוכן ומאומת) וניקוי סכמת Phase 2A.0 (`tier`/`Domain category`/`Domain risk assessment`/`Domain summary`).
7. **production verification: PR #394 (BUG-112 UX cleanup)** — ממתין לדגימת "missing/already-consumed callback" אמיתית אחרי ה-deploy הזה.
7c. **BUG-116 — ✅ VERIFIED IN PROD / CLOSED.** ראה §1/§3 item 15.
7d. **BUG-115 — ✅ VERIFIED IN PROD / CLOSED.** ראה §1/§3 item 14/16 — דגימה עם `live_contracts=10` המפורש שהיה חסר סיפקה את ההוכחה הנדרשת.
7e. **BUG-117 — ✅ VERIFIED IN PROD / CLOSED.** דגימת production (19/07/2026): "📋 זיהיתי 2 לידים אפשריים בקבוצה... ענה כן לשמירת כולם" ואז "כן" נותב ל-"יש כמה פעולות הממתינות לאישור" (9 contracts ישנים), במקום לאשר את שני הלידים בקבוצה. Root cause מאומת בקוד: `app.py`'s `_CONFIRM_WORDS` בדק Tier-1 (`find_live_contracts()`) **תמיד ראשון וללא-תנאי**, לפני שהוא בכלל מגיע לבדיקת ה-Tier-2 `pending_lead_preview` (`resolve_pending_lead_preview()`) — התיעוד הקיים (`core/lead_candidate_handler.py`, docstring של BUG-058) הניח במפורש "Tier 1 מנצח תמיד", הנחה ששברה כבר עבור Tier-1-מול-Tier-1 (BUG-115) אך מעולם לא תוקנה עבור Tier-1-מול-Tier-2. הבוקמארק של BUG-115 (`last_prompted_contract`) לא מכסה batch preview כי זה לא ActionContract בכלל. **תוקן:** פונקציה חדשה `core.lead_candidate_handler.should_prefer_batch_preview()` משווה recency בין `get_pending_lead_preview()`'s `set_at` (TTL 1800s) ל-`get_last_prompted_contract()`'s `set_at` (TTL 600s) — נקראת ב-`app.py` **לפני** ה-gate הבלתי-מותנה של Tier-1, מעדיפה Tier-2 רק כשהוא באמת טרי יותר. שני מנגנוני ה-TTL הקיימים לא שונו. נדרשה הרחבת חלון-תווים בבדיקה מבנית קיימת (`test_c89_preview_confirmation.py`, 3000→5000/5000→6500) — האינווריאנט עצמו לא השתנה. 11 בדיקות חדשות (`test_bug117_batch_preview_precedence.py`), full sweep (140 קבצים) ירוק, כולל `test_c89_preview_confirmation.py`/`test_bug115_confirmation_routing_bookmark.py` ללא רגרסיה. מחוץ לסקופ במפורש: `_CANCEL_WORDS`/`route_cancellation_word()`. **✅ אומת בפרודקשן (19/07/2026):** batch dictation נכנס עם `live_contracts=9`, batch preview הוצג נכון, "כן" אישר שני לידים בפועל ללא disambiguation (`recoLSXsLQNKQG6Gy`, `recgwDYidGrTc9KEU`). הערת דיוק: שורת ה-`live_contracts` נלכדה ל-turn של ה-batch dictation, לא ל-turn של "כן" עצמו — נחשב מספיק כי אין דרך טכנית ל-Tier-2 לשנות ספירת contracts בין שתי הודעות רצופות. ראה `docs/architecture/action-gateway/BUG-117_BATCH_PREVIEW_PRECEDENCE_HIJACK.md`.
8. **BUG-114 — ✅ VERIFIED IN PROD / CLOSED.** ה-burst שנצפה 19/07/2026 אובחן ותוקן בנפרד: `ExecutionLedger.mark_context_interrupted()` (`core/action_gateway.py:558`) סינן רק `status=="pending"`, לא `context_interrupted` — כל contract pending ספג GET+PATCH+GET מיותר על כל הודעה בלתי-קשורה, גם כשכבר `context_interrupted=True`. **תיקון שיושם שונה מההצעה המקורית בביקורת** — `and (c.reconfirmation_required or not c.context_interrupted)`, לא `and not c.context_interrupted` הפשוט, כי הצורה הפשוטה נתפסה תוך כדי כתיבת בדיקות כשוברת רגרסיה קיימת (contract עם `reconfirmation_required=True` חייב עדיין supersede אמיתי, גם אם `context_interrupted` כבר True מהפרעה קודמת — אחרת FSM האישור-החוזר של BUG-108 נשבר בשקט). 12 בדיקות חדשות (`test_bug114_context_interrupt_amplification.py`) + 27 בדיקות רגרסיה קיימות (`test_bug_reconfirmation_oneshot_fsm.py`) ירוקות. אין נגיעה ב-GET-before/after PATCH של `transition()` (TOCTOU/readback, לא מוחלשים). שתי המלצות נוספות (reuse PATCH response; מדיניות TTL ל-contracts ישנים — שאלה 6 באותו audit, "למה 6 contracts חיים בו-זמנית") נשארות מחוץ לתיקון, ממתינות להחלטה נפרדת. **✅ אומת בפרודקשן (19/07/2026):** דיווח מפורש — אין יותר burst חוזר של GET→PATCH→GET על contracts שכבר `context_interrupted=True`. ראו `docs/architecture/action-gateway/BUG-114_CONTEXT_INTERRUPT_CALL_AMPLIFICATION_AUDIT.md`'s §6.
9. **BUG-115 — ✅ VERIFIED IN PROD / CLOSED.** אותה דגימת production ("צור ליד חדש... כן") חשפה ממצא **נפרד** מ-BUG-114 (לפי בקשה מפורשת לא לערבב, למרות שורש-תרומה משותף): תצוגת "📋 זיהיתי ליד..." יוצרת contract Tier-1 אמיתי (עיצוב BUG-056 מכוון), אבל "כן"/"מאשר" שאחריה נותב ל-disambiguation גנרי של contracts ישנים ולא-קשורים (עד 9 בדגימה שנייה) במקום לאשר את הליד שהוצג הרגע — `route_confirmation_word()` סופר contracts חיים בלבד, ללא זיהוי "לאיזה contract ה-'כן' מתייחס". גם חשף tool_name/id גולמיים למשתמש. `TurnEnvelope.active_queue_id` נבדק ונשלל כגורם בפועל (תצפית-בלבד, Phase 0). **תוקן:** bookmark "contract שהוצג לאחרונה" חדש ב-`session_store.py` (TTL 600s, נבדק ב-`route_confirmation_word()` לפני ספירה גנרית) + פונקציה נפרדת `_describe_contract_for_disambiguation()` לתוויות אנושיות (לא הרחבת הפונקציה המשותפת — ניסיון ראשון שבר בדיקה קיימת, `test_stage_b_full_suite.py`'s DoD20, נתפס ותוקן ב-full sweep). 22 בדיקות חדשות + BUG-114/reconfirmation-FSM נבדקו מחדש ללא רגרסיה. **✅ אומת בפרודקשן (19/07/2026):** דגימה עם `[TurnEnvelope] case_c_signal kind=C1 detail=live_contracts=10` — "כן" על ליד טרי נפתר ישירות מול ה-contract הנכון (`approved: contract=4c7b539b...` → `executed: external_id=rec34IdTmCFVbRABo` → `"✅ בוצע: יצירת ליד: יצחק גלבר, 0527696084, general"`) ללא תפריט disambiguation, למרות 10 contracts pending בו-זמנית — התנאי `live_contracts>1` המפורש שהיה חסר עד עכשיו. גם F52 executed-shadow נקייה (`outcome=executed mapped_state=success`, אין דגלי leak); **לא** נספרת כדגימת RP5/EvidenceFinalizer בהיעדר שורת `EvidenceFinalizerShadow` תואמת. ראו `docs/architecture/action-gateway/BUG-115_CONFIRMATION_ROUTING_HIJACK_AUDIT.md`'s §7.
